# ===== Python =====
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
.mypy/

# 虚拟环境
env/
venv/
.venv/
ENV/

# ===== Node =====
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-store/

# ===== 环境变量 =====
# 本地覆盖文件不入库；各环境基线配置（.env.example / .env.development / .env.test / .env.production）入库
.env
.env.local
.env.*.local
# ⚠️ 轮换 Key 时的**备份形态**也必须排除。2026-09-16 实测踩到：
#    `.env.local` 被排除了，但 `.env.local.bak-<ts>` **没有** ——
#    旧 Key 于是变成一个未跟踪文件（`git status` 里显示 `??`），
#    谁顺手 `git add -A` 就把它提交上去了。备份只在**写盘那一刻**看起来无害。
.env*.bak
.env*.bak-*
.env.local.backup*

# ===== IDE / 编辑器 =====
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ===== 微信小程序 =====
miniapp/node_modules/
miniapp/project.private.config.json

# ===== 日志与临时文件 =====
*.log
logs/
tmp/
temp/
*.tmp

# ===== 真机走查产物（截图 / summary.json，只作过程证据，不入库）=====
miniapp-device-artifacts/

# ===== 脚本跑分产物（如 artifacts/h7a.json）=====
# 与上一节同理：机器产出的运行结果不入库，**结论写进文档**
# （`docs/entrust/H7a-真实模型质量验证包.md` §5 已把这些数字落纸）。
# 之前 `artifacts/` 既没被忽略也没被跟踪 —— 于是"按文档跑一次"就会让工作区变脏，
# 而 `git add -A` 会把结果文件一起提交。约定含糊本身就是个坑。
artifacts/

# ===== 本机工作目录（WorkBuddy 在仓库根留下的 `.workbuddy/`）=====
# 里面是跑门禁 / 走查 / 开 PR 时用的**一次性**脚本与探测输出（路径写死本机、
# 含进程与端口探测、含 PR 草稿）。与上一节的差别：`artifacts/` 是**程序**产出的
# 结果，这里是**人手**留下的工具 —— 同样都不该进版本库。
# 立项时留的疑问「项目级技能目录要不要入库」已核实：仓库根 `.workbuddy/` 下
# **没有** `skills/`、也**没有** `memory/`（那两处在本仓库**之外**的工作区
# `E:\PL-Ship-Broker\.workbuddy\`）⇒ 实测 99 个文件全部是一次性工具，整目录忽略。
# 它此前**既未忽略也未跟踪** ⇒ `git add -A` 会把它们一起提交上去（与 `.env.local.bak-*`
# 同型的坑：文件本身无害，但"顺手 add 全部"这个动作会把它变成已提交的噪声）。
.workbuddy/

# ===== SQLite 本地库（开发/测试） =====
*.db
*.sqlite3

# ===== 附件存储（公共静态路径之外，不入库） =====
# ATTACHMENT_STORAGE_DIR 默认指到这里；上传的客户材料绝不进版本库
backend/var/
var/attachments/

# ===== 密钥与证书 =====
*.pem
*.key
*.p12
*.pfx
!*.example.pem
