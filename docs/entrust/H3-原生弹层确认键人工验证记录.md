# H3 · 原生弹层确认键：两类证据与人工验证记录

状态：**两类证据齐备，且 ② 已升级为人手点击（2026-09-16）** ——
① 自动化回调由真机走查 ⑧b 产出；
② 真实点击**跑了两轮**：第 1 轮由本机自动化输入（Win32 `SendInput`）执行、
第 2 轮由**人手**执行。两条**分别记录、互不替换**（见「记录表」与两节执行方式说明）。

为什么要写两行而不是把第 1 行改掉：两轮证的是**同一件事**（原生确认键点得动，
且点击后走同一段业务代码），但**执行者不同**。把自动化那行改写成"人手点过"
＝ 用一个未验证的表述盖住一个未验证的事实，正是 H3 要防的事。

历史口径（时间倒序保留，供追溯）：
`② 真实点击已执行，但执行者是自动化输入、非人手` →
`模板 + 自动化证据已落地；人工点击证据待执行`。

## 为什么这件事需要"两类证据"

支付确认键在原生 `wx.showModal` 里 —— **原生弹层不在小程序渲染树中**，
真机走查工具点不到它的确认键（`.weui-dialog*` 命中 0、tap 确认键失败）。
于是这一条路径天然存在**证据缺口**：自动化拿不到"键可点"的证据。

本项目的处置是**把业务动作从弹层回调里抽出来**，让"确认之后要跑的代码"
有第二个可触发入口，然后两类证据**分开记、互不替代**：

| 证据 | 来源 | 怎么拿到 | 证明了什么 | **不证明**什么 |
|---|---|---|---|---|
| ① 自动化回调 | `source='auto'` | 走查 ⑧b 用 `callMethod('onPayConfirm', ['auto'])` 触发，读 `payConfirmTrace` | 确认回调**这段代码的逻辑正确**（发起支付、幂等、留痕） | 键能不能点 |
| ② 人工点击 | `source='modal'` | 人点真实弹层的「确认支付」，按本文填表 + 截图 | **原生确认键点得动**，且点击后走的是同一段业务代码 | 每次回归都能自动复跑（人工不可自动化） |

⚠️ **两类不许合并成一条**：把"自动化跑通了"记成"确认键已验证"，
等于用一个无法覆盖该事实的证据去盖住一个未验证的事实 —— 这正是 H3 要防的事。

可观测点的位置（`miniapp/pages/trade/payment/payment.js`）：

* `onPayConfirm(source)` —— 业务动作本身，`source ∈ {modal, auto}`；
* `_tracePayConfirm(source, payId)` —— 写两处痕迹：
  * 页面 `data.payConfirmTrace`（页面内可读，走查用）；
  * `getApp().globalData.payConfirmTraces`（**跨页面**可读，人工点击的证据落点 ——
    页面 data 会随页面销毁消失，globalData 不会）；
* 痕迹是**追加**不是覆盖：同一轮里人工与自动化可能各来一次，覆盖会抹掉另一条证据。

`payConfirmTrace` **不参与渲染**，页面不得读它改变界面 —— 否则"有没有点过"
会反过来影响被测对象，证据就污染了被测对象。

## 人工验证怎么执行（每次涉及支付确认键的改动都要跑一次）

前置：真机走查环境已就绪（后端已起、演示数据已铺、订单处于 `matched` 待支付）。

1. 进入**订单页**，对待支付订单点「去支付」→ 进入支付详情页；
2. 点「模拟支付」→ 原生弹层出现（标题「确认支付运费」，确认按钮「确认支付」）；
3. **截图**：弹层出现、金额与文案正确 → 记为 `H3-01-弹层`；
4. 点「确认支付」；
5. **截图**：Toast「支付成功」+ 页面状态变为「已支付」→ 记为 `H3-02-确认后`；
6. 取痕迹确认来源（在开发者工具控制台执行）：

   ```js
   getApp().globalData.payConfirmTraces
   // 期望：至少一条 { source: 'modal', payId: <n>, at: <ts> }
   ```

7. 填下表。

## 记录表

| # | 日期 | 执行人 | 环境（IDE 版本 / 真机 or 模拟器） | 订单 id | 支付单 id | `source` | `at`（痕迹里的时间戳） | 截图编号 | 结论 |
|---|---|---|---|---|---|---|---|---|---|
| 1 | 2026-09-16 | Agent（`_h3_realclick.py` + Win32 `SendInput`，OS 级真实鼠标事件，**非人手**） | 微信开发者工具 `Stable 2.02.2608070` / 开发者工具**内置模拟器**（未用物理真机） | 2 | 2 | `modal` | `1789500214181`（= 2026-09-16 03:23:34.181 +08:00） | `H3-01-弹层` / `H3-02-确认后` | 通过 ⓐ |
| 2 | 2026-09-16 | **人手**（用户亲手点；脚本 `_h3_human_click.py` 全程**零点击调用**，且启动时把 `_win_click.click` 换成抛异常的守卫） | 微信开发者工具（与第 1 行同机；本轮日志未单独记录版本号，故不臆写）/ **内置模拟器** | 2 | 2 | `modal` | `1789506169828`（= 2026-09-16 05:02:49.828 +08:00） | `H3B-00-弹层-模拟器` / `H3B-01-弹层-OS截屏` / `H3B-01b-定位叠加` / `H3B-02-确认后-OS截屏` / `H3B-02-确认后-模拟器` | 通过 ⓑ |

**不通过**时另起一段写清：现象、截图、控制台输出、是否复现稳定 ——
不要把"不通过"改写成"通过"或删行，这张表是证据，不是进度条。

ⓐ 通过的是**事实**「原生确认键点得动」；执行者与模板设定的「人点」不同，披露见下节。

ⓑ 这一行的执行者是**人手**，依据是三条**结构性**证据（取自代码与运行时状态，不是自述）：

1. **脚本点不了也动不了鼠标** — 全文无 `SendInput` / `mouse_event` / `SetCursorPos`
   调用，启动时把 `_win_click.click` 替换为会抛 `AssertionError` 的守卫；
   结果文件记 `automated_clicks: 0`、`cursor_moved_by_script: false`。
2. **光标是外来的** — 提示人手时记录光标在 `[989, 226]`，检出点击时光标在 `[3166, 564]`，
   正落在确认键屏幕坐标 `[3186, 567]` 上。脚本不移动光标 ⇒ 它只能是外部输入搬过去的。
3. **痕迹与后端状态独立成立** — 点击前 `modal` 痕迹数为 0（`modal_traces_before_n: 0`，本轮新起 IDE），
   点击后出现 `{source: 'modal', payId: 2, at: 1789506169828}`；该痕迹只可能由弹层自身的
   `success({confirm: true})` 回调产生（脚本没有 `callMethod('onPayConfirm', …)`）。
   同时后端 `GET /payment/payments/order/2` 由 `pending` → `paid` —— 独立于页面自述。

留存的一点残余不确定性，如实写出：`source='modal'` 里的 `modal` 描述的是**触发来源**
（弹层回调），不是**输入设备**；把这次输入归给人手，靠的是上面第 1、2 条排除法
（脚本自身无能力，光标却在等待窗口内落到按钮上）+ 本轮确实请了人点。
若日后需要更强的输入设备归属，可加装 OS 级低级鼠标钩子直接记录物理事件。

## 第 1 轮执行方式说明（2026-09-16 03:23，自动化输入）

**执行者是自动化输入，不是人手。** 单列一节写清，是因为表格「执行人」列若只写
"已执行"，读者会默认它是人点的 —— 那就成了用一句模糊表述盖住一个未验证的事实，
正是 H3 要防的事。

* **做了什么**：在原生 `wx.showModal` 已显示于开发者工具模拟器之后，用 Win32
  `SendInput` 朝**该弹层「确认支付」按钮的屏幕坐标**发了一次真实鼠标
  （move + down + up）。操作系统层面这就是一次普通点击，与手指按下去是**同一条输入路径**。
* **与走查工具的区别**：走查（`wechatide`）点不到它 —— 原生弹层不在小程序渲染树里，
  `.weui-dialog*` 命中 0。本轮走的是**操作系统输入**通道，不是工具 API，所以够得着弹层；
  但它也确实不是"人在界面上点"。
* **为什么这条证据仍然成立**：② 要证明的事实是「原生确认键**点得动**，
  且点击后走的是同一段业务代码」。证据链三段，缺一段都不算：

  1. 痕迹 `{source: 'modal', payId: 2, at: 1789500214181}` —— 只可能由弹层自己的
     `success({confirm: true})` 回调产生。脚本全程只发鼠标事件，
     **没有** `callMethod('onPayConfirm', …)`；本轮还是新起的 IDE，不存在上一轮的旧痕迹
     （`globalData` 随进程消失，且痕迹里 `payId` 与目标支付单一致）。
  2. 后端 `GET /payment/payments/order/2` 由 `pending` → `paid` —— **独立于页面自述**
     的证据。页面说自己成功不算，服务端状态变了才算。
  3. 定位叠加图 `H3-01-弹层-定位叠加.png` —— 红框套住弹层、绿十字落在「确认支付」
     文字簇上，说明点的是**这个键**，不是蒙对的坐标。

* **仍然不同的那一半**：`source='modal'` 里的 `modal` 描述的是**触发来源**（弹层回调），
  不是**输入设备**。要一份物理手指点过的记录，按上节步骤复跑即可 ——
  两者证的是同一件事，不必二选一，也不必把这一行改写成"人手点过"。
  ⇒ 该复跑已于**同日 05:02 由人手完成**，结果记在记录表第 2 行，执行方式见下节。
* **不瞒的一点观察**：点击生效后，模拟器画面上那个弹层**没有收回去**
  （`H3-02-确认后.png` 里弹层仍在，而它背后的页面已是「已支付」）。
  这不影响上面的结论（痕迹与后端状态都已变），但它说明**弹层的关闭与业务动作
  在模拟器里不是同一时机** —— 看截图的人不必因此怀疑点击没生效。

第 1 轮存证目录：`miniapp-device-artifacts/h3-modal-20260916-031253/`
（`H3-01-弹层.png`、`H3-01-弹层-定位叠加.png`、`H3-02-确认后.png`、
`H3-02-确认后-模拟器.png`、`H3-result.json`、`H3-run.log`）。
该目录在 `.gitignore` 内，与既有 `walk-*` 产物同一约定：**截图不进库，文档记结论**。

## 第 2 轮执行方式说明（2026-09-16 05:02，人手点击）

**执行者是人手。** 与第 1 轮的区别只有"谁输出的这次点击"，
被证明的事实完全一致 —— 所以证据链可以复用，**但"人手"这个属性必须另拿证据**，
不能因为"同一件事"就顺手继承。

* **脚本侧的结构性保证**（这是它敢叫"人手证据"的前提）：`.workbuddy/_h3_human_click.py`
  里没有任何点击调用；启动时把 `_win_click.click` **替换成抛 `RuntimeError` 的守卫**
  （即便有人后来误加调用也会立刻炸，而不是静默生效）；全程不移动光标，
  只在最外层轮询痕迹。结果文件自述 `automated_clicks: 0`、`cursor_moved_by_script: false`。
* **时间线**：弹层出现并存证 → 04:59:50 打印提示并进入等待（此时光标 `[989, 226]`）
  → 05:02:49.828 检出 `modal` 痕迹（等待 **179.5s**，轮询 93 次）→ 后端 `pending → paid`。
* **点击落点**：光标在检出时刻为 `[3166, 564]`，确认键屏幕坐标 `[3186, 567]`
  —— 相差 20px，落在按钮文字簇内（定位叠加图 `H3B-01b-定位叠加.png` 的红框与绿十字）。
* **等待上限**：900s。**没点就记 `timeout-no-human-click`** —— 不写成"失败"，
  更不写成"通过"（脚本退出码已按此约定设计：没等到痕迹 ⇒ 不出"已生效"结论）。

第 2 轮存证目录：`miniapp-device-artifacts/h3-human-20260916-045759/`
（`H3B-00-弹层-模拟器.png`、`H3B-01-弹层-OS截屏.png`、`H3B-01b-定位叠加.png`、
`H3B-02-确认后-OS截屏.png`、`H3B-02-确认后-模拟器.png`、`H3-human-result.json`、`READY`）。
同样在 `.gitignore` 内。

⚠️ **下一步要做的事**：把"人手点击"这条通道**沉淀成可复跑的脚本 + 固定的提示话术**
（现在是 `.workbuddy/` 下的一次性脚本，换台机器就没了）。脚本的判定逻辑里有一处
值得保留的设计：**只认"等待期间新出现的痕迹"**（`traces_before_n` / `modal_traces_before_n`
两次基线），而不是"有没有痕迹" —— 否则上一轮遗留的痕迹会被当成这一轮的点击。

## 与自动化证据的对应关系

* 走查 ⑧b 会产出 3 条自动化断言（回调可触发且留痕 / 重复触发不重复记账 /
  `globalData` 能读到痕迹）以及 1 条 `LIMITATION`（原生确认键未真实点击）。
  ⚠️ 那条 `LIMITATION` **保持原样，不得改写成通过** —— 它描述的是**走查工具**的能力边界
  （工具点不到原生弹层），这个边界没有因为本轮而消失：下次跑走查，⑧b 在弹层点击这一格
  依然记 `LIMITATION`。本记录表补的是**另一条通道**的证据，不是把那一格涂绿。
* 本表 `modal` 行于 2026-09-16 填上（第 1 行自动化输入、第 2 行**人手**）⇒ H3 两类证据齐备，
  且"人手点击"这一格已落实。**当前汇报口径**：「支付后界面与**原生确认点击**均已验证，
  其中一轮点击由**人手**完成（另有一轮由自动化输入，两轮分别记录、互不替代）」。
  口径演进保留在此以便追溯：

  `支付后界面已验证，原生确认点击未验证`
  → `原生确认点击已验证，但执行者是自动化输入、非人手`
  → 当前（人手已完成）。
* 汇报时 `NOT_RUN` / `LIMITATION` 一律**双句写法**（状态 + 缺什么才有证据），
  见技能 `miniapp-device-walkthrough`「结果分档」一节 —— 单句会被读成"这项过了"。
