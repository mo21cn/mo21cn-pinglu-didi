# H7a：真实模型质量验证包

| 字段 | 内容 |
| --- | --- |
| 编号 | H7a（DR-0005 未覆盖项 ①） |
| 状态 | **真实验证已完成（共六轮）**。第 4 轮（2026-09-16，提示词修好「今天」之后）：**3 项达标 / 1 项未达标**（`calibration`）。`expect_date` 由 **4/24 → 36/36**、逐字段准确率 **87.50% → 99.60%**，提示词缺陷已被前/后对照坐实。第 5 轮（同日下午，同口径 `--runs 3` 复跑）：**3 项达标 / 2 项未达标**（`calibration` ＋ **新出现的** `parse_failure`）—— 但它**第一次拿到了 §4.1「逐条 `mocked=false`」的直接证据**（`mocked_responses=0`）。**第 6 轮（2026-09-17，跨日同配置复跑）：4 项达标 / 1 项未达标** —— 准确率 **99.21%**（250/252）、第 5 轮那条 `parse_failure` **未复现**，`calibration` 仍 ❌（**−0.1741**，错答仅 2 次）。未达标项按预定阈值**一条不改**、仍记未达标，**不得宣称通过**（见 §5.6 / §5.7 / §5.8） |
| 日期 | 2026-09-16 ～ 2026-09-17（第 6 轮为跨日复跑） |
| HO 裁决 | 「准备验证包，暂不宣称通过；先完成样本、参考答案、评分规则、模型与费用预算；资源就绪后独立验证。fixture 继续服务 CI」 |

## 1. 它验证什么、不验证什么

`LLM_MOCK=true` 的规则模板只保证**输出结构与真实模式同构**，它证明不了真实模型
**好不好用**。CI 继续由 fixture 覆盖（无 Key、无网络依赖），本验证包是**CI 之外**
的独立质量测量。

- ✅ 验证：货源解析（`agent/service.py::parse_cargo`）在真实模型上的**逐字段准确率**、
  **是否编造**、**置信度有没有区分度**、**延迟**与**多轮一致性**。
- ❌ 不验证：撮合／支付／状态机等确定性内核（架构红线：它们不经 LLM）；
  也不验证 Agent 作业（ag01/ag02）的信封质量 —— 那需要 DB 夹具，列为下一步。
- ❌ **不代表 PRD 验收通过**：本包产出的是"测量结果"，不是"合格证"。

## 2. 位置与用法

```
backend/scripts/llm_key_doctor.py        # Key 体检（只读）：分清「账户有资源」与「这把 Key 能花到那笔资源」
backend/scripts/h7a_samples.json         # 样本 + 参考答案（机器判分，不需要人工逐条看）
backend/scripts/h7a_model_quality.py     # 跑分脚本（真发请求、会产生费用）
```

Key 走 `backend/.env.local`（`.gitignore` 已排除，**禁止入库**），
因为 config 的 `env_file` 把 `.env.local` 放在最高优先级。

```bash
cd backend
# 0) 先体检：不花调用费，只读三件事（Key 是否被站点认可 / 套餐剩多少 / 能不能真调通）
.venv/Scripts/python.exe scripts/llm_key_doctor.py
# 1) 再看会花多少
.venv/Scripts/python.exe scripts/h7a_model_quality.py --dry-run
# 2) 真跑
.venv/Scripts/python.exe scripts/h7a_model_quality.py --runs 2 --out ../artifacts/h7a.json
```

**第 0 步不是可选的礼貌步骤**：2026-09-16 首次真跑 24 次全被 402 挡下，根因查了很久
（见 §5.1）—— 先花两秒体检，能省掉整轮排查。

退出码三态：`0` = 达标 / `1` = 未达标 / `2` = **没跑成（NOT_RUN）**。
`2` 与 `1` 必须分开 —— "没验成"不能倒推成"不达标"，也不能被读成"跑过了"。

## 3. 样本（12 条）

`h7a_samples.json` 的覆盖点，每条都对应一类真实风险：

| 样本 | 考察点 |
| --- | --- |
| S01 | 完整信息（基线） |
| S02 | 缺目的港 + 缺价格 → **必须置 null** |
| S03 | 缺重量 |
| S04 | 集装箱 + 万元口语（"两万五" → 25000） |
| S05 | 液货枚举（tanker） |
| S06 | 中文大写数字（"八十吨" → 80） |
| S07 | **相对日期（"下周三"）** —— §5.2 那条主缺陷的探针；修好后 **36/36 = 100%**（§5.6） |
| S08 | 完全无关输入 → 全 null（**不编造**） |
| S09 | 信息不足（"我想发点货"）→ 全 null |
| S10 | 域外港口（上海/广州不在 13 个枚举内）→ 正确行为是置 null，**不得自创代码** |
| S11 | 中文数字（"三百五十吨" / "四万二"） |
| S12 | 长句 + 干扰（先提"贵港→南宁"的老线路，真实线路是"崇左→贺州"） |

## 4. 评分规则与阈值（**跑之前定死，跑完不许改**）

判分写在 `h7a_model_quality.py::THRESHOLDS`：

| 指标 | 阈值 | 为什么是这个数 |
| --- | --- | --- |
| 逐字段准确率 | ≥ 85% | 12 样本 × 7 字段 = 84 次判定；低于此值解析结果需要人工逐单复核，产品上不划算 |
| 编造次数 | = 0 | 系统提示词写死「信息缺失时置 null，不要编造」，属硬要求，不折算成分数 |
| 置信度校准 | > 0.10 | 答对字段的平均置信度 − 答错字段的；差值为负说明置信度没有区分度，`needs_review` 就是摆设 |
| 调用失败 | = 0 | 输出不可解析 = 该样本完全不可用 |

字段级判定口径：港口/货类**精确匹配**（枚举内）；重量/价格**0.5% 相对容差**；
货名用**关键词包含**（避免"散装水泥"/"水泥"的差异造成假失败）；
**null 与非 null 不一致即判错**，期望为 null 却给出值额外记一次「编造」。

### 4.1 第 5 条判定：真实响应（2026-09-16 补，U-6）

上表四条**衡量模型质量**；这一条**衡量"这轮结果能不能算数"**。HO 的口径是
「正式质量测试必须确认实际响应 `mocked=false`；请求真模型但缺凭据时应报**资源阻塞**，
不能自动降级后继续交'真模型'结果」。

| 判定 | 口径 | 落在哪 |
| --- | --- | --- |
| `real_response` | 每一条响应逐条断言 `mocked == False`；出现一条 `mocked=true` ⇒ 该条**丢弃且不计分**，并计入 `mocked_responses` | `_call()` 返回第 5 个值；`run()` 逐条断言；`_verdict()` 进 `checks`；`main()` 命中即 `rc=2` |

**为什么入口检查不够**：`main()` 原本只看 `LLM_MOCK` 与 `LLM_API_KEY` —— 它证明的是
「**配置声称**要发真实请求」，而 HO 要的是「**这一条响应**确实不是桩」。桩可能由别的开关、
别的注入路径产生，入口看不见。所以判据必须落到**每条响应**上。

**⚠️ 诚实交代**：本包已记录的四轮（§5）跑在这条断言**加入之前**。它们**没有**逐条
`mocked=false` 的证据；只能给**间接**证据（`rc≠2` ⇒ 入口检查通过；响应带非零 `latency_ms`；
准确率/延迟分布符合真实模型特征）。⇒ 结论不变，但**证据等级低一档**。
下次带真实 Key 复跑时，结果文件会多出 `mocked_responses` 与 `mock_incidents` 两个字段，
那才是这条断言的直接证据。

> ✅ **2026-09-16 下午已补上**：第 5 轮复跑（见 §5.7）的结果文件里
> `mocked_responses = 0`、`mock_incidents = []`，且 `verdict.checks.real_response = true`
> ⇒ 这一轮**是**「逐条响应确实不是桩」的直接证据。
> ⚠️ 但 §5 前四轮的证据等级**不因此升级** —— 它们各自是各自那一轮的产物，
> 只能各自标注；「第 5 轮有」不等于「前四轮也有」。

## 5. 真实验证结果（2026-09-16）

**结论：已真实跑通五轮。** 第 4 轮 `rc=1`，3 项达标 / 1 项未达标（`calibration`，且该指标已退化）；
**第 5 轮 `rc=1`，3 项达标 / 2 项未达标**（`calibration` ＋ 新出现的 `parse_failure`），
但第 5 轮**首次拿到了「逐条 `mocked=false`」的直接证据**。

**仍不得宣称通过**：预定阈值一条未改，未达标的就记未达标。但**前四轮**连起来是一次完整的
**"拦住 → 定位 → 修 → 复测"**，且第 4 轮与前 3 轮构成**前/后对照**（只改了提示词一个变量）：

| 轮次 | 退出码 | 拦在哪 | 处置 |
| --- | --- | --- | --- |
| 第 1 轮 | `2` NOT_RUN | 24 次**全部** HTTP 402（Key 与计费方式不匹配） | 换订阅 Key，见 §5.3 |
| 第 2 轮 | `2` NOT_RUN | 24 次里 22 次 `bad_response`（推理模型把思维链内联进 `content`） | 网关剥推理块 + 取围栏内 JSON，见 §6.5 |
| 第 3 轮 | `1` 未达标 | **测出来了**：准确率 87.50%、`expect_date` 仅 17%，见 §5.1 | 定位到提示词缺「今天」，见 §5.2 |
| 第 4 轮 | `1` 未达标 | 提示词修好后复测：**准确率 99.60%、`expect_date` 36/36**；`calibration` 未达标但指标已退化，见 §5.6 | —— |
| **第 5 轮** | `1` 未达标 | 补 §4.1 直接证据（`mocked_responses=0`）；准确率 98.78%；**新出现** 1 次 `bad_response`（S08 无关输入，模型回对话而非 JSON）⇒ `parse_failure` 未达标，见 §5.7 | 登记为**模型偶发不稳定性**＋产品侧待办（§7），阈值不动 |
| **第 6 轮** | `1` 未达标 | 2026-09-17 跨日同配置复跑：准确率 **99.21%**（250/252）；`parse_failure` **回到 0**（第 5 轮那次**未复现**）；`calibration` **−0.1741** 仍 ❌（错答仅 2 次，指标继续退化），见 §5.8 | 阈值不动；`calibration` 的新判据已在 §7 **定死为提案**（待 HO 裁决后生效） |

### 5.1 第三轮实测（12 样本 × 2 轮 = 24 次调用）

| 指标 | 实测 | 阈值 | 判定 |
| --- | --- | --- | --- |
| 字段准确率 | **87.50%**（147/168） | ≥ 85% | ✅ |
| 编造次数 | **0** | = 0 | ✅ |
| 调用失败 | **0** | = 0 | ✅ |
| 置信度校准 | **−0.167** | > 0.10 | ❌ |
| 延迟 | mean 6007ms / p95 9094ms | —— | 记录 |
| 多轮一致 | 2/12 | —— | 记录（按设计无阈值） |

逐字段：`cargo_name` / `cargo_type` / `weight_t` / `origin_port` / `offer_price`
均 **24/24 = 100%**，`dest_port` 23/24 = 96%，**`expect_date` 只有 4/24 = 17%**。

⇒ **`calibration` 与 `expect_date` 是同一个根因**，不是两个独立缺陷：
模型给出**错误年份**时 `field_confidence` 仍报 `1.0`，"答对字段的平均置信度 − 答错字段的"
就被拉成负数。**`calibration` 是 `expect_date` 的影子指标**（见 §5.2）。
三项硬要求（不编造、可解析、准确率）都过了；漂移的是**它不知道今天是几号**。

### 5.2 `expect_date` 17% 的根因：提示词要求按"今天"推算，却从不告诉它今天

`app/modules/agent/service.py::SYSTEM_PROMPT` 写着：

> `"expect_date": string|null,  // 期望装货日期 "YYYY-MM-DD"（口语"下周三"等按今天推算）`

而当时的 `SYSTEM_PROMPT` 是**一个不含任何日期的模块常量**
（第 4 轮已改名 `_CARGO_PROMPT_TEMPLATE`，统一经 `build_cargo_prompt()` 渲染，见 §5.6）。
实测后果分两类：

| 类型 | 样本 | 现象 |
| --- | --- | --- |
| 只说"X月Y日"，年份要靠"今年"补 | S01–S06、S10–S12 | 年份瞎猜：给了 2024 / 2025 / 2027，**错** |
| 相对日期 | S07「下周三」 | 给 `null`（无从推算） |

**这不是"模型不聪明"，而是它缺一个它无法推断的输入**。评分基准本身也假定模型知道年份
（`h7a_model_quality.py::_expected_date` 用 `f"{today.year}-10-08"` 生成参考答案）。

> **产品影响（比 H7a 分数更重要）**：`service.py::_sanitize` 对日期做
> `parsed if parsed >= today else None` —— 猜成过去年份的日期会被置成 null。
> **更正一处早先的说法**：这不算"静默" —— 该字段同时进 `needs_review`，
> 前端 `cargo.js` 会高亮它、`assistant.js` 会列进「待确认字段」。但**值本身确实丢了**：
>
> 货主打「10月8号装货」，拿到的草稿里装货日期是**空的**，得手工补一次。

### 5.3 第一轮 402 的根因：不是「没买订阅」，是「这把 Key 挂不到那笔资源」（已解决 · 留档）

首版把这行记成「账户余额不足 ⇒ **充值后重跑**」。**方向是错的** —— 账户本来就有
**Token Plan Plus 月度订阅**。三行实查证据（`scripts/llm_key_doctor.py`，只读）：

| 检查 | 结果 | 说明 |
| --- | --- | --- |
| `GET /models` | **200**，8 个模型 | Key 被站点认可，不是无效 Key |
| `GET /token_plan/remains` | **200**，`general` **周窗口剩余 99%** | **账号的套餐额度几乎是满的** |
| `POST /chat/completions` | **402 / 1008** | 可真调用说"资源不足" |

三者并存只有一个解释。按 MiniMax 官方（Token Plan 概要 / 常见问题）：

> 订阅 Key 用于 Token Plan 订阅套餐和已购积分；**订阅 Key 与普通按量计费 API Key
> 相互独立，不能混用**。每位用户在所属的每个团队中都有一把专属订阅 Key。

即平台是**两套凭证、两个钱包**：

| 凭证 | 形态 | 花的是 | 从哪取 |
| --- | --- | --- | --- |
| **订阅 Key** | `sk-cp-`（旁证） | Token Plan 套餐额度 / 已购积分 | 账户管理 / Token Plan |
| **按量计费 API Key** | `sk-api-`（旁证） | 开放平台**账户余额**（wallet） | 接口密钥页 |

当时手上那把是 **`sk-api-` 形态（按量计费）**，调 `/chat/completions` 就从**账户余额**扣；
余额为 0 ⇒ 402，**套餐里剩的 99% 与它无关**。
⇒ 一句话：**「账户有钱」与「这把 Key 能花到那笔钱」是两件事**。
把 402 读成"该充值"会让人去充一笔不需要充的钱。

> **形态判读的依据级别**：官方明确规定"两套 Key 互不通用"，但**未公布前缀对照表**；
> `sk-api-` / `sk-cp-` 的对应关系来自社区实践，属**旁证**。
> 最终判据是控制台上「这把 Key 是从哪一页创建的」。
> 另：`GET /v1/account/balance` 返 **403 `api key is not an admin key`**，
> 说明按量计费那把是普通（非 admin）Key。

### 5.4 处置与验证（2026-09-16 已执行）

1. 复制**订阅 Key**（`sk-cp-` 形态）替换 `backend/.env.local` 的 `LLM_API_KEY`；
2. `scripts/llm_key_doctor.py` 四步全绿 —— **`✅ 端到端可调用`**；
3. **正面证据**：`GET /token_plan/remains` 的 5h 剩余从 **100% → 99%**，
   说明这次调用**确实扣的是套餐额度**。这比"没报错"强 —— 它证明钱真的花到了那笔资源上。

⚠️ **轮换 Key 时踩到的一个陷阱（已修）**：备份出来的 `backend/.env.local.bak-<ts>`
**当时不在 `.gitignore` 覆盖范围内**（`.env.local` 覆盖了，`.bak-…` 没覆盖），
于是**装着旧 Key 的备份文件变成一个未跟踪文件**（`git status` 里是 `??`），
谁顺手 `git add -A` 就把它提交上去了。⇒ 已补 `.env*.bak` / `.env*.bak-*` /
`.env.local.backup*` 三条规则，并把该备份移出仓库到工作区 `secrets-backup/`。

### 5.5 一个仍未解的疑点（如实留白，不硬解释）

`token_plan/remains` 里 `current_interval_total_count` 为 **0**，而同一条记录的
`current_weekly_remaining_percent` 为 **99**。两者对"还有没有额度"的口径不一致，
官方文档未说明前者的语义 ⇒ 保持**未知**，不用"大概是 5 小时窗口没开始算"之类的
猜测把它抹平。

### 5.6 第 4 轮：修掉「今天」之后复测（12 样本 × 3 轮 = 36 次调用）

**改了什么**：只改一处 —— 把当天日期注入提示词（`service.build_cargo_prompt()`），
加上同批的测试与脚本取用点。模型、样本、参考答案、阈值**一个字没动**。

| 指标 | 第 3 轮（未修） | 第 4 轮（已修） | 阈值 | 判定 |
| --- | --- | --- | --- | --- |
| 字段准确率 | 87.50%（147/168） | **99.60%**（251/252） | ≥ 85% | ✅ |
| `expect_date` 单字段 | **4/24 = 17%** | **36/36 = 100%** | —— | ✅ |
| 编造次数 | 0 | **0** | = 0 | ✅ |
| 调用失败 | 0 | **0** | = 0 | ✅ |
| 置信度校准 | −0.167 | **−0.1196** | > 0.10 | ❌（指标已退化，见下） |
| 延迟 | mean 6007ms | mean 5813ms / p95 10703ms | —— | 记录 |
| 多轮一致 | 2/12 | 2/12 | —— | 记录 |

⇒ **提示词缺陷被前/后对照坐实**：唯一的自变量是"提示词里有没有今天"，
`expect_date` 从 17% 升到 100%、总准确率从 87.50% 升到 99.60%，其余指标没变差。
**"模型答不对"确实是因为我们少给了一个它推不出来的输入。**

结果文件自证（`artifacts/h7a.json`，不入库）：`prompt_includes_today=True`、
`prompt_chars=1063` —— 不必靠谁记得当时发的是什么。

**唯一的未达标项 `calibration` 本轮不可信 —— 这是测量工具的缺陷，不是模型的**：

- 校准度 ＝（答对字段的平均置信度）−（答错字段的平均置信度）。
  本轮 252 次判定里**只有 1 次判错**（S03 的 `cargo_type`：got `bulk` / want `general`）
  ⇒ **"答错"那一组的 n = 1**，差值 `0.7304 − 0.85 = −0.1196` 由**单个数据点**决定。
- 该指标的设计前提是"错答有一定数量"；准确率到 99.6% 时它自然退化。
  拿它当"未达标"的证据，等于**拿一次抽样当结论**。
- **处置（按纪律）**：阈值**一个字不改**，仍记 ❌ 未达标、**不宣称通过**；
  同时把"该指标在此精度下已失效"作为**测量工具缺陷**如实记录，
  修法另立一项（见 §7）：让错答组有足够样本，或换成不依赖错答数量的指标（AUC / ECE）。
- 顺带一句**不作数的观察**：本轮唯一那次错答给的置信度（0.85）**高于**答对字段的均值（0.73）
  —— 方向上正是"错的时候更自信"。但 n=1，这里只记现象，**不当结论**。

### 5.7 第 5 轮：补上「逐条 `mocked=false`」的直接证据（12 样本 × 3 轮 = 36 次调用）

**改了什么**：**什么都没改** —— 模型、样本、参考答案、提示词、阈值全部与第 4 轮一致
（同为 `--runs 3`）。这是一次**同配置复跑**，目的是补 §4.1 那条后补断言的直接证据，
顺带观察同一配置在多次运行之间的**稳定性**。

| 指标 | 第 4 轮 | 第 5 轮 | 阈值 | 判定 |
| --- | --- | --- | --- | --- |
| **`real_response`（逐条 `mocked=false`）** | **未测**（该断言当时还不存在） | **`mocked_responses=0` / `mock_incidents=[]`** | —— | ✅ **直接证据到手** |
| 字段准确率 | 99.60%（251/252） | 98.78%（242/245） | ≥ 85% | ✅ |
| `expect_date` 单字段 | 36/36 = 100% | 34/35 = 97% | —— | ✅ |
| 编造次数 | 0 | 0 | = 0 | ✅ |
| 调用失败 | 0 | **1** | = 0 | ❌ |
| 置信度校准 | −0.1196 | −0.0455 | > 0.10 | ❌（仍退化，见 §5.7.1） |
| 延迟 | mean 5813ms / p95 10703ms | mean 7108ms / p95 12343ms | —— | 记录 |
| 多轮一致 | 2/12 | 2/12（S09 / S11） | —— | 记录 |

结果文件自证：`prompt_includes_today=True`、`prompt_chars=1063`（与第 4 轮**同值** ⇒ 提示词确实是同一份）、
`today=2026-09-16`，且 `thresholds` 与第 4 轮逐字段相同 —— 对比的前提是**同一套判据**。

**这一轮真正值钱的是第一行**：`real_response` 从「只有间接证据」变成「有直接证据」。
它是 HO 要的那条 —— 不是"配置声称要发真请求"，而是"**这一条响应**确实不是桩"。

#### 5.7.1 未达标项之一：`calibration` 仍退化（老问题，处置不变）

「答错组」均值 `0.80` −「答对组」均值 `0.7545` ⇒ **−0.0455**（错答判定共 3 次，跨轮）。
与 §5.6 同一条判断：**准确率到 98%+ 时该指标已失去区分力**（错答样本太少）。
阈值**一个字不改**，仍记 ❌、不宣称通过；修法见 §7。

#### 5.7.2 未达标项之二：`parse_failure = 1` —— **本轮新出现，属模型偶发，不是我们改坏了**

S08（**完全无关输入**，期望全 null）在第 3 轮回归了**对话模式**：剥掉推理块后剩下的正文是
一段自我介绍与反问，**不是 JSON**，于是网关 `json.loads` 在 char 0 崩 ⇒ 记 `bad_response`。
错误原文（§6 第 6 条"只记 `kind` 不记 `message` 等于没记"的修复在这里**直接生效** ——
光有 `kind` 根本查不出这件事）：

> `Expecting value: line 1 column 1 (char 0)；剥离推理块后 82 字符，开头
> '您好，我是货源解析助手，仅用于解析货主货源信息，不提供天气查询服务。…'`

三条必须分清的边界：

1. **不是 §6.5 那个老缺陷复发**。老缺陷是"答案前面挂着 `<think>…</think>`"，
   剥完就能拿到 JSON；这一次剥完**根本没有 JSON** —— 模型压根没按 `response_format`
   输出结构化结果。两者现象相似（都落 `bad_response`）、根因不同。
2. **不是本轮改了什么导致的**。本轮**零改动**，第 4 轮同一配置是 0 次失败
   ⇒ 只能归因到**模型侧的不稳定性**（同一输入、同一提示词，跨运行结果不同）。
   ⚠️ **n=1 的观察，不当结论**；但"同一配置两次跑出不同失败数"这个**事实**是硬的。
3. **产品影响比分数重要**：真实场景里货主输入一句无关的话，
   `parse_cargo` 会以 `bad_response` 抛出，用户看到的是**报错**，
   而不是"没识别到货源信息，请重新描述"这种**可继续**的引导。
   ⇒ 已在 §7 登记为**产品侧待办**（把"模型不返回结构化结果"降级为**未识别**）。
   这属于**兜底缺失**、不属于模型质量 —— 但它恰恰是"真跑才暴露出来"的那一类。

### 5.8 第 6 轮：跨日同配置复跑（12 样本 × 3 轮 = 36 次调用）

**改了什么**：与第 5 轮一样**什么都没改**（模型 / 样本 / 参考答案 / 提示词 / 阈值全部一致，
同为 `--runs 3`）。唯一的新变量是**日期** —— `today` 由 `2026-09-16` 变成 `2026-09-17`，
相对日期类样本（`S07`「下周三」、`S01`–`S06`/`S10`–`S12` 的"X月Y日需补年份"）在
**另一个"今天"下**重跑。这是「提示词注入今天」修好之后**第一次跨日验证**。

| 指标 | 第 4 轮 | 第 5 轮 | **第 6 轮** | 阈值 | 判定 |
| --- | --- | --- | --- | --- | --- |
| `real_response`（逐条 `mocked=false`） | 未测 | `mocked_responses=0` | **`mocked_responses=0` / `mock_incidents=[]`** | —— | ✅ |
| 字段准确率 | 99.60%（251/252） | 98.78%（242/245） | **99.21%（250/252）** | ≥ 85% | ✅ |
| `expect_date` 单字段 | 36/36 | 34/35 | **35/36** | —— | ✅ |
| 编造次数 | 0 | 0 | **0** | = 0 | ✅ |
| 调用失败（`parse_failure`） | 0 | **1** | **0** | = 0 | ✅ |
| 置信度校准 | −0.1196 | −0.0455 | **−0.1741** | > 0.10 | ❌ |
| 延迟 | mean 5813 / p95 10703 | mean 7108 / p95 12343 | **mean 6427 / p95 13125** | —— | 记录 |
| 多轮一致 | 2/12 | 2/12（S09 / S11） | **2/12（S09 / S12）** | —— | 记录（按设计无阈值） |

结果文件自证（`artifacts/h7a_round6.json`，不入库）：`today=2026-09-17`、
`prompt_includes_today=True`、`prompt_chars=1063`（与第 4/5 轮**同值** ⇒ 提示词确实是同一份）、
`thresholds` 逐字段与第 5 轮相同 —— 对比的前提是**同一套判据**。

#### 5.8.1 `parse_failure` 回到 0 —— 但**不能**据此说"已修复"

第 5 轮的 S08 那次失败**没有复现**，与 §5.7.2 的判断一致（**偶发**）。
⚠️ 但它**不等于**"产品侧兜底可以不做了"：第 5 轮那条"用户看到报错而不是可继续的引导"
的产品影响**依然真实** —— 只要它发生过一次，就存在。§7 那条兜底待办**保持不动**。

#### 5.8.2 `calibration` 仍是唯一未达标项，且它继续在退化

- 三轮判错分布：第 1 轮 84/84、第 2 轮 83/84、第 3 轮 83/84 ⇒ 错答共 **2 次**；
  答对组均值置信度 **0.7259**、错答组 **0.9** ⇒ **−0.1741**。
- ⚠️ **一条如实交代**：结果文件的 `per_sample` 只保留**最后一轮**明细 ⇒
  2 次错答里只有 1 次能查到原始记录（S03 的 `cargo_type`：got `bulk` / want `general`、
  置信度 `0.8`）。另一次（第 2 轮）**明细未保留**；由「错答组均值 0.9 × 2 − 0.8」推得
  它的置信度应为 **1.0** —— 这是**算术推算**，不是读出来的原始值，标注清楚以免后人对账时踩空。
- 与 §5.6 / §5.7.1 同一条判断：准确率到 99%+ 时错答样本太少（n=2），该指标**被单点主导**、
  已失去区分力。阈值**一个字不改**，仍记 ❌、不宣称通过。
- **一个连续三轮同向的现象（仍不当结论）**：第 4 轮（n=1）错答置信度 0.85 > 答对均值 0.73；
  第 5 轮（n=3）0.80 > 0.7545；第 6 轮（n=2）**0.9 > 0.7259**。三轮同向、累计 n=6，
  "错的时候更自信"这个**方向**是稳定的 —— 但样本量仍不足以当结论。
  它恰恰是 §7 那条「换一个不依赖错答数量的指标」的理由：
  **现有指标看不见这个现象，只会给出一个越来越负、也越来越不可信的数字。**

## 6. 本轮顺带发现并已修的缺陷

它们不是 H7a 的目标，但都是"第一次真发请求"才暴露出来的：

1. **HTTP 402 被归类成 `bad_request`**（`app/modules/agent/llm.py`）。
   报出来是"请求被拒绝"，掩盖了"该充值"这个**真正要做的动作**。
   ⇒ 新增 `quota` 分类，且**不可重试**（重试不会让账户有钱），
   见 `RETRYABLE_KINDS` 与 `tests/test_entrust_agent.py::test_quota_error_is_not_retryable`。
   **顺带**：402 的用户可见文案也改了 —— 原来只说"需充值"，但查清 §5.3 后可知
   402 有两种成因（账户余额为 0 / **Key 与计费方式不匹配**），文案须同时指向，
   否则会把人推去充一笔不需要充的钱。
2. **`.env.local` 会污染测试环境**（conftest 只防了微信，没防 LLM）。
   本机为调试真模型配上 `LLM_MOCK=false` 后，
   `test_claim_next_consumes_attempt_and_execute_succeeds` 由 `succeeded` 变 `failed`
   —— 真因是本机改了配置，不是代码坏了，但现象与"并发守卫写错"同形。
   ⇒ conftest 补 `force_llm_mock` autouse（与既有 `force_wechat_mock` 同一套约定）。
3. **`scripts/h7a_model_quality.py` 缺 sys.path 自举** ⇒ 本文件 §2 那条运行命令
   **实际跑不起来**（`ModuleNotFoundError: No module named 'app'`）。
   同目录 `seed_entrust_demo.py` / `seed_entrust_orgpicker.py` 早有同样的补法，本脚本漏了
   —— 教训：**"验证包写完了"与"验证包能按文档跑一遍"是两件事**，
   后者要在干净 shell 里真敲一次才知道。
   ⇒ 已在脚本头部补 `sys.path.insert` 并逐行标 `# noqa: E402`。
4. **代码注释与结论漂移**：`_report` 里仍写着"HTTP 402，本客户端当前归类为 bad_request"，
   而 402 早已经改成 `quota`；同时 `_report` 的"常见原因"仍只说"余额不足"。
   ⇒ 与 §5.3 同批订正。判据变了，读代码的人**总是后到** —— 注释必须跟着改。
5. **`chat_json` 读不懂推理模型的输出** —— **本包最值钱的一条**（`app/modules/agent/llm.py`）。
   MiniMax-M2.7 是**推理模型**：它把思维链直接内联进 `message.content`
   （`<think>…</think>`），答案跟在其后；并且**无视** `response_format={"type":"json_object"}`。
   网关却拿整段去 `json.loads` ⇒ **必然**在 char 0 崩，24 次调用全记 `bad_response`。
   修法：解析前剥推理块（取**最后一个** `</think>` 之后），并从中取第一个完整的顶层
   JSON 对象。
   * **不用**供应商的 `reasoning_split=true`（实测有效）：那是专有参数，
     而"content 夹推理块"在 OpenAI 兼容生态里是普遍现象（DeepSeek-R1 系同形），
     客户端剥一次覆盖全部供应商，网关保持中立；剥下来的推理原文进 `LLMResult.reasoning`
     留档（不参与判分），免得"我们主动要它思考，却把思考直接丢掉"。
6. **只记 `kind` 不记 `message`，等于没记**（`scripts/h7a_model_quality.py`）。
   重跑时 S10 报 `bad_response`，结果文件里只有 `{"id":"S10","run":1,"kind":"bad_response"}`
   —— 光看这个**查不出任何原因**，只能另写探针复现才看到"答案是 markdown 围栏里的 JSON"。
   ⇒ `_call` 改为同时返回错误原文，`errors` 条目加 `message` 并当场打印。
   **错误分类用来路由，错误原文用来定位**，两者缺一不可。

### 6.1 一条方法论教训：`bad_response` 的"围栏"兜底，我判断错了一次

剥推理块之后，我在**同一份真实载荷**上实测了四种组合、都拿到裸 JSON，于是判断
"markdown 围栏只是理论边界情况，没观测到就不做兜底"。重跑 H7a 立刻被 S10 打回来 ——
它的输出是 `</think>` + ```` ```json … ``` ````。

而且**它不稳定**：复现时同一载荷走 `chat_json` 两次都成功（拿到裸 JSON），
绕开网关直发却两次都出围栏。⇒ 教训不是"要多加兜底"，而是：

> **"没观测到"不等于"不会发生"，尤其当样本量是 4 次、而线上是每单一次。**
> 判断该不该做兜底时，依据应当是**目标格式的稳定性**，而不是**我这几次恰好看到了什么**。

## 7. 待办（不属于本包）

- ~~把当天日期注入 system prompt~~ —— **已完成（第 4 轮）**：`service.build_cargo_prompt()`
  每次调用时渲染 `_CARGO_PROMPT_TEMPLATE`，把「今天（星期几）」显式给模型；
  `h7a_model_quality.py` 走同一个渲染入口（否则"验证"验的是另一套提示词），
  并补了 `tests/test_agent.py` 两条测试锁住它（断言下在**网关收到的那一份**上）。
  复测结果见 §5.6：`expect_date` 4/24 → **36/36**。
- **修 `calibration` 指标本身（测量工具缺陷，不是模型问题）** —— 见 §5.6 / §5.7.1 / §5.8.2：
  准确率到 99%+ 时"答错"组只剩 1～2 个样本，指标**被单点主导**、失去区分力。
  ⛔ **2026-09-17 HO「0917 决策」：本提案**未被批准**。下面的表格保留作**历史记录**，
  **不得再按它执行**。HO 的理由与替代要求见 §7.1。

  <details><summary>（已作废的旧提案，仅存档）</summary>

  | 项 | 定死的口径 |
  | --- | --- |
  | 主判据（新增） | **ECE（Expected Calibration Error）**：置信度分 **10 个等宽箱**（`[0,0.1) … [0.9,1.0]`），每箱取 `|平均置信度 − 该箱实际正确率|`，按箱内样本数**加权**平均；**阈值 ≤ 0.10**（与现 `calibration_gap_min` 同量级）。空箱不进加权 |
  | 旧指标降级 | `calibration_gap` 由**判定项**降为**记录项**；且**错答 n < 5 时不得作为任何结论的依据**（含 §5.8.2 那条"错的时候更自信"的方向性观察） |
  | 这是补偿，不是放宽 | 新增的 ECE 比旧指标**更严**：它对**每个**置信度区间都要求"说的与做的一致"，而旧指标只看两组均值的差、且分母可以退化到 1 |
  | 样本侧 | 补 **≥ 5 条**"故意易错"样本（歧义货名、超纲单位、边界数量），使错答组 n ≥ 5；**样本与参考答案先写好再跑**，不许跑完再挑 |
  | 生效时点 | **第 7 轮起**，且 `THRESHOLDS` 与本表**同一次提交**改完。第 6 轮及以前**仍按旧判据**判定，结论不变 |
  | 若 HO 不同意 | 维持现状、继续记 ❌ —— **绝不**单方面把 `calibration_gap_min` 调低来"让它过" |

  </details>

### 7.1 HO 2026-09-17「0917 决策」—— H7a 部分的裁定与落地

**裁定：暂不批准用 ECE 替换原验收主判据。** 三条理由：

1. **两个指标衡量的不是同一件事**：`calibration_gap` 是「置信度能否**区分**对错」（区分能力），
   ECE 是「置信度与实测正确率是否**一致**」（校准程度）。
   **不能因为阈值恰好都是 0.10 就称 ECE 更严格或与之等价。**
2. **换成 ECE ≤ 0.10 不会让第 6 轮通过**：按第 6 轮已报告的汇总数（正确组均值、错误组均值与样本量），
   整体平均置信度约 **0.7273**、准确率约 **0.9921** ⇒ 由 ECE 定义可推得 **ECE ≥ |0.9921 − 0.7273| ≈ 0.265**
   （这只是**下界**，不是重算出的精确值，但足以说明"换指标就能过"是错觉）。
3. **仓库里先有三个待修的测量缺陷**，不修就换指标等于换一把没校准的尺子：
   ① 评分脚本把**缺失/非法置信度补成 0**（混淆"没报告"与"报告概率为零"）；
   ② **只持久化最后一轮**样本明细 ⇒ 前面轮次的聚合结果**无法复算**；
   ③ 产品侧默认置信度用了 `or`，可能把**合法的 0** 替换成默认值，与评分脚本口径不一致。

**要求（已落地于本轮）**：

| 要求 | 落点 | 状态 |
| --- | --- | --- |
| 修符号显示 | `h7a_model_quality.py` 的 `+{gap}` 改 `+.3f`（旧写法把 −0.174 打成 `+-0.174`，看着像正数） | ✅ |
| 全轮次明细落盘 | 新增 `per_run_detail`（每一轮的判分明细 **＋ 模型原始输出**），旧字段 `per_sample` 保留兼容 | ✅ |
| 置信度缺失/非法与合法 0 分开 | 新增 `coerce_confidence()`（**产品侧与评测侧共用同一函数**）＋ 四态 `missing/invalid/range/ok`；校准统计只收 `ok`，其余单独计数 | ✅ |
| ECE **仅作记录项** | 新增 `ece`（10 等宽箱）字段与打印行，**不进 `verdict`、不参与 `all_met`、历史判定不改** | ✅ |
| 挑战样本 ≥5 条 | 新增 `h7a_challenge_samples.json`（**7 条**，预先固定）＋ `--set orig｜challenge｜both` | ✅ |
| 原集与挑战集分别报告 | 每个集合单独统计；**判定只按主集 `orig`**，挑战集为观察项 | ✅ |
| 确定性回归（不等随机复现） | `tests/test_agent.py` 新增 3 条（合法 0 不被顶掉／兜底计数／`bad_response` 失败类型与审计保留） | ✅ |
| **下一版协议冻结后**再做一次**有明确目的**的真实验证 | 未执行 —— 需先冻结协议 | ⏸ |

⚠️ **"不原样循环重跑"**：HO 明确要求**不继续原样付费重跑**。
第 7 轮必须**先写明目的**（例如"验证挑战集上模型能否正确弃权"），再跑。

### 7.2 H7a 的地位（HO 0917 第 6 条，**防止它变成全项目闸门**）

- H7a 验证的是**旧域 `cargo_parse`**，**不是**委托 AG-02 的完整质量；
- **不得以 H7a 未绿阻塞 S2 / S3 / S4**；
- **不得用 H7a 的结果替代** DEMO-1 的真实报价会话验收；
- **在校准被证实之前，置信度不得用于自动确认或绕过经理人审核**。
- **「模型不返回结构化结果」要有兜底**（第 5 轮新暴露，`parse_failure`）—— 见 §5.7.2：
  S08 这类**无关输入**会让模型回到对话模式，`parse_cargo` 直接抛 `bad_response`，
  用户看到报错、而不是可继续的引导。**第 6 轮该次失败未复现**（`parse_failure=0`），
  但**处置不变** —— 偶发不等于不会发生，见 §5.8.1。  **修法与阈值无关**（不是调判据），属产品侧兜底。
  ✅ **2026-09-17 HO 0917 已批准**「用户侧可恢复兜底」，但带**五条硬约束**：

  | # | 约束 |
  | --- | --- |
  | 1 | 呈现为「**解析未完成**，请重述或手工填写」的**可恢复界面** —— 是"没做完"，不是"做完了但结果为空" |
  | 2 | **禁止**把解析失败伪装成"成功解析出的全 null 结果"（那会把失败改记成功） |
  | 3 | **保留**失败类型、原始 `bad_response` 审计与失败统计；**不得覆盖用户已有草稿/输入** |
  | 4 | 网络、鉴权、额度（402）等错误**不得**被统一吞成"未识别" —— 它们与"模型没返回结构化结果"是两回事 |
  | 5 | 用**已有失败载荷做确定性回归**，不等线上再次随机复现 |

  ⚠️ 别顺手加"重试" —— 同一输入重试大概率同样不返回 JSON，白花钱。
  ⚠️ **本轮未改行为**：当前仍是抛出 `AgentServiceError`（`bad_response` → 502）。
  已先补**确定性回归**（`tests/test_agent.py::test_non_structured_response_keeps_failure_kind_and_audit`）
  锁住"失败类型与原始审计都在"，把可恢复兜底本身留作**独立切片** —— 它要改的是
  **接口契约**（502 → 200 + incomplete），不该夹在测量修复里一起上。
- **作废那把按量计费 Key**（`sk-api-…`）：它已出现在对话与本地备份里。
  订阅 Key 与它互不通用，作废不影响本包。⚠️ 作废是**控制台侧动作**，脚本做不了。
- Agent 作业（ag01/ag02）的信封质量：可机器判分的维度是「引用来源必须落在
  服务端枚举的来源目录内」，但要先解决 DB 夹具。
- 费用预算：一次完整跑（12 样本 × N 轮）输入侧 ≈ `N × (12 × 样本字数 + 1063 × 12)` 字符
  （第 4 轮 `--runs 3` 实测 **39285 字符 ≈ 13095 tokens**，提示词本身 1063 字符／次）。
  延迟实测：第 4 轮 mean 5813ms / p95 10703ms，第 5 轮 mean 7108ms / p95 12343ms
  —— 同一配置两次跑出的延迟也有约 20% 差异，与 §5.7.2 那条"跨运行不稳定"是同一类现象。
  **单价未查证，不臆造** —— 重跑前请按供应商当期定价核算。
