# DEMO-1 验收记录（Acceptance）—— **当前唯一汇总**

> 本文件是合同 §12 要求的 **Acceptance report**，也是**当前唯一的验收汇总**：
> ⛔ 此前 `readiness` §10.3 的两套小计（`8／1／6／2／1` 与逐行标签）**均不作为当前结论**，
> 已在该处标注为**历史**并指向本文件（HO 2026-09-20 裁定）。
>
> ⛔ **本文件不是"已验收"**：合同 §10.4 的判定口径未变 —— **当前构成 `partial preview`**。
> **HO 验收记录**见 §6（**留白待填**）。
>
> 依据：合同 §9（逐行 Required evidence）／§10.4（候选判定）／§12（交付物）；
> 证据：`DEMO-1-evidence-index.md` §1–§3.3 ＋ `docs/entrust/evidence/**`
> 最近更新：2026-09-20

---

## 1. 候选与可复跑入口

| 项 | 值 |
| --- | --- |
| **候选（合并对象）** | **PR #185 的 HEAD** —— ⛔ **本行不写死 sha**：合并前的权威值以 `git rev-parse HEAD`／PR 页面为准，**CI 必须在该 sha 上 6/6 全绿**。<br>⭐ **本轮全部设备证据（B1' 主链／B2 无模型轮／B3 复位演练）所在提交**＝`26f8a2996625b47e21cb1d0d7fd2d35a4a1a6b4b`；**基线**＝`9addfe8`（`develop`，PR #184 squash）。<br>⚠️ 本行改成"不写死"是有原因的：**写死 sha 已连续两次落后于候选**（`83ccb3f`／PR #180 落后 2 个候选；`9addfe8`＋"本分支收尾提交"甚至**不可检出**）—— 取证提交一多，写死的值必然过期。合并后以 squash 得到的 `develop` sha 为准。⭐ **#185 已于 2026-09-21 合并**（squash `aa880b3`）—— ⛔ 合并不等于验收，仍以本文件 §2／§6 为准 |
| **CI** | **`26f8a299…` 上 6/6 job SUCCESS**（后端 lint+test／迁移 SQLite／迁移 MySQL 8.0／并发集成 MySQL／前端静态契约／前端端到端）。⚠️ CI 绿 **≠** 走查绿：主链走查同轮的汇总行是 `RESULT: NOT_RUN`（见 §7 第 1 条） |
| **本机门禁** | `python -X utf8 scripts/verify_local_gates.py` ⇒ `{"total": 16, "failed": 0}` |
| **后端单测** | pytest **1015** 项（通过 998／跳过 17／失败 0／错误 0），其中 MySQL 并发/幂等一组见 `backend/tests/test_mysql_integration.py` |
| **证据入口** | `docs/entrust/evidence/`（`* -text` 逐字节入库；已复核「工作区哈希 == 索引 blob」） |
| **演示脚本** | **`DEMO-1-walkthrough.md`**（合同 §12 交付物，2026-09-20 建立） |
| **模型模式** | ⚠️ 每一轮都要写明：真模型／fixture（`--llm-mock`）。**两种模式下的读数不可互相冒充** |

## 2. D1-01 – D1-18 逐行验收（**按合同 §9 的 Required evidence 判定**）

### 2.0 判定口径（三条，先立规则再看结果）

1. **"已设备运行"只描述证据来源，不等于本项验收通过**（HO 2026-09-20）。
   每一行都按合同 §9 的 `Required evidence` 逐条核对**证据是否覆盖要求**。
2. **合同明确允许用 API／服务层／MySQL 测试证明并发、幂等与拒绝规则**（§9 尾注）；
   **用户入口与可见结果仍须有运行证据**。⇒ 不因"没在设备上点"一概否定，也不因"点过一次"一概通过。
3. 三种"不算通过"的原因要分清：`部分`（覆盖不全）／`未开始`（没做）／`工具限制`（方法不覆盖，
   合同 §10.3 的 `LIMITATION`）。⛔ 三者都不计入 PASS，但处置完全不同。

### 2.1 逐行表

| D1 | 合同要求证据（§9 摘要） | 实际持有证据 | 判定 |
| --- | --- | --- | --- |
| 01 | 版本/配置清单、**UI 运行**、assignment ID | ㊸ 第 1 步（真实入口提交 ⇒ `aid=6`，API 直证）＋ ㉞；配置清单见 runbook §3–§5 | **PASS** |
| 02 | UI/API 结果 **＋ 并发测试** | ㊸ 受理（`submitted→claimed`）＋ ㊱/㊴/㊵；并发：MySQL `test_double_claim_race_exactly_one_winner` | **PASS** |
| 03 | **live 调用**证据、job/message/artifact ID、可见结果 | ⭐ **H 轮（`--section 43`，runner 打印 `模型模式：真模型`）**：会话屏真实点击 → job 到 `succeeded`、提案 1 条、来源含 `attachment_text`、页面可见 → 采纳成成果（`artifact=8`）→ v2 生效。ID 与读数见索引 §3.3 | **PASS**（此前只有 fixture 轮，O-3 至此可关） |
| 04 | 跨视图记录比对（前后） | ㊸ 第 3 步：会话屏 → 成果页 → 工作台侧看到**更正后**内容，同一 `artifact_id=8`、`v1→v2`、生效版本切换 | **PASS** |
| 05 | **离线/人工**通路证据；无语义上必需的动作被阻塞 | ⭐ **2026-09-21 凌晨：无模型人工替代通路已在设备上取证（分两段，两段都绿）**。<br>**段A · 附件分支**（独立轮 `walk-20260921-032823`，`hold18`）`PASS 6 / LIMITATION 1 / NOT_RUN 1`、**`FAIL=0`**：上传内置**真 PNG** 扫描件 ⇒ ② 页面原文「图片没有文本层，**本服务不做 OCR**」⇒ ③ **服务端** `extract_status='needs_transcription'`（＋**正控** `canReference=False`）⇒ ④ 「人工转录」**按状态**出现 ⇒ ④b 转录落库（界面 `人工转录`／**服务端** `text_source='manual_transcription'`、67 字、`canReference=True`）⇒ ⑤ 点「让 Agent 解析」⇒ job `errorKind='llm_network'`、`errorMessage='LLM 服务端错误 502'`、**`mocked=False`**（真模型不可用，⛔ 不是 fixture）。<br>**段B · 业务分支**（`--chain` 13 章干净轮 `walk-20260921-044802`，`hold19`）**`FAIL=0`**（`NOT_RUN=13 / LIMITATION=2`），**13 个取单点全部同一 `aid=7`**：`㊹ 〇` **经界面人工组装**对客报价 `customer_quote id=9`（来源 `manual`）⇒ 发布 ⇒ `㊿ ⑥` 变更后复核 `still_valid=False`（只有 `capacity` 不过）⇒ `chain11 ①b-a/b/c` **不齐备 ⇒ 点结案被拦**（缺项逐条一致、状态未变）⇒ `③b/④` 齐备 `ready=True 缺 []` ⇒ `53 ⑧-b` **结案成功** `completed` ⇒ `⑨-a…⑨-e` **受控重开** ⇒ `chain12 ④a` 重载后**逐字相同**、`④c` **零写入正控 `写请求=无`**。<br>⇒ 「**模型不可用时，经理仍能完成同一业务生命周期**」成立；⛔ 不要求 AG-02 成功（其四格按运行条件记 `NOT_RUN` 并点名，**有模型轮次判据未改宽**）。<br>⚠️ 三轮的定档与 **13 条 `NOT_RUN` 逐条去向**见 `DEMO-1-evidence-index.md` **§3.12／§3.12.1**；其中**实例退化**那一轮（`hold18` 第 3 轮，`accessSync:fail`）**保留不抹掉**，并已按纪律**换实例**重取。<br>⚠️ 2 条 `LIMITATION`＝原生文件选择器 ＋ **OS 摄像头**（系统层弹层，⛔ 不宣称通过、⛔ 不为它扩建 OS 输入通道） | **PASS**（两段均 `FAIL=0`，同一张单，可核对） |
| 06 | 正例与负例（有效性/容量/证据） | ㊺（含过期候选 / 证据不足负例）＋ `test_capacity_confirmation_*` 一组 | **PASS** |
| 07 | 客户 UI ＋ API 负例 ＋ 版本审计 | ㊹（经理冒充 ⇒ **403**、二次响应 ⇒ **409**、客户看到的是**发布时那一版**）＋ 章 51（首次发布**被来源门槛拒**、核验后同版本发布成功）＋ `test_offer_response_is_unique_per_release` | **PASS** |
| 08 | 合同版本 ＋ 关联证据检查 | ㊾（派生自**已接受事实**，证据与状态不混同实时电子签） | **PASS** |
| 09 | 案件快照、映射目标、**旧版本保留**、刷新后的确认 | ⭐ **2026-09-20 已在主链那张新委托上完成**（链式轮 P/Q，八章同一 `aid=6`）：㊿ 经**界面**走 案件→复核→批准→应用，`800.000 吨 → 950.000 吨`（页面与服务端同源、`quantity-changes` 对得上）；同一确认**只** `capacity` 不过（`still_valid=False`，`changed_fields=['demand_tonnes']`）；既有确认行**逐字未被改动**（`㊺ ④f` 复算前后相同 ⇒ 旧版本保留）；复核**传播落点**在页面上逐条列出。链式第 9 步的复核（`chain9 ①b`）给出同一结论 | **PASS** |
| 10 | 事件/证据 ID、**Decimal 金额**、费用与结算版本 | 章 52 逐条（草稿**不进合计** → 确认后 `100000.0000` → 提争议**退出合计** → 显式「计入」＋最终金额 `88000.0000` → 缺件派生/补录 → 结算**版本** v1 → 内部确认 → 财务状态＋成因 → 收付依据 `labeled_sample` → 客户确认**绑定在该版本**上）⇒ 金额、版本、证据关联**覆盖齐全** | **PASS**（HO 授权：核对齐全即可判） |
| 11 | 后端负例测试 ＋ 客户投影检查 | ㊲/㊵（撤权 ⇒ 403 ⇒ 刷新）＋ 客户白名单投影与负例测试 | **PASS** |
| 12 | **迟到作业**测试、**持久化**重启/恢复结果、幂等证据 | ⭐ 可引用且已核对：MySQL `test_agent_job_stale_worker_write_is_discarded`（旧 worker 迟到写入作废）、`test_agent_job_expired_lease_is_taken_over`、`test_agent_job_two_workers_race_exactly_one_claim`；服务层 `test_lease_expiry_allows_reclaim`、`test_takeover_bumps_generation_and_fences_stale_executor`；幂等 `test_idempotency_required_and_replay`、`test_idempotency_concurrent_duplicate_side_effect_once`；设备侧第 55 章（接管 ⇒ 代次 0→1、**旧代次提交 409 且理由点名**）＋ ㊴ | **PASS**（HO 授权：引用已有证据即可，不要求重做设备测试） |
| 13 | 真实命令/UI、**有理由的拒绝**、DR-0013 的 10/20、**MySQL 竞态** | 并发证据：MySQL `test_complete_race_exactly_one_winner`、`test_close_versus_new_blocking_case_is_serialized`、`test_reopen_race_exactly_one_winner`（不变）。<br>⭐ **2026-09-20 深夜（B1' 轮：干净实例 ＋ 完整配方，12 章同一 `aid=7`，`FAIL=0`）**：**第 10–13 步在同一张单上一次跑通**——`chain11 ①b-a/b/c` **结案负例前置**（清理**之前**点结案：界面先给出 3 项缺项清单且与服务端逐条一致｜点下去**被拦**且理由逐条可读｜状态**未变** `claimed`）｜`②b` 10 条任务逐条处置全成功｜`③b` 关案件一次成功（`version 取自 detail.case.revision_no`）｜`④` 收尾前 `ready=False 缺 3 项` ⇒ 收尾后 `ready=True 缺 []`｜`53 ⑧-b status='completed' completedAt='2026-09-20 15:18:13'`、⑧-c 入口消失、⑧-d 多出「结案时间」｜**受控重开 ⑨-a…⑨-e 全 PASS**（理由必填、`completed_at` 清空、结案入口复现、无「撤回」入口且**带正控**）。<br>⇒ 「**不齐备 ⇒ 被拦**」与「**齐备 ⇒ 成功 ＋ 重开**」**在同一张单、同一轮、同一章群**里并存 —— 这正是 X3 缺的那一件事。<br>⚠️ X3 的 9 条 FAIL 定档（本轮**未复现**；**成因未完全查明**，故只登记"未复现"这一事实）：① **㊸ ×3 ＋ ㊿ ④a**：相关章节在本轮**全 PASS**（`㊿ ④a` 计数通道 `新值=1 单位=1 依据=1` ＋ 正控数得动）⇒ 对 X3 里那几条，本轮能说的**只有**："**同一脚本在新实例上未复现**" —— ⛔ **不得据此断言"不是产品缺陷"**，历史原因（实例／探针侧抑或产品侧）**仍未完全查明**；⛔ 也不是把 FAIL 自动改档；② `53 ①⑥⑦⑧-a`：章节起点假设过期 ⇒ **已改状态感知**，负例另由 `chain11 ①b` 承担；③ `chain12` 裸下标 ⇒ **已修**。<br>⚠️ 另有 **7 条 `NOT_RUN` ＋ 1 条 `LIMITATION`** —— ⛔ 不用「对象不存在」一句话替代验收判断：**逐条去向见 §3.3**（3 条由同轮其他章节证明或更正／2 条设计使然／1 条工具边界／1 条真空通过主动不记） | **PASS**（同单一轮并存，可核对） |

| 14 | 见证式运行期导航/返回/重进 ＋ 栈证据 | ㉝（含 1 条 `LIMITATION`）、㉞；㊸/㊿/52/53 各有"重进后仍在" | **PASS** |
| 15 | **同一个值**的前后记录 ＋ UI 结果 ＋ 重载后仍在 | ⭐ **2026-09-20 深夜（B1' 轮）按 HO 裁定的新口径取得，全绿**（同一个值＝货量 `800.000 → 950.000`，全部落在同一张 `aid=7`）：<br>**① 前后记录**＝服务端 `quantity-changes` 的 `(800.000, 950.000)` ＋ **案件页** `changeText='800.000 吨 → 950.000 吨'`、`source='来源变更案件 #3'`；<br>**② 下游计算/校验变化**＝同一条 900 吨候选确认 `still_valid=False`、**只有** `capacity` 不过（判据取自 `GET …/recheck`，本节**只读**）；<br>**③ UI 结果**＝详情页 `quantity='950'`；<br>**④ 重载后仍在**＝`reLaunch` ＋ 重登后 变更记录／货量／费用行逐字签名／复核结论／缺项集合**逐字相同**（总判据 `④a`：不同的键＝**无**）；<br>**⑤ 零写入正控**＝`④c`：本节运行期脚本**没有发出任何 `/entrust/` 写请求**（读数 `写请求=无`），这正是 HO 裁定「第 13 步改为纯读取和比对，不得新增费用、确认费用或修改结算」的**机械判据**。<br>⛔ **撤回**上一稿「结算面按 `chargeId=3` 命中」那一句：那是本节**自己造的费用行**，把结算版本打成过期（`settlement_stale`），与原始日志不符（见 §2.3）。本轮**没有**造任何数据；`④b` 里 `settlement={ok:True,count:0}`、`balance={ok:True,count:0}` 是**既有**事实。 | **PASS**（新口径，四段读数 ＋ 零写入正控） |
| 16 | 候选 SHA、CI 链接、迁移与回归报告 | 本文件 §1（SHA ＋ 6/6 CI ＋ 16/16 门禁 ＋ 1015 项单测） | **PASS**（已自动化验证） |
| 17 | 复位说明/演练 ＋ **带标签**的夹具清单 | ⭐ **2026-09-20 深夜（B3 轮）**：<br>**① 真实复位演练**＝`reset_demo_env.py --db <仓库外隔离库> --selftest --report` ⇒ **`RESET: OK`**，五项判据**全 True**（①复位清除改动（两轮）`[True,True]`：改动后 `ent_` 42 行 ⇒ 复位后 0｜②迁移记账一致｜③两轮种子后基线一致｜④两轮复位后指纹一致｜⑤两轮均无环境错误）；安全闸 **5/5** 通过（显式 `--db`／SQLite／非开发库／非生产／仓库外）。<br>**② 「新建主链」与「预置检查点」来源可区分**：复位后重铺 ⇒ 4 张种子单各有**唯一标签**（`演示委托·工作台样本`／`…（待受理）`／`演示委托·甲组织队列样本`／`演示委托·乙组织队列样本`），而**新建主链**的载体标题是 `走查·主演示第1-3步（钢材800吨）`（第 43 章经**界面真实提交**建出）——**标签不同、取用方式也不同**（夹具＝**按标题**取；主链＝**链锚点/本轮记下的 id**）。<br>⚠️ 两处已知边界照旧如实登记（不因本轮"OK"而变绿）：**附件一致性**＝`LIMITATION`（默认附件目录多环境共享）、**客户端身份缓存**＝`NOT_APPLICABLE`（走查侧用"每轮全新 IDE 实例"代替清缓存）—— 原文见 `runbook` §6.3。<br>⚠️ 首次演练曾在 `--selftest` 第 2 轮 `unlink` 上撞到 `PermissionError [WinError 32]`（文件被占用）；**换隔离目录后一次通过**，该现象已如实登记（见 `evidence-index` §3.10）。 | **PASS**（真实演练 ＋ 标签可区分） |
| 18 | §12 的**全部文件** ＋ **评审后的 HO 验收记录** | §12 八份：**2026-09-20 起齐**（本轮新增 `DEMO-1-walkthrough.md`、`DEMO-1-acceptance.md`）；⛔ **HO 验收记录尚无** | **部分** |

### 2.2 重算结果（**逐号列出，可从上表数出来**）

| 判定 | 逐号 | 计数 |
| --- | --- | --- |
| **PASS** | 01、02、03、04、**05**、06、07、08、09、10、11、12、13、14、15、16、17 | **17** |
| **部分** | 18 | **1** |
| 合计 | | **18** |

⇒ 按合同 §10.4 `all D1 rows pass`：**达 PASS 17 行、未达 PASS 1 行**。
（⚠️ 09 由 `部分` 转 `PASS` 的依据是**主链那张新委托上的读数**，见 §2.1 第 09 行与
`DEMO-1-evidence-index.md` §3.5／§3.6 —— ⛔ 不是又拿 canonical 的结果充数。
⭐ **13／15 于 2026-09-20 深夜由 B1' 轮转 `PASS`**：干净实例 ＋ **完整配方**、12 章同一 `aid=7`、`FAIL=0`。13 拿到了 X3 缺的那件事（「被拦」与「齐备⇒成功＋重开」**同一张单同一轮并存**）；15 用 HO 裁定的**纯读**新口径并带**零写入正控**。
余下 3 行：**05**（无模型人工通路，B2 轮）、**17**（复位演练，B3 轮）、**18**（HO 验收记录 —— 按裁定放最后）。
⛔ **两个方向都必须跟着证据走**：证据被撤回时判定要**退**（15 上一稿曾由 PASS 降回部分）；证据补齐后判定要**进**（13／15 本轮转 PASS）—— 两个方向都不许留上一稿的红利或欠账。

### 2.3 与两套历史数字的关系（**都不是当前结论**）

| 来源 | 数字 | 状态 |
| --- | --- | --- |
| `readiness` §10.3 小计 | 已设备运行 8／已自动化验证 1／部分 6／未开始 2／补录 1（⇒ 达 PASS 9、未达 9） | **历史**（其口径未随逐行状态重算 ⇒ 已在该处标注并指向本文件） |
| 本文件上一稿 §2.1 | 达 PASS 13／未达 5 | **作废**（规则与结果自相矛盾：一边说"有残留按部分计"，一边把它们计入 PASS） |
| 本文件 2026-09-20 夜稿 §2.2 | 达 PASS 14／未达 4 | **作废**（15 的支撑读数被 HO 抽查证伪 ⇒ 已撤回） |
| **本文件 §2.2（当前）** | **达 PASS 17／未达 1** | **当前结论**（13／15 经 B1' 干净轮转 PASS；17 经 B3 复位演练转 PASS；**05 经 2026-09-21 的无模型人工替代两段取证转 PASS**（段A `FAIL=0`、段B `FAIL=0`、同一张单）；余 **18**＝HO 验收记录） |

## 3. 十三步的**证据覆盖**与**业务顺序**（合同 §10.1）

### 3.1 覆盖分布（`readiness` §10.4 的 2026-09-19 补记 —— ⚠️ **历史，不可作当前结论**）

* **已设备运行 11 步**：1／2／3／4／5／6／7／8／10／11／12
* **部分 2 步**：9（判据需按 §4 的固定前提在主链上重做）／13（齐备⇒结案段依赖夹具，且需主链结案动作）
  * ⚠️ **本节为历史**：第 9 步已于链式轮转 `PASS`；第 13 步已于 2026-09-20 深夜（B1' 轮）在同一张单上完成并转 `PASS` —— **当前结论只看 §2.2**。

⚠️ 这是**证据覆盖分布**，⛔ 不得读成完成率；⛔ **"有设备证据" ≠ "按业务结果可演示"**（后者是 HO 裁定）。

### 3.2 ⛔ 已识别的主链组织缺陷（HO 2026-09-20 第五节）—— **2026-09-20 已落实（步 1–13）**

> ⚠️ **本节记录的是当时的诊断与当时的落实进度**；文中的"仍未落实"一句已过期，见下方改写。

当时的诊断：链式日志虽把各章绑到同一张新委托（`aid=6`），但**仍混入专章测试内容**
（第 4 步读到的服务端任务数为 0；后续章节还建了 `air` 与铁路运输段）——
它们检查通用功能有价值，**却不构成合同要求的「公路—水路—公路」业务演示**。
⇒ 绑定同一个 ID 只解决"是否同单"，还要解决"这张单是否按正确业务顺序推进"。

**现在的状态**（证据 `DEMO-1-evidence-index.md` §3.5／§3.6，链式轮 N/P/Q）：

* 固定顺序 `43,chain4,45,44,49,50,chain9,55` ⇒ **8 个取单点全部 `aid=6`（同一张=是）**；
* 第 4 步末端三段方式 **`['road','water','road']`**、**没有** `air`/铁路段；
* 第 4 步的"任务数 0"已消除：服务端任务 3 条、前置链 `8 → 9 → 10`；
* ~~仍未落实的只剩**第 12–13 步**（结案/重开/重进）在本单上的动作 ⇒ 对应 D1-13 仍记 `部分`。~~
  ⚠️ **该句为历史**：第 12–13 步已于 **2026-09-20 深夜（B1' 轮）在同一张单（`aid=7`）上完成**（`chain11 ①b` 结案被拦 → `③b` 关案件 → `④ ready=True 缺 []` → `53 ⑧` 结案成功 → `⑨` 受控重开），
  `D1-13` 已转 `PASS`（依据见 §2.1 第 13 行、证据索引 §3.9）。
### 3.3 B1' 轮的 `NOT_RUN` / `LIMITATION` 逐条去向（⛔ 不用"对象不存在"一句话替代验收判断）

该轮（B1'，**`FAIL=0`**）共 **7 条 `NOT_RUN` ＋ 1 条 `LIMITATION`**。逐条写清**它要证什么**、
**本轮为什么没跑**、**已由哪里证明或边界在哪**（原始行见证据索引 §3.9）：

| # | 条目 | 要证什么 | 本轮没跑的原因 | 去向 |
| --- | --- | --- | --- | --- |
| 1 | `㊸ 第2步 · 从系统里选文件` | 走 **OS 原生文件选择器**上传 | 工具够不到 OS 弹层（`LIMITATION`，见 §5 L-2／L-7） | ⛔ **不宣称通过**；同章**内置样本上传通路**通过 ⇒ 该步的**业务结果**已证（两条路径分开记，见 L-2） |
| 2 | `㊹ 前置 · 客户可见成果` | 第 6 步只能发布**客户可见**类型 | 该委托下**没有**客户可见成果（白名单 `contract_review`／`customer_quote`，命中 0） | 这是**设计**（内部成果如 `quote_parsed` 一律 400）；该前置随后由同轮组装的对客报价满足 |
| 3–5 | `㊹ 第6步 · 来源核验 三条` | 「被拒 ⇒ 逐条核验 ⇒ 再发布」 | 本轮载体是**经界面人工组装**的成果（来源 `manual`）⇒ 服务端**没有待核验声明**（`gate.ok=True`、`pending` 0）⇒ 门槛**一次就过**，负例无对象 | ⚠️ 上一轮这三格记 `FAIL` 并据此报了「界面无核验入口」—— **那是误读**，本轮已更正 |
| 6 | `㊹ 第6步 · 授权清单冻结在发布记录` | 授权清单**真冻结**（非"同属一单就全开"） | 载体 `artifact#10` **没有附件**⇒ 断言会**真空通过**（`[] == []` 恒真） | ⛔ **主动不记**真空通过；要取证需让载体本身带附件（夹具面最小改动，未做） |
| 7–8 | `53 ⑥／⑦ · 不齐备时点结案` | 「不齐备 ⇒ 点结案**被拦** ＋ 状态不变」 | 本章选到的单**已齐备**（`ready=True`、`missing=[]`）⇒ 无"被拦"可验 | ⭐ **已由同轮 `chain11 ①b-a/b/c` 证明**（清理**之前**点结案：界面先给缺项清单、点下去**被拦**、状态未变 `claimed`）；历史轮 X／X2 另有一条**不齐备**的独立读数 |

⇒ 8 条里 **7 条有可核对的去向**：3 条由**同轮其他章节证明或更正**、2 条是**设计使然**、
1 条是**工具边界**、1 条是**真空通过主动不记**。
⛔ 本节**不改变**任何行的 `PASS`／`部分` 判定 —— 它只是把"为什么没跑"写成可核对的形式。

---


## 4. 主演示的**固定业务前提**（HO 2026-09-20 裁定，⛔ 不得为迎合剧本改规则）

> **同一张新委托**，初始需求 **800 吨**、**不允许拆批**；先确认一条 **900 吨候选**（此时有效）；
> 需求经**审批**变为 **950 吨**后，**原确认失效**；经理完成**替换／重新确认**，
> 并处理相应的**客户重新接受**与 **A2 接管**。

* ⚠️ 上述条件必须在**变更前**作为业务记录建立（经命令/界面），⛔ **不能在失败后直接改库**。
* ⛔ 「允许拆批」场景下该格的 `NOT_RUN` **不能替代 D1-09**（那只说明该次测试条件不适用）。
* ⛔ 不能换一张 canonical 委托取得 `PASS` 再把它算作新委托的连续演示结果。

## 5. 限制清单（如实）

| # | 限制 | 类型 |
| --- | --- | --- |
| L-1 | **㊸ 第 2 步**：链上四轮复跑全部走通（每轮 `PASS ｜ ㊸` **29** 条；H 轮 `NOT_RUN（LIMITATION=1）`）。此前 `FAIL` 只出现在**"保活 3 小时的老实例"**轮次 ⇒ 非稳定缺陷、根因**未定档**；⛔ **产品代码一行未改** ⇒ 不得写成"已修复" | 未定档项（诊断降级为有边界的待查，见 §7） |
| L-2 | **第 2 步的两条路径分开记**（HO 裁定）：① **本轮选定的内置样本上传通路** —— 按实际结果判定（上传/提取/作业/采纳/更正全通过）；② **系统文件选择器** —— **保留未验证／工具限制**，⛔ 不宣称通过，⛔ 也不为它扩建 OS 自动化、不把它变成新的交付前置、不删除原始限制记录 | 工具边界 |
| L-3 | **1 个"部分"**：**18**＝缺 **HO 验收记录**（按 HO 裁定**放最后**签）。<br>⚠️ **05／13／17 均已转 `PASS`**，不再计入：05＝2026-09-21 **无模型人工替代通路**两段取证（段A 附件分支 `FAIL=0`、段B 业务分支同单 `FAIL=0`，见索引 §3.12／§3.12.1）；13＝B1' 干净轮「被拦」与「齐备⇒成功＋重开」**同一张单同一轮并存**；17＝B3 **隔离复位演练** `RESET: OK`＋「新建主链 vs 预置检查点」**标签可区分** | 缺口 |
| L-4 | **真模型 / fixture 两种模式不可互相冒充**：真模型运行中**实际触发并验证的业务规则同样是有效证据**（⛔ 不得固定要求真模型复现 fixture 专属输出）；反过来说，fixture 轮也不证明模型质量 | 证据口径 |
| L-5 | **同一时刻只允许一个走查 runner**（都占 8000）；runner 已加硬闸门（占用即拒跑并点名），⛔ 但它不替用户杀进程 | 环境 |
| L-6 | **本机无 MySQL** ⇒ 并发/幂等锚点只由 CI 的 `pytest-mysql` 执行；长 `COMMENT` 不折行 ⇒ 迁移 SQL 本机无法执行验证 | 环境 |
| L-7 | 走查工具的既有 `LIMITATION`：下拉刷新真手势、原生文件选择器、分页第 2 页（需 >20 张单） | 工具边界 |

## 6. HO 验收记录（**留白 —— 待 HO 签署**）

> **这是 D1-18 唯一缺的那一件**：合同 §12 的八份文件**已齐**（见 §2.1 第 18 行），
> 缺的是「**评审后的 HO 验收记录**」。逐行计数以 **§2.2 在签署时**的结论为准。
> 本栏**由 HO 填写**，⛔ 未经签署不得写成 `DEMO-1 已验收`。
> 签署入口与待拍板事项见 `DEMO-1-delivery-package.md` **§8**（那份是 HO 终验的单一入口）。

### 6.0 签署栏（**留给 HO，本文件不代填**）

| 项 | 内容 |
| --- | --- |
| 覆盖范围 | 合同 §12 八份文件（见 §2.1 第 18 行）＋ 本文件 §2.2 的逐行结论<br>候选＝**两段**：`aa880b3`（已合，PR #185）＋ `0baad07`（**待合**，PR #186，D1-05 的两段证据所在） |
| 验收结论 | _（待 HO 填写：`pass` / `partial preview` / `打回`）_ |
| 签署人 / 日期 | _（待填）_ |
| 附条件 | _（待填；无则写「无」）_ |

**填写口径（三条，避免签成"不知道签了什么"）**

1. 结论只能取三值之一；填 `pass` 才构成合同 §10.4 的 `all D1 rows pass`（**含第 18 行本身**）。
2. 余量与限制**不在本栏重述**，以 §5 的限制清单（L-1…L-7）与交付包 §6 的演示边界为准；
   若 HO 对其中任一条不接受，写进「附条件」，⛔ 不要靠"结论填 pass"把它抹掉。
3. **合并与否与验收是两件事**：PR #186 是否授权合并（CI 6/6、`mergeable=clean`）不影响本栏填写，
   但**未合并时 D1-05 的证据只在该分支上** ⇒ 复核须先检出它。

### 6.1 三个待裁定项的处置（**已由 HO 2026-09-20 裁定，本节记录落地情况**）

| 待裁定项 | HO 裁定 | 落地 |
| --- | --- | --- |
| 计数口径 | **两者都不直接采用，按合同判据与有效证据重算**；旧统计转历史 | ✅ §2.0–§2.3（重算 **16／2**：09 于 2026-09-20 由主链同单读数转 PASS；13／15 于同夜 B1' 干净轮转 PASS；17 于 B3 复位演练转 PASS）＋ `readiness` §10.3 标注历史 |
| 允许拆批下 900 吨候选未失效能否记 `NOT_RUN` | **可作为该次测试条件不适用的记录，不能据此完成 D1-09** | ✅ §4 固定业务前提；**D1-09 已于 2026-09-20 在主链同单上达 `PASS`**（㊿ 的 `NOT_RUN` 分支保留在脚本里，链式轮次不走它） |
| 是否本轮补 `walkthrough.md` | **是，属既定交付** | ✅ `DEMO-1-walkthrough.md`（2026-09-20 建立） |

## 7. ⛔ 结论

1. **自动化侧**：**PR #185 已合并**（squash `aa880b3`，合并前在 `26f8a299…` 上门禁 **16/16**、
   CI **6/6 SUCCESS**、pytest 1015 项 0 失败）；**⛔ 合并不等于本文件通过**。
   2026-09-21 的 P1 分支（`s7f-manual-substitute-chain`）另含：日期夹具修复（提交 `4002dc3`，
   门禁回到 **16/16**）、证据取证小工具（`a134850`）、无模型人工替代通路（段A／段B）。
   ⚠️ **CI 绿 ≠ 走查绿**：设备侧的汇总行要逐轮看 —— B1' 轮是 `RESULT: NOT_RUN`
   （`FAIL=0 / NOT_RUN=7 / LIMITATION=1`，去向见 **§3.3**）；段B 干净轮是 `RESULT: NOT_RUN`
   （`FAIL=0 / NOT_RUN=13 / LIMITATION=2`，去向见索引 **§3.12.1**）。⛔ 都**不得写成"全绿"**。
2. **D1 逐行：达 PASS 17 行、未达 PASS 1 行**（口径与逐号见 §2.2）。未达的一行：
   **18**＝HO 验收记录（按裁定放最后签）。
   **05 于 2026-09-21 转 `PASS`**：模型**持续不可用**下，经理**经界面人工建立成果**后，
   在**同一张委托**（13 个取单点全部 `aid=7`）上走完 接受→合同→变更→履约→结算→**结案**
   →**受控重开**→**重载一致性**，且该轮 **`FAIL=0`**（⚠️ 前两轮的 4+4 条 FAIL 已逐条定档：
   1 条我方判据太宽、3 条＝模型不可用未做状态感知、4 条＝**实例退化**并已**换实例**重取）。
3. ⛔ **仍构成 `partial preview`**，**不是** `DEMO-1 已验收`：
   **第 1–13 步已在同一张新委托上跑通**（2026-09-20 深夜 B1' 轮：12 章**同一 `aid=7`**、`FAIL=0`），
   其中「**不齐备 ⇒ 结案被拦**」与「**齐备 ⇒ 结案成功 ＋ 受控重开**」**在同一张单、同一轮**并存；
   13 步的**下游一致性**由 `chain12` 纯读证明（重进后逐字相同 ＋ **零写入正控** `写请求＝无`）。
   ⭐ 2026-09-21 再加一层：**模型持续不可用时**（`LLM_BASE_URL` 不可达、`mocked=False`）
   **同一条链**也在**同一张单**上跑通（13 章、`FAIL=0`），成果由**人工组装**建立
   ⇒ 覆盖 D1-05 的**业务生命周期**主张。
   仍未闭合：**18**（HO 验收记录 —— 本文件 §6 留白待签）。
4. **下一步**（HO 2026-09-21《PR185 交付包评审与裁决建议》三件事）：
   * **P1** 真实**人工替代路径**：模型**持续不可用**（mock 关闭）⇒ 从新委托经**现有人工入口**
     建立成果，并沿**同一张单**一路到 **接受→合同→变更→履约→结算→结案→重载**；
     ⛔ 不恢复模型、⛔ 不注入成功提案、⛔ 不改库补数据。
   * **P2** `reset_demo_env.py` 的 **`unlink` 占用最小修复**（⛔ 不靠换目录）＋ **定向两轮**复验。
   * **P3** 文档收口（本节即其产物：候选固定、过期读数作废、限制清单归位、确定性推断软化）。
   ⛔ 不再以"新增规则／技能条款／大量全量复跑"代替交付。
5. ⛔ 本文件**不含** HO 验收记录（§6 留白待签）。
