# DEMO-1 验收记录（Acceptance）—— **待评审稿**

> ⛔ **本文件不是"已验收"**：它是一份**可核对的候选**，**HO 验收记录一栏为空**。
> 合同 §10.4 的判定口径未变：**当前状态构成 `partial preview`**，不是 `DEMO-1 已验收`。
> 本文件按合同 §12 建立（此前**缺失**，见 `DEMO-1-evidence-index.md` §4 第 3 条）。
>
> 建立日期：2026-09-20 ｜ 依据：`DEMO-1-readiness.md` §10.2–§10.5、`DEMO-1-evidence-index.md` §1–§3.2

---

## 1. 候选版本（可复跑）

| 项 | 值 |
| --- | --- |
| **候选 SHA** | `83ccb3f65c296b4cd1dbea609bd7b074241fb533`（`develop` 当前 HEAD；PR **#179** squash 合并的产物） |
| **CI** | 该 SHA 上 **6/6 job SUCCESS**：后端 lint+test ／ 迁移 SQLite 冒烟+幂等 ／ 迁移 MySQL 8.0 真实执行 ／ 并发集成 MySQL 8.0 ／ 前端静态契约 ／ 前端端到端（真后端载荷驱动） |
| **本机门禁** | `python -X utf8 scripts/verify_local_gates.py` ⇒ `{"total": 16, "failed": 0}` |
| **后端单测** | pytest **1015** 项（通过 998 ／ 跳过 17 ／ 失败 0 ／ 错误 0） |
| **证据入口** | `docs/entrust/evidence/`（44 件入库；`* -text` 逐字节入库，工作区哈希 == 索引 blob 已复核） |
| **索引** | `DEMO-1-evidence-index.md` §1（链式连跑）／§2（E-B）／§3（门禁）／**§3.1**（全量回归＋定档）／**§3.2**（整改验证轮） |
| **环境** | 见 `DEMO-1-runbook.md`；⚠️ **模型模式必须写明**（§8.7）：真模型 vs fixture **不可互相冒充** |

## 2. D1-01 – D1-18 逐行结果

| D1 | 判据概要 | 设备侧出口 | **档（逐行）** | 行内 ⚠️ 残留 |
| --- | --- | --- | --- | --- |
| 01 | 全新委托经真实入口创建提交 | ㊸ 第 1 步、㉞ | 已设备运行 | — |
| 02 | 受理 ＋ 竞争受理不得造出第二个责任主体 | ㊸、㊱、㊴、㊵ | 已设备运行 | — |
| 03 | 一次 **live** AG-02 会话产出带来源提案 ＋ 可见 job 状态 | ㊸ 第 2 步（fixture） | **部分** | live 调用只有**脚本**证据，**非**设备侧同轮 ⇒ O-3 |
| 04 | 更正/采纳在聊天、详情、工作台是同一 artifact/revision | ㊸ 第 3 步 ＋ 会话屏 | 已设备运行 | — |
| 05 | 无模型/API Key 也能走完范围内生命周期 | 步 1–7 本轮；步 8/10/11/12 见 ㊿／52／53/54 | 已设备运行 | 后三者来自**单独跑**；章 53「齐备⇒结案」段依赖夹具 |
| 06 | 两家可比 ＋ 只在证据齐备时确认运力 | ㊺ | 已设备运行 | — |
| 07 | 客户接受/拒绝**精确版本**；未释放/被取代/经理冒充都失败 | ㊹（403 ／ 409 两条负例） | 已设备运行 | 「被来源门槛拒」分支由**章 51** 补（fixture 轮 `PASS`） |
| 08 | 合同从**已接受事实**派生；证据 ≠ 实时电子签 | ㊾ | 已设备运行 | 收尾口径见 §10.5 |
| 09 | 800→950 走审批/应用/复核，且 900 吨候选变为不适用 | ㊿ ＋ ㉛/㉜ ＋ ㊺ | **部分** | ⭐ 2026-09-20 实测：该格**载体相关** —— 允许拆批的候选上「变更后不再成立」**没有对象**（记 `NOT_RUN`）；canonical（不允许拆批）上 `PASS` ⇒ 引用时**必须写清是哪一档载体** |
| 10 | 交接证据/争议加项/结算与确认被记录，总额正确 | 章 52 | 已设备运行 | 前置任务经**接口**铺（界面无 `required_evidence` 入参）；补录动作是真机点击 |
| 11 | 未授权读/写/下载/作业一律失败且**不泄漏** | ㊲／㊵ | 已设备运行 | — |
| 12 | 接管/重启/重试不得覆盖人工成果或重复产生后果 | ㊴ ＋ **第 55 章** | 已设备运行 | 真·**跨进程**续接记 `NOT_RUN`（㉞） |
| 13 | 结案只在全部适用前置满足时成立 | 章 53（＋54 读侧） | 已设备运行 | 来自**单独跑**；齐备段依赖夹具 `seed_entrust_completion_ready.py` |
| 14 | 主/抽屉/会话/工作台/客户/案件导航经真实入口且保上下文 | ㉝、㉞ | 已设备运行 | 含 1 条 `LIMITATION`（下拉刷新真手势，工具边界） |
| 15 | 见证者改一个受支持的值，下游随之变化且**重载后仍在** | ㊸ 第 3 步 ＋ ㊶ ＋ ㊺ | **部分** | 两条断言（"重载后仍在"／"下游随之变"）**未串在同一条链上** |
| 16 | 既有必需 CI 绿；新迁移在**全新与升级**库上都可用 | CI（无设备侧） | **已自动化验证** | — |
| 17 | 环境可**一致复位**；fresh-run 与 seeded-checkpoint 可区分 | DR-0018 的 B | **部分** | 「可区分」那一半属 S5-1/S5-2 |
| **18** | 最终证据包与原需求**余量台账**完整且自洽 | 本文件 ＋ 索引 | **部分** | §12 八份：本轮起**已 7 份**（新增本文件）；仍缺 `DEMO-1-walkthrough.md`；⛔ **HO 验收记录尚无** |

### 2.1 ⭐ 计数（**逐号列出，可从上表数出来**）

本文件采用的口径：**行内仍有 ⚠️ 残留的，按 `部分` 计**（"整行达 PASS"＝该行全部判据都在设备侧成立）。

| 档 | 逐号 | 计数 |
| --- | --- | --- |
| 已设备运行 | 01、02、04、05、06、07、08、10、11、12、13、14 | **12** |
| 已自动化验证 | 16 | **1** |
| 部分 | 03、09、15、17、18 | **5** |
| 合计 | | **18** |

⇒ 按合同 §10.4 `all D1 rows pass`：**达 PASS 13 行**（12 ＋ 1）、**未达 PASS 5 行**。

### 2.2 ⚠️ 与 `readiness` §10.3 小计的差异（**需 HO 明确口径**）

`readiness` §10.3 的小计写的是：已设备运行 **8**（01/02/04/06/07/08/11/14）、已自动化验证 1（16）、
**部分 6**（03/05/09/12/15/17）、未开始 **2**（10/13）、补录 **1**（18）。

两者差在 **05 / 10 / 12 / 13** 这 **4 行**（各自的 `档` 列写"已设备运行"，而小计把它们算进"部分/未开始"），
外加 **18 行**的措辞（本文件记 `部分`、小计记"未开始（本轮补录）" —— 同属"未达 PASS"，不影响计数）。
可核对的事实是：

* 这 4 行的 `档` 列**都带一个 ⚠️ 残留**（单独跑／依赖夹具／`NOT_RUN` 子分支）；
* 但小计把**同样带 ⚠️ 的 07** 算进了"已设备运行" ⇒ 小计用的**不是**"无 ⚠️"这条规则；
* S7 落地（章 51/52/53 的 `PASS`）发生在小计之后 ⇒ 小计**极可能未随逐行状态重算**。

⛔ **本文件不擅自改小计**：两条计数都列出、差在哪几行也点名，**口径由 HO 裁定**
（这也符合 `DEMO-1-readiness.md` §10.4 的既有纪律：逐行计数必须能从上表数出来）。

## 3. 合同 §10.1 十三步的**设备证据分布**

以 `readiness` §10.4 的 2026-09-19 补记为准：

* **已设备运行 11 步**：1 ／ 2 ／ 3 ／ 4 ／ 5 ／ 6 ／ 7 ／ 8 ／ 10 ／ 11 ／ 12
* **部分 2 步**：9（变更后判据**载体相关**，见上表 D1-09）／ 13（齐备⇒结案段依赖夹具）

⚠️ 这是**证据覆盖分布**，⛔ **不得**把 11÷13 读成完成率 —— 后半程涉及费用、确认与原子结案。
⛔ **"有设备证据" ≠ "按业务结果可演示"**：后者是 HO 的裁定。

## 4. 限制清单（如实）

| # | 限制 | 类型 |
| --- | --- | --- |
| L-1 | **㊸ 第 2 步「用内置示例报价单」**：2026-09-20 上午**四轮复跑全部走通**（每轮 `PASS ｜ ㊸` **29** 条；最优读数 `NOT_RUN（LIMITATION=1）`，唯一非 PASS 的是**工具边界**那一格）。⚠️ 之前的 `FAIL` 只出现在**"保活 3 小时的老实例"**轮次 ⇒ **不是一个稳定缺陷**；⛔ 但**产品代码一行未改**、根因**未定档**（疑与 IDE 会话年龄/复用轮数相关）⇒ **不得写成"已修复"** | 已复跑的**未定档**项 |
| L-2 | **真模型质量未验证**：fixture 轮（`--llm-mock`）证链路规则；真模型轮下 AG-02 提案集合由模型输出决定 | 证据口径 |
| L-3 | **O-3**：D1-03 的 live 会话与设备侧**未同轮取证**（需真 Key 额度） | 开放项 |
| L-4 | **O-5**：交付前的既有 `FAIL`（⑮/㉕B–D 一类）逐条归因已完成（⑮ 2026-09-20 转 `PASS`）；⛔ 改 `NOT_RUN` 只能说明"未执行"，**不能消除验收缺口** | 开放项 |
| L-5 | **O-8**：㊾ 章"多章连跑"下的三条 `FAIL` 归因**未完成**（单跑全绿）；已裁定为"不是稳定缺陷、修就绪判据即可"，⛔ 未升级为"已修复" | 开放项 |
| L-6 | **工具边界 `LIMITATION`**：下拉刷新真手势、原生文件选择器（OS 弹层）、分页第 2 页（需 >20 张单）—— 都是**工具够不着**，不是产品缺陷 | 工具边界 |
| L-7 | **本机环境约束**：无 MySQL（并发锚点只由 CI 的 `pytest-mysql` 执行）；长 `COMMENT` 不折行 ⇒ 迁移 SQL 本机无法执行验证 | 环境 |
| L-8 | **同一时刻只允许一个走查 runner**（都占 8000）；已加硬闸门，⛔ 但它**不替用户杀进程** | 环境 |

## 5. ⛔ 结论

1. **候选 `83ccb3f` 的自动化门禁与 CI 全绿**（本机 16/16、CI 6/6）。
2. **设备侧证据覆盖 11/13 步（＋2 步部分）**，D1 逐行 **13 行达 PASS / 5 行未达**（口径见 §2.1）。
3. ⛔ **仍构成 `partial preview`**，**不是** `DEMO-1 已验收`：D1-03 的 live 与设备未同轮、
   第 9 步判据载体相关、㊸ 的根因未定档（失败只在老实例轮次复现）、两份 §12 交付物尚缺一份
   （`DEMO-1-walkthrough.md`）。
4. ⛔ 本文件**不含** HO 验收记录 —— 下一节留白，由 HO 填写。

## 6. HO 验收记录（**留白 —— 待填**）

| 项 | 内容 |
| --- | --- |
| 验收结论 | _（待 HO 填写：`pass` / `partial preview` / `打回`）_ |
| 签署人 / 日期 | _（待填）_ |
| 附条件 | _（待填）_ |

### 6.1 提交 HO 裁定前的三个待明确点

1. **计数口径**（§2.2）：05/10/12/13 这 4 行按"逐行 `档` 列"还是按 §10.3 小计算？两套数字都已逐号列出。
2. **第 9 步的载体口径**（D1-09）：允许拆批的候选上「变更后不再成立」记 `NOT_RUN` 是否认可？
   （canonical 载体上该格 `PASS`；两档读数都在索引 §3.1。）
3. **是否把 `DEMO-1-walkthrough.md` 纳入同一轮交付**（§12 八份中唯一尚缺的一份：
   主脚本 / 角色 / 预期结果 / 诚实回退）。
