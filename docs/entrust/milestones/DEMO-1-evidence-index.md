# DEMO-1 证据索引（Evidence index）

> 合同 §12 要求的第 8 份仓库内交付物。本合同条款允许**大型录制品放在 Git 之外**，
> 但必须给出**稳定、可授权的引用**；文本日志与关键截图**直接入库**。
> ⛔ 本文件不含口令、访问令牌、模型 Key，也不含与本任务无关的客户数据
> （入库前已对 `sk-` / `Bearer` / `token=` / `Authorization` / `api_key` / `SECRET`
> 逐项扫描，仅有两处**假阳性**：`task-5` 含 `sk-`、`token=已切到 owner` 是状态说明）。

## 0. 怎么读这张索引

每条证据给四样东西：**候选版本**、**命令**、**环境与模型模式**、**可打开的原始文件**。
「档」用与 `DEMO-1-readiness.md` 相同的四档：已实现／已自动化验证／**已设备运行**／仍待验收。

⛔ **一条纪律**：本索引只登记**实际发生过**的运行。开发者报告（口头或聊天里的读数）
一律标注「开发者报告，未经 HO 核验」。

---

## 1. E-2026-09-19-A — 全量 `--section all` 首跑（**全量分章回归**，不是主链）

| 项 | 值 |
| --- | --- |
| **档** | **已设备运行**（47 章 / 787 断言） |
| **性质（⛔ 不得误读）** | 这是**全量分章回归首跑**，**不是**「同一张新委托走完 13 步」。各章各自挑委托（部分受 `--anchor` 偏好影响）⇒ 其结果**不能拼成**主链证据。发包方 2026-09-19 评估已明确此定性 |
| **候选版本** | `dc3e711`（`#174` squash；本轮使用的走查脚本内容与它**同一棵树** —— 运行开始时这些改动尚未提交，事后以 `#174` 合入） |
| **命令** | `python scripts/run_walkthrough_devtools.py --section all --pay --extra-seeds seed_entrust_canonical.py,seed_entrust_contract_flow.py` |
| **时间** | 2026-09-19 `17:34:45` 起（产物目录名）～ 约 `18:46` 止 |
| **环境** | Windows；仓库 `E:\pinglu-didi`；`.venv`（CPython 3.13）；模拟器＝微信开发者工具（wechatide 工具链）；数据库＝**runner 自建临时 SQLite**（非生产库） |
| **模型模式** | ⚠️ **未设 `LLM_MOCK`**（`run_walkthrough_devtools.py` 里 `LLM_MOCK` 出现 **0** 次）。该轮**究竟走真模型还是降级 —— 未定**：判定取决于 `backend/.env.local` 是否存在，而该文件（gitignore，内含**真订阅 Key**）在 **2026-09-19 核实期间已缺失**，且**回收站无记录**（git 的 `unlink` 是**永久删除**，不经回收站）⇒ **无法回溯该轮的实际模式**。⛔ **不得写"本机无 Key"**（那是错误前提：Key 一直存在，只是文件被删）；⛔ 也不得据此声称跑过真实模型会话。**下一轮复跑时把 `LLM_MOCK` 显式钉在命令行**并记进本索引 |
| **逐步结果** | 见下方「原始文件」中的 `walk-all-stdout.txt`（每步一行，含判据与读数） |
| **汇总** | `PASS=750` / `FAIL=11` / `NOT_RUN=9` / `LIMITATION=17` / `ENV_BLOCKED=0` / `REVIEW_REQUIRED=0`；运行期 console error **0** |
| **覆盖边界** | ⛔ 本章循环当时**不含** `53` / `54`（`DEFAULT_ORDER` 静默漏项，已修）⇒ **第 12 步与十三步产物链不在本轮内**；产物里 `53`/`54` 各出现 **0** 次 |

### 原始文件（已入库）

| 文件 | 大小 | sha256(前 16) | 说明 |
| --- | --- | --- | --- |
| [`evidence/2026-09-19-walk-all/walk-all-stdout.txt`](evidence/2026-09-19-walk-all/walk-all-stdout.txt) | **140,636 B** | **`23ed84b331e499d9`** | 走查**完整 stdout**（含每步 `PASS/FAIL/NOT_RUN/LIMITATION` 与读数、汇总节、截图目录、重跑关联）。⚠️ 2026-09-20 **重记**：首次登记记的是**工作区 CRLF 原件的**体积/哈希（141,738 B / `a60e1d3a46b8ab03`），而根 `.gitattributes` 的 `* text=auto eol=lf` 把它归一化成 LF 入库 ⇒ **从 git 取出来的哈希与登记对不上**。现已给 `docs/entrust/evidence/` 加 `* -text` 并重存（见 `evidence/.gitattributes`），本行的数字是**当前入库字节**的实测值 |
| ~~`evidence/2026-09-19-walk-all/backend-access.log`~~ | ~~1,105,018 B~~ | ~~`cbb03f60aca21631`~~ | ⛔ **已撤销登记 —— 该文件从未入库，且原件已不可取得**。见下方「§1 的缺口」 |

### §1 的缺口（2026-09-20 复查发现，如实登记）

| 项 | 情况 | 处置 |
| --- | --- | --- |
| 后端访问日志 `backend-access.log`（原记 1,105,018 B） | ⛔ **从未入库**；且 `miniapp-device-artifacts/_uvicorn.log` 是**每轮运行复用/截断**的同一个文件，现已被后续运行覆盖（当前 1,342,216 B，属 09-20 00:56 那轮）⇒ **原件不可再取得** | ① 撤销该行的"已入库"登记；② 它当年支撑的判断（"㊸ 那一段没有 `POST /entrust/attachments`"）已由 **§10.2.2** 的四个直接读数取代 —— 那是**更强**的证据形式（读数在产物文件里、可随 PR 复核），不必依赖这份日志；③ 后续运行若需要后端日志，应在当轮**另存**一份到 `evidence/<日期>/`，⛔ 不能只留 `_uvicorn.log` |

### 关键截图（已入库，取自 `walk-20260919-173445/`）

| 文件 | sha256(前 16) | 对应 |
| --- | --- | --- |
| [`shots/43-5-专属会话屏.jpg`](evidence/2026-09-19-walk-all/shots/43-5-专属会话屏.jpg) | `dfb374ee36fb4400` | 步 1–3 · 进入会话屏（上传前） |
| [`shots/43-6-附件已上传并提取.jpg`](evidence/2026-09-19-walk-all/shots/43-6-附件已上传并提取.jpg) | `650f4f8e2f5a920f` | ⭐ **`㊸` 失败现场**：与 43-5 画面几乎无差异（`25253` vs `25260` B）⇒ 点击后页面**没有进入上传态** |
| [`shots/15-委托发货-功能预览.jpg`](evidence/2026-09-19-walk-all/shots/15-委托发货-功能预览.jpg) | `cf8c9fd91baac060` | ⭐ `⑮` 失败现场：真机落点＝**受理屏**（断言当时仍写「功能预览」占位页 ⇒ 过期断言） |
| [`shots/25-A-成果详情-查看态.jpg`](evidence/2026-09-19-walk-all/shots/25-A-成果详情-查看态.jpg) | `c6915133e814cc2e` | `㉕B` 查看态（编辑前的界面） |
| [`shots/25-B-成果详情-编辑态.jpg`](evidence/2026-09-19-walk-all/shots/25-B-成果详情-编辑态.jpg) | `fc274cec1b294149` | ⭐ `㉕B/C/D` 失败现场：`.btn-primary` 点到「发布」提交键 ⇒ **未进入编辑态** |
| [`shots/45-5-确认成功.jpg`](evidence/2026-09-19-walk-all/shots/45-5-确认成功.jpg) | `9badc58c5f596b04` | **第 5 步**（运力确认闭环）通过 |
| [`shots/52-财务与结算.jpg`](evidence/2026-09-19-walk-all/shots/52-财务与结算.jpg) | `0afc2567e0f4d506` | **第 10–11 步**（费用→缺件→结算→客户确认→结清）通过 |

### Git 之外的大件（附引用方式）

| 资产 | 体量 | 位置与获取 |
| --- | --- | --- |
| 本轮**全部截图** | 146 个文件 / 4.9 MB | 开发机 `miniapp-device-artifacts/walk-20260919-173445/`（`.gitignore:67` 排除）。**入库的 7 张是关键子集**；需要全集时由开发者打包提供（⛔ 该路径本身**不构成**交付） |
| 本轮临时数据库 | — | runner 每次新建、跑完即弃 ⇒ **不可复现取证**，请以 `walk-all-stdout.txt` 的 API 直证读数为准 |

---

## 2. E-2026-09-19-B — `--section 53,54`（第 12 步 ＋ 十三步产物链）

| 项 | 值 |
| --- | --- |
| **档** | **已设备运行**（章 53 `×24` 全 PASS；章 54 读侧 `PASS=9 / NOT_RUN=1`） |
| **命令** | `python scripts/run_walkthrough_devtools.py --skip-ide --section 53,54 --extra-seeds seed_entrust_canonical.py,seed_entrust_contract_flow.py,seed_entrust_completion_ready.py` |
| **产物** | `_walk_090034_out.txt`（章节 `53,54`，34 断言）＋ `_walk_124045_out.txt`（章节 `50,54`，27 断言，`54 ⑤` 转 PASS 那一次） |
| **⚠️ 登记口径** | 这两份**尚未入库**（当时未按 §12 收口）⇒ 现按**开发者报告**登记，**未经 HO 核验**。补齐方式见本文件末尾「待补」 |

---

## 2.1 E-2026-09-20-A — 「同一张新委托」**链式连跑**（步 1–11 ＋ 步 13 的一半）

**这是什么**：第一次让**同一张新建的委托**走完后续各步（机制见 `DEMO-1-readiness.md` **§10.2.3**）。
⛔ 它**不是**"13 步全部走完"：第 9 步的接管与第 12 步不在本轮链上（见下面"未覆盖"）。

| 项 | 值 |
| --- | --- |
| 候选版本 | 工作区 `E:\pinglu-didi`（分支 `s5l-upload-readouts-and-guard-auto`；合并前请以 PR 正文的 head 为准） |
| 命令 | `python scripts/run_walkthrough_devtools.py --skip-ide --pay --section 43,46,47,48,45,44,49,50,31,32,52,41 --extra-seeds seed_entrust_canonical.py,seed_entrust_contract_flow.py` |
| 时间 | 2026-09-20 00:16 – 00:46（约 30 分钟） |
| 环境 | 复用已就绪的实例（`--skip-ide`），IDE 由 `--prepare-ide --keepalive 10800` 保活 |
| 模型模式 | **命令行未显式钉 `LLM_MOCK`** ⇒ 本轮实际模式**未定**（与 E-A 同一口径，⛔ 不得写成"无 Key"） |
| 链式载体 | 章 43 新建的委托 **`aid=6`**（读数直证 `链式锚点=已生效`） |
| 结果 | 250 条断言 · `PASS=232` / `FAIL=2`；截图 42 张 |

### 原始文件（已入库）

| 路径 | 字节 | sha256(前 16) |
| --- | --- | --- |
| `evidence/2026-09-20-chain-13steps/walk-chain-stdout.txt` | 57501 | `a4e397b721c0188d` |
| `evidence/2026-09-20-chain-13steps/summary.json` | 70778 | `60fb82f0edc90348` |

### 关键截图（已入库，取自 `walk-20260920-001602/`）

| 路径 | 字节 | sha256(前 16) |
| --- | --- | --- |
| `shots/43-2-提交后落详情.jpg` | 29178 | `c53cf5ac25cf8310` |
| `shots/43-4-受理成功.jpg` | 41915 | `5607138872e4cafa` |
| `shots/43-6-附件已上传并提取.jpg` | 35298 | `aae1cb779759024b` |
| `shots/43-13-从工作台打开同一份成果.jpg` | 23163 | `c2f0bcfcc0b5ac41` |
| `shots/㊻-运输计划卡.jpg` | 30477 | `e27afefd786034a5` |
| `shots/㊼-航段命令写侧.jpg` | 33142 | `0b0c7bfb25abf863` |
| `shots/㊽-货主建段.jpg` | 33118 | `bb0c257441911491` |
| `shots/45-5-确认成功.jpg` | 33128 | `84b1d5cad3eb6374` |
| `shots/44-7-客户已接受.jpg` | 33118 | `360e41a9ea09e587` |
| `shots/㊾-合同派生与签署证据.jpg` | 33117 | `cbe4a9ea3bec1c15` |
| `shots/31-案件页-应用后-复核传播.jpg` | 22747 | `a19763620bbff5d7` |
| `shots/32-成果页-待复核徽标.jpg` | 30331 | `741eaa1bee893b0d` |
| `shots/52-财务与结算.jpg` | 38579 | `b4a4f71e26af1b0a` |
| `shots/41-6-详情页-reLaunch-重进后仍在.jpg` | 29866 | `d3083212ef433998` |

### Git 之外的大件（附引用方式）

| 项 | 路径 | 说明 |
| --- | --- | --- |
| 演示录屏 | `E:\_diag\demo-13steps-run3.mp4` | **126.9 MB / 1843.5 s / 2 fps**（碎片化 mp4 ⇒ 中途被杀也可播）。⛔ 因体积未入库，需要时按该路径取。 |

### ⛔ 未覆盖 / 未定档（如实登记，⛔ 不得当通过）

* **第 9 步的"人工接管"**、**第 12 步**：不在本轮链上。接管那一半已由走查**第 55 章**单独补
  （`--section 55` ⇒ `PASS=9 / FAIL=0`）；第 12 步仍以 **E-B** 的定向运行为证据。
* **两条 `FAIL` 已定档**（2026-09-20 晨，见 **§3.1**）：㊻⑤（任务行服务端 0）、㊿⑥（变更后运力确认仍有效）
  都是**链式载体的状态后果** —— 换成"有任务行 / 不允许拆批"的载体后，全量轮两条都 **`PASS`**。
* **只有章 43 与章 44 的读数直接印出锚定委托 id**；其余十章靠机制保证，未逐章直证。

## 3. E-2026-09-19-C — 自动化与静态门禁（可复跑）

| 项 | 值 |
| --- | --- |
| **档** | **已自动化验证** |
| **本机门禁** | `python -X utf8 scripts/verify_local_gates.py` ⇒ `{"total":16,"failed":0}`；后端 pytest **1015** 项（通过 998 / 跳过 17 / 失败 0 / 错误 0） |
| **CI** | PR `#176`：6/6 job **SUCCESS**（后端 lint+test / 迁移 SQLite / 迁移 MySQL / 前端静态契约 / 前端端到端 / 并发集成 MySQL） |
| **命令与环境** | 见 `DEMO-1-runbook.md`；CI 定义以 `.github/workflows/ci.yml` 为权威 |

---

## 3.1 E-2026-09-20-B — 全量 `--section all` 回归 ＋ 7 条 `FAIL` 的最小复现（设备侧）

| 项 | 值 |
| --- | --- |
| **档** | **已设备运行**（⚠️ 全量轮 `FAIL=7` ⇒ ⛔ 不得记为通过） |
| **全量轮** | `--skip-ide --pay --section all` ＋ canonical/contract_flow/completion_ready ⇒ 04:38:16–05:42:54，**`FAIL=7 / NOT_RUN=10 / LIMITATION=17`**，42 张 |
| **复现 A** | `--section 15,7,43,51`（与全量轮同配置）⇒ **`FAIL=5 / NOT_RUN=2 / LIMITATION=1`** |
| **复现 B** | `--section 51,15,7`（**反序**）⇒ **`FAIL=2`**（只有 ⑮×2） |
| **本轮收口** | ㊻ ⑤ **`PASS`**（任务行 13／前置行 13）、㊿ ⑥ **`PASS`**（`still_valid=False` 只有 capacity 不过、候选 900 vs 需求 800）⇒ §10.2.3 那两条链上读数确为**载体后果** |
| **定档** | ⑮×2＝**走查脚本缺前置**（产品行为正确）；⑦＝**长会话读数**（短轮两次 `PASS`）；㊸×2/㊹＝**未定档**；51②＝**编排/会话复用依赖**（B 轮 `PASS`） |
| **归档** | `docs/entrust/evidence/2026-09-20-full-regression/` · 详述见 `DEMO-1-readiness.md` §10.2.4 |

| 文件 | 字节 | sha256[:16] |
| --- | --- | --- |
| `summary-full.json` | 201538 | `9343782fb27b6e8c` |
| `walk-all-stdout.txt` | 150424 | `a783fb3a7e9f1f1b` |
| `walk-a-stdout.txt` | 11660 | `8a30ed3278e3ea4a` |
| `walk-b-stdout.txt` | 8779 | `d3993d47e1866e6d` |
| `shots/all-15-委托发货-受理屏.jpg` | 31755 | `a20f10ffb6a5526b` |
| `shots/all-43-6-附件未上传.jpg` | 25262 | `2f9dfe4d403f9ddb` |
| `shots/all-㊻-运输计划卡.jpg` | 30877 | `e820a156ceee65ed` |
| `shots/a-15-委托发货-受理屏.jpg` | 14527 | `15bd3dd433cee715` |
| `shots/a-43-6-附件未上传.jpg` | 25099 | `9e18e9acecdb0317` |
| `shots/b-51-1-模型来源成果页.jpg` | 27131 | `a4270bb6b65f0d1c` |
| `shots/b-51-3-同版本发布成功.jpg` | 28007 | `40cf9521926a711e` |

⚠️ **本地未入库的部分**（体积，按路径取）：三轮的完整截图目录
`miniapp-device-artifacts/walk-20260920-043817`（151 张）、`…-055333`（16 张）、`…-060325`（13 张）；
两份**废弃**的并发轮 `_diag/run6b.log`、`run7.log` 只作"别并发"的教训留存，⛔ **不是证据**。

⛔ **同一条 runner 不能并发**：两个走查 runner 都监听 8000 ⇒ 会互相接到对方的临时库，
读数会长出 `拿不到 seed-owner 的 token` 这种"看起来像产品坏了"的假象（本轮实测废掉两轮）。

---

## 3.2 E-2026-09-20-C — 四处整改的**验证轮**（设备侧）

| 项 | 值 |
| --- | --- |
| **档** | **已设备运行** |
| **C 轮** | `--section 15,43,51` ⇒ **`FAIL=3 / NOT_RUN=3 / LIMITATION=1`**（⑮ **19 条全 PASS**；㊸ 定档为"处理器未执行"，仍 `FAIL`） |
| **D 轮** | `--section 51 --llm-mock`（fixture）⇒ **`RESULT: PASS（全为 PASS）`** —— 51 章首次全绿 |
| **对照正确性** | 真模型轮（同节次、未加 `--llm-mock`）51 ② 记 `NOT_RUN`（`mocked=False` ⇒ 提案由模型决定），本轮的 `PASS` **只证链路规则**，⛔ 不可外推为"模型质量已验证" |
| **改了什么** | 见 `DEMO-1-readiness.md` **§10.2.5**：⑮ 补前置 ＋ 到达判据；㊸ 补第五格 ＋ 受控重试；51 按 `mocked` 三态分断；runner 加 8000 互斥闸门 ＋ `--llm-mock` |

| 文件 | 字节 | sha256[:16] |
| --- | --- | --- |
| `summary-c.json` | 15179 | `db297d0b5f47b9f9` |
| `summary-d.json` | 8114 | `ec5681697d8cc58f` |
| `walk-c-stdout.txt` | 12911 | `af72f5e5db0b9090` |
| `walk-d-stdout.txt` | 5438 | `938bdbd486d9979b` |
| `shots/15-委托发货前-源页表单已填.jpg` | 31243 | `ccc58e48e1006515` |
| `shots/c-15-受理屏-带过货名与货量.jpg` | 20074 | `0cba1f3d8648aa53` |
| `shots/c-43-6-点击后仍无附件.jpg` | 24623 | `bc0bd6333a4849c9` |
| `shots/d-51-3-fixture下同版本发布成功.jpg` | 26503 | `089414a2058fef5d` |

⛔ **两条边界**：① ㊸ **未修**（只从"未定档"推进到"已定档"）；② 51 的 `PASS` 是 fixture 模式结论，
真模型下同一格记 `NOT_RUN` —— 两者**不得混用**。

---

## 3.3 E-2026-09-20-D — ㊸ 第 2 步的**四轮复跑**（设备侧）

| 项 | 值 |
| --- | --- |
| **档** | **已设备运行**（⚠️ 整章最优读数是 `NOT_RUN（LIMITATION=1）` —— 唯一非 PASS 的是**工具边界**那一格） |
| **E 轮** | `--section 43` ⇒ `PASS ｜ ㊸` **29** 条；`FAIL=1` ⇒ ⚠️ 那 1 条是**我写错的读数**（把"`rects()` 读不到几何"记成"不在视口内"），已修 |
| **F 轮** | `--section 43`（读数改三态）⇒ `29` 条 PASS，`NOT_RUN=1 / LIMITATION=1`，**零 FAIL** |
| **G 轮** | `--section 15,43`（**43 不是第 1 章**）⇒ `29` 条 PASS，**零 FAIL** ⇒ **推翻"位置依赖"假说** |
| **H 轮** | `--section 43`（摘掉已证伪的断言）⇒ `29` 条 PASS，**`NOT_RUN（LIMITATION=1）`** |
| **链上关键读数（H）** | `清残留=True`／样本文件 `先=False 后=True(2674字节)`／`attachments=1 done=1`／`handler_ran=True`／`notice='已上传…并提取 1228 字'` |
| **新假说（⛔ 未定档）** | 失败只出现在"保活 3 小时的老实例"轮次（全量 04:38／run8 A 05:53／C 06:48）；E–H 用的是 07:37 **新起实例** ⇒ 疑与 **IDE 会话年龄/复用轮数**相关。下一步：**同一实例：先全量、再单跑 43** |
| **口径** | 产品代码**一行未改**（只改走查读数）⇒ ⛔ 不得写成"已修复"；详述见 `DEMO-1-readiness.md` **§10.2.6** |

| 文件 | 字节 | sha256[:16] |
| --- | --- | --- |
| `summary-074003.json` | 14251 | `cd643c52bddaac3c` |
| `summary-074639.json` | 14617 | `258a1f77cdc871cb` |
| `summary-075158.json` | 17923 | `09c56f35e6e1d314` |
| `summary-075901.json` | 14614 | `6a9ef4ee53fb975a` |
| `walk-e-stdout.txt` | 10598 | `38ded073fdeec1b1` |
| `walk-f-stdout.txt` | 10736 | `6c4f9c82dd8b55d4` |
| `walk-g-stdout.txt` | 13194 | `ccc1a179bf799946` |
| `walk-h-stdout.txt` | 10224 | `d6ff5f344f8463cf` |
| `shots/h-43-6-附件已上传并提取.jpg` | 27213 | `6e3e5a6541944def` |
| `shots/h-43-12-详情页-成果槽位.jpg` | 24375 | `3b8b2a6c0df432e0` |

---

## 3.4 E-2026-09-20-E — 主链**第 4 步**的业务写侧（`chain4`，设备侧）

| 项 | 值 |
| --- | --- |
| **档** | **已设备运行**（`RESULT: PASS（全为 PASS）`） |
| **命令** | `--skip-ide --pay --section chain4 --extra-seeds seed_entrust_canonical.py` |
| **做了什么** | 在**同一张委托**上：① 经**界面**建出 **公路（厂区→南宁港）→ 内河（南宁港→贵港港）→ 公路（贵港港→卸货地）**；② 经**接口**建 3 条任务（`execution/execution/handover`）并设**前置链**；③ 回工作台**逐行**展示 |
| **关键读数** | 段 `3→6`、**末三段方式 `['road','water','road']`**、渲染段行=6；任务 `task_id=['8','9','10']`、**前置链 `8 → 9 → 10`**；`服务端任务=3 / 渲染任务行=3 / 前置行=3`；**带前置的任务=2 条** |
| **两个被读数抓出来的坑** | ① `POST /entrust/tasks` ⇒ **405**（路由不存在；权威路径是 `POST /entrust/assignments/{id}/tasks`）；② `task_type` 是 **R1 固定取值域**（自造 `pickup` 被 **400** 拒，服务端把取值域原样列了出来）；③ 「服务端任务数」**不能读 plan 载荷**（它不回 `tasks` ⇒ 恒 0，看起来像页面多渲染） |
| **口径** | 段**经界面**；任务与前置**经接口**（⚠️ 界面无建任务入口、无 `precondition_task_id` 入参 —— 如实登记，⛔ 不假装是界面点的） |

| 文件 | 字节 | sha256[:16] |
| --- | --- | --- |
| `summary-m.json` | 6203 | `d83823014de14299` |
| `walk-m-stdout.txt` | 3793 | `46571016020b6ac0` |
| `chain4-工作台-公路水路公路与任务前置.jpg` | 27502 | `9c155f5a907869d6` |

⛔ **这只是第 4 步**：第 5／7／9／12／13 步"沿用本单"**尚未串起来**（见 §4 第 3b 条）。

---

## 3.5 E-2026-09-20-F — 链式**第 1–7 步同单**（设备侧，2026-09-20 晨）

| 项 | 值 |
| --- | --- |
| **档** | **已设备运行**（`RESULT: PASS`：`PASS=132 / FAIL=0 / NOT_RUN=6`，其中 **1 条是工具限制**） |
| **命令** | `--skip-ide --pay --chain --section 43,chain4,45,44,49 --extra-seeds seed_entrust_canonical.py,seed_entrust_contract_flow.py` |
| **做了什么** | 在**同一张新委托**（`aid=6`）上按业务顺序走：第 1–3 步（客户提交 → 受理 → 样本 → AG-02 → 采纳 → 更正）→ 第 4 步（建 `road/water/road` ＋ 任务前置）→ **第 5 步（登记 900 吨、不允许拆批候选并确认）** → 第 6 步（经界面组装 `customer_quote` → 发布 → 客户接受精确版本）→ **第 7 步（从本单已接受发布派生合同 ＋ 记签署证据）** |
| **⭐ 同一张的直证** | 取单追踪：`sec_43=6 / sec_44=6 / sec_45=6 / sec_49=6 / sec_chain4=6` ⇒ **`合计 5 个取单点 · aid 集合=['6'] · 同一张=是`** |
| **第 5 步关键读数** | `㊺ ⓪ 本单 quantity='800' 吨；装载口径=不允许拆批`；页面 `吨位='900' 拆批=False(期望 False)`；服务端 `candidate_id=4 容量='900.000' 拆批=False`；`㊺ ②d … 本单需求='800'吨 → 容量够`；确认 `confirmation_id=1 artifact_id=9 rules=4 ruleset='v1'` |
| **第 7 步关键读数** | `㊾ ③ 页面='11' 服务端='11' 报价版本=1`；来源表 `页面行数=18 服务端=18`；证据行「第 1 版 · 样件扫描件 · 样件标注」；常驻声明渲染出来 |
| **剩余 `NOT_RUN`（如实登记，⛔ 不当通过）** | ① `㊸` 系统文件选择器（**工具限制**：OS 原生弹层不在渲染树里）；② `㊹` 组装**前**的"本单无客户可见成果"（这是 §10.1 之外那一步的**前置状态**，随后被 〇 段补上）；③–⑤ `㊹` 的"来源门槛被拒 ⇒ 逐条核验"剧本 —— 本轮载体是**人工组装**（`manual`）⇒ `gate.ok=True`、`pending=0`，**该剧本没有可跑的对象**（它的对象见 §3.2 的章 51 fixture 轮）；⑥ `㊹` 授权下载清单（本单载体**没有附件** ⇒ `[] == []` 会**真空通过**） |
| **前提落地状态** | `DEMO-1-walkthrough.md` **§2.1**：HO 固定的 800 吨 / 900 吨无拆批 / 第 4 步先建再展 / 第 6–7 步在本单，**逐条有设备读数** |

| 文件 | 字节 | sha256[:16] |
| --- | --- | --- |
| `summary-n.json` | 42746 | `d5c6ae5ec549489e` |
| `walk-n-stdout.txt` | 36617 | `8ec7271971e39dee` |
| `shots/n-44-0-本单组装对客报价.jpg` | 35203 | `a4053d45b6a79a51` |
| `shots/n-44-7-本单客户已接受.jpg` | 35190 | `b8dca6ac5ce5074f` |
| `shots/n-45-2-不允许拆批-900吨.jpg` | 35214 | `b24a53b8366d5def` |
| `shots/n-45-3-候选已登记.jpg` | 36825 | `35956dde56a58664` |
| `shots/n-45-5-确认成功.jpg` | 35211 | `440e266408a81485` |
| `shots/n-49-本单派生合同与签署证据.jpg` | 35187 | `407def8eb3a9823e` |
| `shots/n-chain4-公路水路公路与任务前置.jpg` | 35247 | `1d10a4301f92e9a5` |

---

## 3.6 E-2026-09-20-G — 链式**第 1–9 步同单**（设备侧，2026-09-20 午；含新章 `chain9`）

| 项 | 值 |
| --- | --- |
| **档** | **已设备运行**（权威轮 **R**：`RESULT: NOT_RUN` —— **零 `FAIL`**；`NOT_RUN=5 / LIMITATION=1`，5 条中 4 条是"前置不成立/剧本无对象"） |
| **命令** | `--skip-ide --pay --chain --section 43,chain4,45,44,49,50,chain9,55 --extra-seeds seed_entrust_canonical.py,seed_entrust_contract_flow.py` |
| **同一张的直证** | 取单追踪：`sec_43/44/45/49/50/55/chain4/chain9` **全部 `aid=6`** ⇒ `合计 8 个取单点 · aid 集合=['6'] · 同一张=是` |
| **第 5 步** | `㊺ ②b 吨位='900' 拆批=False(期望 False)`；`㊺ ②c candidate_id=4 容量='900.000' 拆批=False`；`㊺ ②d 本单需求='800'吨 → 容量够`；`④a confirmation_id=1 artifact_id=9 rules=4` |
| **第 6–7 步** | `㊹ 〇` 经界面组装 `customer_quote`（`artifact#10`）→ 发布 → 客户本人接受；`㊾ ③ 页面='11' 服务端='11' 报价版本=1`、来源表 `18=18` |
| **第 8 步** | `㊿ ⑤c 页面='800.000 吨 → 950.000 吨' 服务端=('800.000','950.000')`；`⑥ still_valid=False 不过的规则=['capacity'] 允许拆批=False` |
| **第 9 步（新章）** | ①`still_valid=False 不过=['capacity'] changed_fields=['demand_tonnes']`；②替换候选 `1000.000/不拆批 ⇒ still_valid=True`；③`artifact#13`（**新成果**）→ `release#3` → 客户 `decision='accept'` 绑 **release 3 / v1**；③i **旧 `release#2` 仍 `released` ＋ 响应仍在** |
| **第 9 步的两条负例** | ⓐ `POST /offer-releases/2/withdraw` ⇒ **409**，`detail='发布 2 已被客户响应，不能撤回或取代（已接受的版本永久保留，后续变更要新的客户确认）'`（⇒ ∴「重新接受」必须换**新成果**）；ⓑ 经理替客户响应 ⇒ **403** |
| **A2 接管（同单）** | `55 ③ lease_generation 0→1 assignee None→2`；`④ 旧代次提交 ⇒ 409`，`detail='执行代次已过期：任务当前代次 1，提交代次 0（人工已接管或任务被改派）'`；`⑤ 详情页接管锚点命中=0`（与 O-1b 一致） |
| **两条真实教训** | ① 首轮把负例请求体写成 `note` ⇒ **422**（`OfferWithdrawIn.reason` 必填）—— "入参不合法"被读成"服务端拒绝"；② **给已被接受的那份成果加版再发布 ⇒ 409** ⇒ 按服务端规则把剧本从"加一版"改成"换新成果"（判据先于剧本） |
| **⚠️ 本轮已知证据弱点** | R 轮的两张客户侧截图（`r-chain9-4-*` 与 `r-chain9-5-*`）**逐字节相同** ⇒ 只作"页面已就绪"用，⛔ **不当作"已接受后的界面态"**；接受事实以 **API 读数**为准（`decision='accept'` 绑 release 3）。已在脚本里加"静置再截图"（下一轮生效） |
| **⛔ 作废轮 Q** | 11:29 起跑、36 分钟，撞上**保活到期连带杀 IDE**（12:00）⇒ 后段读数（`页面数据键 0 个`、`未停在身份选择页（）`）是环境死，**整轮作废**。⚠️ 它同时是同一实例的**第 4 轮**，早期章节已开始退化 ⇒ 记入"实例复用轮数"假说 |

| 文件 | 字节 | sha256[:16] |
| --- | --- | --- |
| `summary-o.json` | 53882 | `5c678dbabee29754` |
| `summary-p.json` | 59433 | `886a58961a860e28` |
| `summary-r.json` | 60412 | `167d7574db56e15f` |
| `walk-o-stdout.txt` | 45297 | `2ad93a524cd9ddaf` |
| `walk-p-stdout.txt` | 49604 | `357547d0e81023bd` |
| `walk-r-stdout.txt` | 49209 | `769e897032aea29c` |
| `shots/r-chain9-1-旧确认复核-不再适用.jpg` | 23770 | `f7c6e45710b47bf8` |
| `shots/r-chain9-2-替换候选已确认.jpg` | 35181 | `ac9332f72116e8a6` |
| `shots/r-chain9-3-新成果页.jpg` | 26406 | `0d20cfbd62080a6b` |
| `shots/r-chain9-4-客户侧-新报价.jpg` | 35180 | `b822f1531d3bb3a5` |
| `shots/r-chain9-5-客户已重新接受.jpg` | 35180 | `b822f1531d3bb3a5`（⚠️ 与上一张相同，见上表"已知弱点"） |
| `shots/r-55-1-委托详情-无接管入口.jpg` | 35208 | `88cba80dcae5f002` |
| `shots/chain9-1-旧确认复核-不再适用.jpg`（轮 O） | 23799 | `b06edb5053ff1508` |
| `shots/chain9-2-替换候选已确认.jpg`（轮 P） | 35177 | `c978b609c6158613` |
| `shots/chain9-3-新成果页.jpg`（轮 P） | 26434 | `614c25a15c9cd7f7` |
| `shots/chain9-4-客户侧-新报价.jpg`（轮 P） | 35188 | `96699806900e1b81` |
| `shots/chain9-5-客户已重新接受.jpg`（轮 P） | 35169 | `1ca6854935a0fec3` |

---

## 3.7 E-2026-09-20-H — 第 10–13 步的收尾章节（`chain11`／`chain12`，设备侧；含两轮作废）

| 项 | 值 |
| --- | --- |
| **档** | **部分**（权威轮 **V**：`FAIL=10 / NOT_RUN=9 / LIMITATION=1`；⛔ **不是**全绿，逐条见 `readiness` §10.2.8） |
| **命令** | `--skip-ide --pay --chain --section 43,chain4,45,44,49,50,chain9,55,52,chain11,53,chain12 --extra-seeds canonical,contract_flow,completion_ready` |
| **同一张** | **12 章全部 `aid=7`** ⇒ `合计 12 个取单点 · aid 集合=['7'] · 同一张=是` |
| **⭐ 任务侧（第 10–11 步）** | `chain11 ②b`：**10 条任务全部处置成功**（`takeover`→`start`→`complete`）；本单缺项 **3 → 1**（只剩 `cases_not_closed`） |
| **⭐ D1-15** | ①a 服务端 `800.000→950.000`｜②a 详情页 `quantity='950'`｜②c **结算面** `数量 950.000 吨`（按 `chargeId` 命中）｜③a/③c 重进后逐字相同 |
| **仍未闭合** | ①b 页面 `quantityHistory` 读到 0 行（记 `NOT_RUN`）；`chain11 ③b` 处置取值域写错（**已修未复验**）；`chain12 ④` 维度按"费用"匹配（**已修未复验**） |
| **⛔ 作废轮** | **W**（`FAIL=13 / NOT_RUN=13`；多处「未停在身份选择页（）」、路径空串）—— **上一轮保活到期把 IDE 带走**（与轮 Q 同一故障）；⇒ 环境纪律：多个保活附着同一实例时，**任何一个到期都会拉走它** |

| 文件 | 字节 | sha256[:16] |
| --- | --- | --- |
| `summary-v.json` | 74005 | `deacca4388359e43` |
| `walk-v-stdout.txt` | 63177 | `01240a23ee1887ee` |
| `shots/v-52-财务与结算.jpg` | 37209 | `d991f9eaf65d97de` |
| `shots/v-53-结案五维清单.jpg` | 35197 | `ad96bce6b19d0b36` |
| `shots/v-chain12-2-结算-数量950吨.jpg` | 30479 | `77a10b1cca46cedd` |
| `shots/v-chain12-3-重进后-结算行仍在.jpg` | 30462 | `2db99b6e0cdb99a5`（⚠️ 与上一张不同 ⇒ 截图差异问题已解决） |

---

## 3.8 E-2026-09-20-I — 保活**闸门**的注入验证 ＋ 第 1–13 步干净复验（设备侧；含一条根因连锁）

**目录**：`docs/entrust/evidence/2026-09-20-keepalive-gate/`（闸门注入验证）＋
`docs/entrust/evidence/2026-09-20-chain-1-13-clean/`（轮 X／X2／X3）。

### 3.8.1 闸门注入验证（HO 建议 5 ＋ §10.2.9）
| 文件 | 内容 | 字节 | sha256[:16] |
| --- | --- | --- | --- |
| `inject-a-second-prepare-ide.txt` | 已有持有者时再起 `--prepare-ide` ⇒ `rc=2`、点名 `pid=10940 还剩 10686s`、**IDE 实例数 21 → 21** | 1347 | `c73ce8a01a0e61b3` |
| `inject-b-min-keepalive-left.txt` | `--min-keepalive-left 99999` ⇒ `rc=2`「剩余 10683s < 要求的 99999s」 | 1168 | `5444995706303a86` |
| `keepalive-claim.json` | 认领记录（pid/到期时刻/章节） | 149 | `ec40c2c2e498e4f0` |
| `keeper-release-hold14.log` | ⭐ **释放路径的真跑读数**（HO 0919「同意顺手归档」）：`--keepalive 10800` 的持有者跑满 **3 小时 02 分**后**正常退出**（17:15:46 → 20:18:23）⇒ 日志 `已撤销保活认领记录：是`、`_ide_keepalive.json` **已不存在**、IDE 进程 **22 → 0**。⇒ 闸门**不会**把后续轮次永久锁死；⚠️ 同时说明**一个 keeper 只覆盖一个保活窗口** | 2180 | `bdd70c46a15536e0` |

### 3.8.2 主链第 1–13 步（12 章同一张单）
**每轮取单追踪均显示 12/12 全为同一 `aid`**（轮 X/X2/X3 载体为 `aid=7`）。

| 轮 | 命令要点 | `RESULT` | 关键读数 |
| --- | --- | --- | --- |
| **X** | `--chain`，干净实例，`--min-keepalive-left 3600` | `FAIL=7 / NOT_RUN=7 / LIMITATION=1` | 关案件 `409 expected_revision=1 已过期`（**一条根因**）；`chain11 ②b` 10 条任务逐条处置**全成功** |
| **X2** | 修字段名（`revisionNo`）＋ chain12 ④ 判据方向 | 同上 | ⭐ **`①b`/`③d` 双双 PASS**：`changeText='800.000 吨 → 950.000 吨'`、`source='来源变更案件 #3'`；③b 仍 409（详情的 version 在**包裹层**里，未取到） |
| **X3** | 按 `detail.case.revision_no` 取乐观锁 | `FAIL=9 / NOT_RUN=6 / LIMITATION=1` | ⭐ **第 10–13 步全链跑通**：`③b` → `已关 ['3(尝试1次)']；version 取自 ['3:detail.case.revision_no']`；`④ ready=True 缺 []`；`53 ⑧-b status='completed' completedAt='2026-09-20 11:15:26'`；`⑨-b/⑨-c/⑨-d/⑨-e` 全 PASS（重开：理由必填、`completed_at` 清空、入口复现、无「撤回」入口且**带正控**）。⚠️ 同时暴露 **9 条 FAIL**，成因见下 |

**X3 的 9 条 `FAIL` 逐条定档**（⛔ 不挑绿灯；**分布 = 上传 3 ／ 货量审批单位输入框 1 ／ 53 章起点与结案顺序 4 ／ `chain12` 异常 1**）。
⚠️ **2026-09-20 HO 抽查指出：本表原缺「单位输入框」那一条（表里只有 8 条）** ⇒ 已补第 1 行。

| 条目 | 定档 |
| --- | --- |
| `㊿ ④a`（三只货量输入框，报 `新值=1 **单位=0** 依据=1`） | ⭐ **两条通道结论相反 ⇒ 工具侧「计数假 0」**（2026-09-20 补查）：同一次运行里 `④b` 用 `input_text('[data-df="qty-unit"]', …)` **写成功了**（`quantityForm.unit` 初值 `''` ⇒ `'吨'`，`case.js:117`）⇒ 元素**存在且 `bindinput` 执行过**，`count()` 那个 0 不可信。⇒ 判据改为**两通道并排 ＋ 正控**（计数为 0 时用写入通道复核；正控数不动 ⇒ `ENV_BLOCKED` 而不是记假红）。⛔ 不写成产品缺陷，也不写成"这一格通过" |
| ㊸ ×3 FAIL ＋ ㊸ ×1 NOT_RUN | **疑似实例／自动化通道问题**（⛔ 措辞按 HO 裁定收窄）：样本文件探针 `err='Error: accessSy…'`、`uploading=False/False`。⚠️ 它只直接说明**样本文件不存在**，**不能单独证明 IDE 退化**、也不能证明处理器一定没进入 ⇒ 保留"疑似"，**待独立探测**（实例退出／通道失效）。⛔ 不据"这是第 3 轮"把 `FAIL` 改判 `LIMITATION` |
| `53 ①` ／ `⑥` ／ `⑦` | **章节的起点假设过期**：它们假设"本章起点一定不齐备"（⑥⑦ 要演示"被拦"）。链式轮次里 **`chain11` 先把同一张单收尾成齐备** ⇒ ⑥ 的点击**真的结案成功**，⑦ 于是读到 `completed`、⑧ 的"齐备⇒结案成功"**失去对象**（⑧-a 读到 `closure` 空、`ready=None`）。⇒ 已把这三条改成**状态感知**（齐备时 `NOT_RUN` 并点名，且**不**在该处点提交）；负例改由 **`chain11 ①b`（清理之前**先点结案、证"被拦"）承担 |
| `53 ⑧-a` | 同上（被 ⑥ 提前结掉）⇒ 改后应恢复 |
| `章节 chain12 执行异常 IndexError` | **我的判据里的裸下标**：②c 的读数里写 `mine[0]`，命中 0 行时整章崩溃且**一条读数都不留**。⇒ 已修（改为先取 `first`），并给章节兜底**加 traceback 现场** |

**文件**（逐件 sha256）
| 文件 | 字节 | sha256[:16] |
| --- | --- | --- |
| `walk-x-stdout.txt` | 66219 | `7dc2fd526c8dece9` |
| `walk-x2-stdout.txt` | 67334 | `afffd59d63ca7abc` |
| `walk-x3-stdout.txt` | 60652 | `ebb725568905f5b6` |
| `summary-x.json`／`summary-x2.json`／`summary-x3.json` | 81166／82166／74887 | `e532b9286d35ce6b`／`2a28ae7ea05fc631`／`58b676361922323f` |
| `shots/x2-chain12-1-案件页-变更前后记录.jpg` | 23612 | `9df7e1c8b9fdb8a7` |
| `shots/x2-chain12-2-结算-数量950吨.jpg` | 36966 | `0ae9e0866b1f2abe` |
| `shots/x2-chain12-3-重进后-结算行仍在.jpg` | 36952 | `e3f009df2fa83aba` |
| `shots/x2-53-结案五维清单.jpg` | 35162 | `7fefd6379a6711ca` |
| `shots/x3-53-结案五维清单.jpg` | 35198 | `21ae8a25cd99a55e` |
| `shots/x3-53-结案后（留痕）.jpg` | 33970 | `b3e1f7fd364735c9` |
| `shots/x3-53-重开-理由必填.jpg` | 34007 | `eec88f10e1804b3f` |

⭐ **本轮最重要的一条**：`chain11 ③b` 的 409 是**一条根因、6 条下游**
（`chain11 ④`／`53 ⑧-a`／`⑧-b`／`⑧-d`／`⑨-a`／`第13步 ④`），
⛔ 报告里不许写成 6 个独立缺陷；根因是**乐观锁取错层级**（`row.revision` → 真实位置
`GET /entrust/exceptions/{id}` 的 `case.revision_no`，见 `schemas.ExceptionCaseDetailOut`）。
另有**一条我自己的判据错**：结案五维的 `count` 数的是**缺项条数**，我写成 `count >= 1`
（＝要求"这里有缺项"），**方向反了**；以及**一条裸下标**把整章打挂（②c 的 `mine[0]`）。

---

## 4. 待补（本索引自己的缺口，如实登记）

| # | 缺口 | 处置 |
| --- | --- | --- |
| 1 | **E-B 的两份原始产物未入库** | 下一轮随主链 PR 一并入库（或按附条件引用） |
| 2 | ~~**本轮的"全量"不含 53/54**~~ | ✅ **已做**：2026-09-20 晨跑了真正的全量（含 53/54/55），读数 `FAIL=7 / NOT_RUN=10 / LIMITATION=17`（见 **§3.1**）。⚠️ 它是"全量覆盖"，**不是**"全绿" |
| 3 | ~~**合同 §12 另有交付物尚未建立**~~ ⇒ **2026-09-20 八份齐** | 本轮建立 **`DEMO-1-walkthrough.md`**（主脚本/角色/预期结果/诚实回退 ＋ 业务顺序三条要求）与 **`DEMO-1-acceptance.md`**（当前唯一验收汇总，**达 PASS 12／未达 6**，逐行按合同 §9 的证据要求重算）。⇒ §12 八份**全部到位**；⚠️ 其中 `acceptance` 的 **HO 验收记录仍留白**（D1-18 因此仍是 `部分`） |
| 3b | ~~**主链的「业务顺序」尚未落实**~~ ⇒ **2026-09-20 已落实（步 1–7）** | 见 **§3.5**：五个章节落在**同一张 `aid=6`**、按业务序推进，且**没有** `air`/铁路段（第 4 步末三段方式 `['road','water','road']`）。⚠️ **步 8–9／12–13 仍在推进**（章 `50` ＋ 新章 `chain9` ＋ 结案/重开） |
| 4 | **同一张新委托的 13 步主链** | ⭐ **2026-09-20：第 1–9 步已在同一张新委托上跑通**（见 **§3.5 ＋ §3.6**：八章同一 `aid=6`，命令见 `runbook` §8.9）—— 含第 5 步的 900 吨/不拆批候选、第 6–7 步在本单组装→发布→客户接受→派生、第 8 步 `800→950`、第 9 步复核＋重新确认＋**换新成果重新接受**＋A2 接管。**仍未覆盖：第 10–13 步在本单上的动作**（费用/结算已有章 52 证据、结案/重开已有章 53） |
| 5.5 | **证据文件的行尾归一化会毁掉可核对性** | 已修：`docs/entrust/evidence/.gitattributes` 加 `* -text`（该目录逐字节入库）；⛔ 新证据入库前要**复核「工作区哈希 == 索引 blob 哈希」** |
| 5 | **密钥文件曾被本机删除故障永久删除** | 2026-09-19 核实发现 `backend/.env.local`（含**可用订阅 Key**）缺失、**回收站无记录**；已从 `_b_recon3.txt`（09-16 的 env 基线快照）找回并还原，`llm_key_doctor.py` **四步全绿**。⚠️ **untracked 文件不在护栏的恢复范围内** ⇒ 已扩展护栏（见 DR-0019 追加节） |
