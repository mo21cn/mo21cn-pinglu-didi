# DEMO-1 前置就绪台账（收尾项 / 阻塞项）

| 项 | 值 |
| --- | --- |
| 核实日期 | 2026-09-16 |
| 核实基线 | `develop` = `b808d98`（PR #111 / #112 已合并） |
| **收尾日期** | **2026-09-16**（HO 授权"下一步建议 1–5 全部执行"） |
| **收尾后基线** | `develop` = **`60a94b3`**（PR #113 已 squash 合并） |
| 核实用途 | 回答"进入 DEMO-1 之前还有什么收尾项、阻塞项必须先解决" |
| 口径 | 只写**核实过**的状态，每条带依据（文件行 / CI 运行号 / 接口返回）；**未执行 ≠ 延期 ≠ 通过** |

## §0 结论（先看这里）

**没有硬阻塞。** DEMO-1 的 S1 具备开工条件。

- 上一版台账（`HO决策请求-阻塞项与待定调清单.md`，2026-09-15 @ `a2525e2`）共 **12 项遗留**（H1–H9、D1–D3），
  经逐条复核：**9 项已闭合**（H1 / H2 / H3 / H4 / H7 / D1 / D2 / D3，以及 **H8** —— 原 3 处确认已改、
  本轮又发现 3 处同类陈旧并一并订正，见 §4 U-1～U-3 与 §7 PR-2），
  **3 项仍未闭合**（**H5 / H6 / H9**）—— 三项**都不阻塞 S1**（详见 §2、§5）。
- 同台账的**预测清单 4 项**（P2 / P3 / P5 / P7）单独计：**3 项本轮已办**（P2 / P5 / P7），
  **1 项未办**（P3，需外部资源）。⇒ 合计 **16 项中 12 项闭合、4 项未闭合**。
- HO 于 2026-09-15 给出的三条裁决（**D1=B / D2=A / D3 本轮不扩展**）**已全部落地**，
  不是"待办"（依据见 §3）。
- HO 点名的**三处影响决策的证据口径**，两处已修、一处本就成立；另**自纠一处我自己的误判**（§3.4）。
- 真正需要"前置解决"的是 **7 件小事**（§7 PR-1～PR-7）。**截至 2026-09-16 收尾：7 件全部办结**
  （PR-1～PR-5 随 PR #113；PR-6 授权后已执行；**PR-7 落在 PR #114**）。详见 §7 与 §9。

## §1 为什么这份台账必须重做一遍，而不是照抄 2026-09-15 那份

那份台账的结论**当时属实、之后被新结果覆盖**。举三条最容易读错的：

| 编号 | 2026-09-15 的说法 | 今天的事实 |
| --- | --- | --- |
| H1 | "口径已裁决，**未写一行实现**" | **已实现并已采纳**（`role_snapshot` 改名 + 留痕语义 + 测试） |
| H2 | "只有声明，**没有运行期实测**" | **已实测闭环**（走查第 ㉝ 章，实测最深链 4 层、平台硬限 10 层） |
| H7② | "MySQL 上**作业并发**未在 CI 真实执行" | **已由 CI 真实 MySQL 覆盖**（`pytest-mysql` job 11/11 通过，含三条 Agent 作业锚点） |

⇒ 直接引用旧台账会让人**去修一个已经修好的问题**，并把已完成的事重排一遍期。
本文件与旧台账并存：旧台账留作历史，**以本文件为当前口径**。

## §2 旧台账逐条复核（12 项 + 预测清单）

| 编号 | 旧说法 | **当前核实状态** | 依据 |
| --- | --- | --- | --- |
| **H1** DR-0017 方案 A | 已裁决、**未实现** | ✅ **已闭合** | `backend/app/core/security.py:5` 写明 `role_snapshot` 是"签发那一刻的留痕、**不参与任何判权**"；`:33/:40` payload 用新名；`backend/tests/test_auth.py:143-157` 有留痕断言；前端 `miniapp/utils/auth.js:221` 同名同步；`decisions/0017` 状态行 = **已采纳并实现（方案 A）** |
| **H2** 页面栈运行期验证 | 只有声明 | ✅ **已闭合** | 走查第 **㉝** 章（`--section 33`）。最近一轮 `walk-20260916-022603`：㉝ 共 25 条，**实测最深链 4 层**（与声明链深一致）、**平台硬限实测 = 第 10 层被拒**（与源码 `MAX_STACK=10` 对账一致）、预算 8 < 硬限 10；`decisions/0011` §6.2/§6.3 已回写 |
| **H3** 原生弹层 | 人工点击待执行 | ✅ **已闭合**（两类证据齐备） | 自动化：走查 ⑧b（`source=auto`）；人工：`H3-原生弹层确认键人工验证记录.md` 记录表**第 1 行（OS 级自动化输入）+ 第 2 行（人手，2026-09-16 05:02）**。⑧b 那条 `LIMITATION` 按纪律**保持原样** |
| **H4 / D2** IDE 不稳定 | 待 D2 结论 | ✅ **HO 裁 A，且已落地** | 处置为"探测 → 复用 → 有界恢复"，暂停专项追根因、不换工具链；恢复边界只回收 `OWNED_IDE_PIDS`、复用模式一个进程都不动；起 IDE 不再传沙箱代理 |
| **H5** 验证 20 / 10 归 S3 | `not-run`，R1 必需 | ⏳ **仍未执行**（现状未变，且**不得**记通过/延期/删） | `backend/app/modules/entrust/exceptions.py:448` 自述"S3 的结案校验（验证 20/10）仍未上线" |
| **H6** 结案命令 | 不存在 | ⏳ **仍不存在** | 同上。合同 §1.3 点名禁止"只查异常"的捷径实现 ⇒ 属 **S4 交付内容**，非开工前置 |
| **H7①** 真模型质量 | 无通道 | ✅ **通道已通**（H7a 已跑四轮） | `backend/.env.local` 配订阅 Key（不入库）；四轮结论 **3 项达标 / 1 项未达标**；未达标项 `calibration` 是**指标退化**（错答组 n=1），阈值未改、**不宣称通过** |
| **H7② / H7b** Agent 作业 MySQL 并发 | 未验证；列为 **R1 前必须闭合** | ✅ **已闭合** | `backend/tests/test_mysql_integration.py` 第 **581 / 637 / 668** 行三条锚点（双 worker 竞争 / 租约过期接管 / 迟到写入作废）；CI run `35024379769` 的 `pytest-mysql` job 日志：`collected 691 items / 680 deselected / 11 selected` + `tests/test_mysql_integration.py ...........`（**11 个点 = 11 项通过**），模块恰好 11 个用例（模块级 `pytestmark = [pytest.mark.mysql, …]`） |
| **H8** 文档陈旧 | 3 处 | ✅ **已闭合**：原 3 处确认已改；**本轮又查出 3 处同类陈旧并已一并订正** | 原 3 处（0009:95 / 04-A2:6 / 04-A2:171-174）确认已改；新 3 处见 §4「U-1～U-3」与 §7 **PR-2** |
| **H9** 合并授权 | 需授权窗口 | ⏳ **仍是约定**（本轮已按"集中清单"方式执行） | `delete_branch_on_merge=false`；`develop` **已有分支保护**且 `strict=true`（见 §4 U-4） |
| **D1** console 口径 | 待裁决 | ✅ **HO 裁 B，且已落地** | `scripts/verify_miniapp_devtools.py:4194-4201`：未豁免 error ⇒ `review_required()` ⇒ `NON_PASS` ⇒ `verdict≠PASS` ⇒ `rc=1`（`:4271-4272`）；白名单**绑定发生阶段**（`openPhaseWaived` / `bizPhaseUnwaived` 分桶）；重跑保留 `attempt` + `previousRuns`（首次结果不覆盖）|
| **D3** 旧域 TODO | 待裁决 | ✅ **HO 裁"本轮不扩展"** | TODO-01/03/04/07/08 留档于 `docs/甲方实操评审指南.md:367-374`；不实现、也不删登记 |
| **P2** 真模型 Key | 无通道 | ✅ 已解决（同 H7①） | —— |
| **P3** 真实登录 / 支付 | 需 AppID + 商户号 | ⏳ **本轮不联调**（HO 已裁），但需**拆成两项**登记 | HO 2026-09-15：微信登录**不应被商户号阻塞**；支付另列商户配置、回调与资金验收条件。⇒ 见 §6 E-1 |
| **P5** MySQL 由 CI 覆盖 | — | ✅ 成立，但**必须限定对象** | 覆盖范围 = 迁移 / 双认领 / 并发编辑 / 幂等 / 五类时间戳渲染 / **Agent 作业三条并发锚点**，共 11 项。**不得**据此宣称"所有 MySQL 行为都已验证" |
| **P7** 镜像表校验 | 需登记 | ✅ **本轮已办** | 规范已写入 `AGENTS.md` §3.5（HO 定为开发规范、直接执行），见 §4 U-5 与 §7 **PR-3** |

## §3 HO 点名的三处证据口径 —— 逐条给代码证据

HO 在《近期HO 决策：阻塞项与待定调清单决策列表》开头写明要"先纠正三处会影响决策的证据口径"。
这三条我逐条到代码里核，结论如下。

### 3.1 "⑧b 19/19 不能作为完整点击链路通过"（要求**调整统计**，不只是补一句说明）

**已修，而且修在结构上，不是修在措辞上。**

- 未开支付开关时，⑧b 走 `w.rep.not_run(...)`（`:2809`），**不再**走 `rec(..., True, ...)` ——
  源码里那条注释直接写明："⚠️ 且必须走 `not_run()` —— 曾写成 `rec(..., True, ...)`，
  汇总里与真通过**长得一模一样**（HO 2026-09-15 点名要求修正）"（`:2807-2808`）。
- 弹层点击那一条走 `w.rep.limitation(...)`（`:2960` 附近），注释写明"本条**不计入通过**"。
- `rec()` 的 docstring 现在带警告："⚠️ **不要**再用它记「未执行」或「限制」——那是把非通过
  写成通过的经典错法"（`:654`）。
- 分档与退出码：`NON_PASS = (FAIL, ENV_BLOCKED, REVIEW_REQUIRED, NOT_RUN, LIMITATION)`（`:620`），
  `return 0 if verdict == Reporter.PASS else 1`（`:4272`）。

⇒ 最近一轮 `walk-20260916-022603` 的 `tally` 是 `PASS 417 / FAIL 0 / LIMITATION 4`、
`verdict=NOT_RUN` —— **限制没有被算进通过数**，整体判定也没有被写成通过。

### 3.2 "复用路径 1/1 成功"不能表述为稳定

`decisions/0009` §"仍然不同的那一半"前后现在写的是：
"**已做过一次对照实测（各 1 样本）**……⇒ **否掉了'代理是主因'这个猜测**"
（`:614-617`），并且把现象定性为"**新起**的实例注册不稳定（本轮 2/2 失败；历史上也有过成功）、
而**复用已在运行**的实例 1/1 成功 ⇒ 处置仍以'先探测、优先复用'为准"（`:617-618`）。

⇒ 表述是"**有可用缓解**"，不是"已证明稳定"。**无需改动**。
（另：HO 要求的恢复边界"不得默认关闭 HO 的其他 IDE 实例"已实现，见 §2 H4/D2。）

### 3.3 "MySQL 已由 CI 覆盖"必须限定对象

**已满足，且范围可以写得更宽。** 精确口径：

`pytest-mysql` job 覆盖 11 项 = 迁移落定 / 基线建表 / **双认领竞争** / 乐观锁并发编辑 /
幂等并发副作用恰好一次 / 五个模块的时间戳渲染 / **Agent 作业三条并发锚点**
（双 worker 竞争同一作业、租约过期接管、旧 worker 迟到写入作废）。

⚠️ 但要说清两件事：
1. **SQLite 结果不能替代 MySQL 锁正确性**（这条纪律在 CI 注释与测试模块头部都写着）；
2. 上面这 11 项**不等于**"MySQL 上的所有行为都已验证"——未覆盖的部分仍按 `not-run` 记。

### 3.4 自纠：我自己在这一轮误判过一次（留档）

我最初拉 `gh api .../actions/jobs/{id}/logs`，看到三个 H7b 用例名**出现 0 次**，
就倾向判断"它们被 deselect 了"。**这是误判**：那一处 pytest 输出是**点号格式**
（`tests/test_mysql_integration.py ...........`），用例名根本不会逐行出现。

**正确判据**是三条合起来：① `collected 691 items / 680 deselected / 11 selected`；
② 该行有 **11 个点**；③ 模块的 `def test_` 恰好 **11 个**且模块级 `pytestmark` 含 `mysql`。

⇒ 教训：**"名字没出现"不等于"没执行"**。用"选择/通过计数"判执行，别用"字符串是否出现"判执行。

## §4 收尾项（**处置结果**；仍未闭合的也都不阻塞 S1）

本表 9 条：**6 条已闭合**（U-1 / U-2 / U-3 / U-4 / U-5 / U-6），
**1 条随阶段产出**（U-9），**2 条按纪律保持登记不动**（U-7 / U-8，均为走查 `LIMITATION`）。

| 编号 | 事项 | 现状与依据 | 处置 |
| --- | --- | --- | --- |
| **U-1** | `decisions/0009` 内部自相矛盾：§本文档 :100-102 把"真机页面栈深度"仍列为未覆盖缺口，而文末「后续增量」:714-715 已写"✅ 2026-09-15 实测闭环" | 同一文件两处口径打架 | **本轮已修**：:100-102 收窄为只剩 console 口径一条 |
| **U-2** | `scripts/verify_miniapp_devtools.py:61` 自述"真机页面栈深度……**仍未做运行期验证**" | 该脚本自己就是㉝ 章的实现者，自述已过期 | **本轮已修** |
| **U-3** | `docs/entrust/00-基线对齐报告.md:32-33` 两行过期：`分支保护 main/develop 均未受保护`、`AGENTS.md 不存在（规范要求补齐）` | `AGENTS.md` **已存在**（7126 字节）；`develop` **已有分支保护**（`required_status_checks.strict=true`） | **本轮已修**（保留原判断为"当时属实"，加订正注） |
| **U-4** | `并发集成（MySQL 8.0 真实执行）` **未列入必需检查** | 分支保护的必需检查原为 5 条：后端 lint+test / 迁移 SQLite / 迁移 MySQL / 前端静态 / 前端端到端。**不含 `pytest-mysql`** | ✅ **已办**（2026-09-16 授权后执行）：`PATCH .../protection/required_status_checks` ⇒ **5 条 → 6 条**；`strict=true` 与其余保护项逐项复核未被改动。见 §6 E-2、§9 |
| **U-5** | HO 要求"镜像表校验作为开发规范直接执行"（新枚举/状态/字段映射必须增加对应契约检查，无需每次审批），但该规范未写入 `AGENTS.md` | `AGENTS.md` 里搜不到"镜像/mirror/契约检查" | **本轮已补**（`AGENTS.md` §3.5） |
| **U-6** | H7a 脚本**未逐条断言 `mocked is False`** | HO 要求"正式质量测试必须确认实际响应 `mocked=false`；请求真模型但缺凭据时应报资源阻塞，不能自动降级后继续交'真模型'结果"。原状：脚本只在**入口**判断（`LLM_MOCK or not LLM_API_KEY` ⇒ 打印"本脚本无意义"并退 `rc=2`），**满足**"不静默降级"；但**每条响应**上没有断言 `mocked` 字段 | ✅ **已办**（PR **#114**）：判据从"入口配置"下沉到"**每条响应**" —— 命中 `mocked=true` 即**丢弃、不计分、单独留痕**，`_verdict()` 增第 5 条 `real_response`，命中即 `rc=2`（资源阻塞）。见 §7 PR-7、§9 |
| **U-7** | 走查第 ㉝ 章的**预算耗尽路径**走不到 | 真实入口可达的最深链实测只有 **4 层**，到不了预算 8 ⇒ "栈到预算改走 fallback"这一支无法用真实入口覆盖。产品形态决定，非工具问题 | **保持 `LIMITATION` 登记**，不改成通过；待出现深度 ≥ 8 的真实业务链时再覆盖 |
| **U-8** | 走查 ⑧b / ㉕D 两条 `LIMITATION`（原生弹层）；同类还有 ㉞ 章 tabBar 凸起那条 `NOT_RUN`（`[data-key='publish']` 页面级命中 0） | 工具侧结构性边界，**没有因为另开通道取证而消失**。⑧b / ㉕D 的原生弹层不在渲染树；tabBar 凸起是**组件**、不在页面级查询范围内 —— 两者都是**走查工具**够不着，不是"事实不可验证" | **一律保持登记**（`LIMITATION` / `NOT_RUN` 都不改成通过）。另开的通道补的是**另一条**证据：⑧b ⇒ `H3-原生弹层确认键人工验证记录.md`（OS 级自动化输入 + 人手两行）；tabBar 凸起 ⇒ `㉞-tabBar凸起-OS级真实点击记录.md`（`SendInput`，页面栈 1→2 落发布货源页，且有对照点击排掉"蒙对坐标"）。⚠️ 下次跑走查，那两格**照旧**出现 |
| **U-9** | 业务产出的 5 份文档尚未落位（合同 §12 要求 8 件） | ⚠️ **2026-09-16 更新**：`DEMO-1-interface-delta.md` **已产出**（S1 第一个工作项触发：货主侧无接口可列出自己的生效委托授权 ⇒ 新增 `GET /my-entrustments`）。**其余 4 份**（`runbook`（S1 起步）/ `walkthrough` / `acceptance` / `evidence-index`（S5））仍未落位 | 按计划产出，**不为假想需求预写空壳**（合同 §11.1 明令禁止）；`interface-delta` 的产出即"第一处接口面变更"这条触发条件的落地 |

## §5 阻塞项（真正会挡住开工的）

| 编号 | 事项 | 判定 |
| --- | --- | --- |
| — | **无** | 逐条核完，没有任何一项满足"不解决就无法开始 S1"。 |

S1 的出口判据（合同原文）是
`A fresh UI-created assignment survives reload, appears in the correct queue,
can be claimed once, and remains inaccessible to unrelated organization B.`
⇒ 它只依赖**已有**的后端受理/认领能力 + 一个新界面，**不依赖** S3/S4 的客户侧闭环与结案命令。

**两个"咽喉"不是开工前置，而是新合同的交付内容**（写在这里防止被当成阻塞）：

1. **S3 客户侧闭环零实现** —— 无 `/customer` 端点、无"释放不可变报价版本"、
   `registry.py:project_for_customer` 仍**无生产调用方**（唯一链路是
   `envelope.py:259` 的 `customer_visible_only=True`，而它的唯一生产调用
   `agentjobs.py:890` 用的是默认 `False`）。⇒ 这是 S3 要建的东西。
2. **委托级真实结案命令不存在** —— `exceptions.py:448` 自述未上线。⇒ 这是 S4 要建的东西。

## §6 需要外部动作的项（我做不了的）

| 编号 | 事项 | 需要谁 | 说明 |
| --- | --- | --- | --- |
| **E-1** | 把 TODO-07「真实微信登录 / 真实支付」**拆成两项独立登记** | HO / 用户 | HO 已明确：登录**不应被商户号阻塞**；支付另列商户配置、回调与资金验收条件。现状是合成一条，会让"登录做不了"这个结论失真 |
| **E-2** | 是否把 `并发集成（MySQL 8.0 真实执行）` 加入**必需检查** | HO | ✅ **已办**（2026-09-16 HO 授权"1–5 全部执行"）⇒ 5 条 → **6 条**。见 U-4、§9 |
| **E-3** | 作废旧**按量计费** Key（`sk-api-…`，控制台接口密钥页删除） | 用户 | 脚本做不了。工作区 `secrets-backup/` 备份**已不存在**（已确认） |
| **E-4** | 远端已合并分支清理 | HO 授权 | ✅ **已办**（2026-09-16）⇒ **40 条 → 3 条**，删 **38 条**、失败 0；四条判据 + 逐批复验 404。见 §9 |
| **E-5** | S5 的"见证人改一个值"需要人类见证人 | HO / 委托方 | 合同 §10.3 要求；时间需协调。**HO 已裁：押后**到"备案号下来、做全量上线准备工作清项"时再实现 |

## §7 前置项处置结果（本轮）

| 编号 | 前置项 | 结果 |
| --- | --- | --- |
| **PR-1** | **合同采纳记录**：HO 于 2026-09-16 明确"同意 S0 → S5 的节奏安排，继续执行" ⇒ 在合同登记本补**采纳行**，`DEMO-1-plan.md` 状态由"草案 v0.1"升为**生效**，§9.1 的"唯一未闭环项"随之闭合 | ✅ 本轮 |
| **PR-2** | 三处文档陈旧（U-1 / U-2 / U-3） | ✅ 本轮 |
| **PR-3** | 镜像表校验写入 `AGENTS.md`（U-5，HO 要求"直接执行"） | ✅ 本轮 |
| **PR-4** | 本地已合并分支清理（按"PR merged + tip sha == PR head sha"判据） | ✅ 本轮 |
| **PR-5** | 前 4 项的证据与结论落成**本文件**（DEMO-1 的开工依据） | ✅ 本轮 |
| **PR-6** | 远端分支清理 | ✅ **已办**（2026-09-16）⇒ 40 → 3 条，删 38 条、失败 0（E-4 授权后执行，见 §9） |
| **PR-7** | H7a 补 `mocked is False` 断言（U-6） | ✅ **已办**（2026-09-16）⇒ PR **#114**（分支 `chore/h7a-real-response-assertion`）。原建议"随 S2 live 接线"未采纳 —— HO 授权即刻执行，与本文件同批办结。见 §9 |

## §8 下一步（进入 S1 的入口）

**本文的载体**：PR **#113**（分支 `docs/DEMO1-S0-prereq-closure`），含本文件 +
合同采纳记录 + 三处陈旧订正 + `AGENTS.md` 镜像表规范 + 走查脚本头部注释。
本地门禁 **13/13 PASS**、pytest **691（通过 680 / 跳过 11 / 失败 0）**。
⇒ **已于 2026-09-16 squash 合并**：`develop` = **`60a94b3`**（见 §9 第 1 项）。
本节的收尾回写以 `60a94b3` 为基线。

S1 第一条分支：`feature/DEMO1-S1-customer-intake`（从 `60a94b3` 切出），两个工作项起步：

1. 把真实发布流程里的「委托发货」入口接上受理实现（现状 `miniapp/pages/publish/cargo/cargo.js:84`
   的 `pickEntrustDelivery` 仍是"功能预览，即将开放"占位跳转、无接口调用）；
2. 新建 UI-07 客户委托草稿/提交屏。

PR 描述按计划 §1 P20 的五项写；S0-6（`DEMO-1-interface-delta.md`）随第一处 schema 变更产出。

### 8.1 S1 第一切片：落地状态（2026-09-16）

分支 `feature/DEMO1-S1-customer-intake` 已从 `60a94b3` 切出，**两个起步工作项已写码**：

| # | 工作项 | 落地内容 | 证据等级 |
| --- | --- | --- | --- |
| 1 | 真实发布入口接受理实现 | `miniapp/pages/publish/cargo/cargo.js` 的 `pickEntrustDelivery()` 由"功能预览，即将开放"占位跳转改为经 `R.go()` 进入 UI-07，带货名 / 货量 / 单位（**不带标题** —— 源页没有"要办什么"这个信息） | 静态契约（`verify_ui_interactions.js` 第 ⑨ 章已同步改写断言） |
| 1′ | 只读端点 `GET /api/v1/entrust/my-entrustments` | 服务层 `list_my_entrustments` + `MyEntrustmentOut` / `MyEntrustmentListOut` + `scope_matrix`（**61 → 62 条**）；前端 `fetchMyEntrustments()` | 后端用例 **14 条全过**，含"选项集合 ≡ 提交门禁放行集合"的**交叉断言** |
| 2 | UI-07 客户委托草稿 / 提交屏 | `miniapp/pages/entrust/intake/intake.{js,json,wxml,wxss}`，**五处登记齐全** | 前端静态 8 项全过；**未做真机走查** |

门禁：**13/13 PASS**（`scripts/verify_local_gates.py`）；
pytest **705**（通过 694 / 跳过 11 / 失败 0 / 错误 0）；mypy 98 文件无问题。

⚠️ **三条不得算作已完成**（须进余量台账）：

1. **S1 出口判据一条都还没验证** —— "survives reload / 出现在正确的队列 / 只能被认领一次 /
   B 组织不可见"这四条都要求**真机 + 真载荷**，本切片只做到"静态契约 + 后端用例"层；
2. **UI-07 未做真机走查** —— 五态渲染与提交链路都还没有设备证据；
3. **幂等键只活在页面实例里** —— "响应丢失 + 杀掉小程序重进"会再建一张草稿
   （已在 `intake.js` 文件头如实登记；修复需按用户隔离持久化）。

⇒ 已推送并开 PR **#115**（base `develop`，head `b2145d4`）；**已按 HO 授权 squash 合并**
⇒ merge commit **`d932c43`**（执行记录见 §9.3 第 1 行）。

### 8.2 S1 第二切片：落地状态（2026-09-16，分支 `feature/DEMO1-S1-exit-criteria`）

第一切片登记的三条"不得算作已完成"，本切片逐条处置（**不新造口径**，仍按 §8.1 原编号）：

| # | 第一切片的余量 | 本切片处置 | 证据等级 |
| --- | --- | --- | --- |
| 1 | S1 出口判据**一条都还没验证** | 落成 `backend/tests/test_entrust_s1_exit_criteria.py` **8 条**可执行断言（四项判据各有正例 + 负例；文件型 SQLite，终局一律**换新会话读**，见 DR-0002） | 后端用例 **8 条全过**（junitxml 判据） |
| 2 | UI-07 **未做真机走查** | `verify_miniapp_devtools.py` 新增 **㉞ 章**：全链**真实点击**（身份卡 → 发布货物页 → 「委托发货」→ 真实输入标题 → 提交 → 详情落地）走进 **69 项**断言 | **PASS=62 / FAIL=0 / NOT_RUN=4 / LIMITATION=3** |
| 3 | **幂等键只活在页面实例里** | 在途状态按用户隔离落 `storage`（`entrust_intake_draft_<user_id>`，拿不到 `user_id` 的账号**不持久化**）；`reLaunch` 换页面实例后自动续接**同一张**草稿 | 真机 PASS（编号 `6` → `6`、创建键一致） |

门禁：**13/13 PASS**（`scripts/verify_local_gates.py`）；
pytest **713**（通过 702 / 跳过 11 / 失败 0 / 错误 0）；mypy 98 文件无问题；ruff `check` / `format --check` 均 rc=0。

远端：已推送并开 **PR #116**（base `develop`，head `fe42f34`）⇒ **CI 12/12 全绿**
（`push` 与 `pull_request` 两个事件的 6 项各跑一遍，`conclusion` 全 `SUCCESS`
—— 含本地跑不了的「前端端到端（真后端载荷驱动）」与「并发集成 / 数据库迁移（MySQL 8.0）」）。
⚠️ **CI 绿 ≠ 出口判据通过**；且**未合并**（DR-0003，等 HO 授权），见 §9.4。

⚠️ **两条不得误读**：

1. **㉞ 章的章节结论是 `NOT_RUN`，不等于"没跑"。** 62 项 PASS、FAIL=0，但另有 4 项 `NOT_RUN`
   与 3 项 `LIMITATION`；六档口径下**只有 `PASS` 计入通过** ⇒ 本章**不能**写成"通过"。
   逐条清单与"为什么没有证据"见 `DEMO-1-plan.md` §4 S1 的交代块。
2. **本切片在走查中修掉了两个真实产品缺陷**（都不是走查脚本的问题）：
   ① **入口参数二次编码** —— `onLoad` 拿到的 query 是**百分号原样串**，页面按"已解码"重建 url
   会再编码一次；中文货名 8 字 → **69 字符**撞 `cargo_name.maxLength` ⇒ 页把自己的入口判成非法。
   ASCII 参数上是恒等变换，所以此前六个只带 id 的页面都没暴露；
   ② **`router.py` 漏捕 `AccessDeniedError`** ⇒ 越权认领与无生效授权提交返回 **HTTP 500** 而非 403
   （函数里那个 `→ 403` 分支根本没机会执行）。两条都带了回归用例
   （前端 `verify_ui_interactions.js` ⑩ 章 / 后端出口判据用例）。

## §9 收尾执行记录（2026-09-16，HO 授权 1–5）

HO 于 2026-09-16 授权"下一步建议 **1–5 全部执行**（含进入 S1 的入口）"，
并明确 **E-1 / E-5 押后**到"备案号下来、做全量上线准备工作清项"时再实现。执行结果：

| # | 事项 | 结果 | 证据 |
| --- | --- | --- | --- |
| 1 | 合并 PR #113 | ✅ squash → **`60a94b3`**；本地 develop 已同步（`--is-ancestor` 快进校验通过、树一致） | `PUT /pulls/113/merge` 返回 `{"merged":true}` |
| 2 | 「并发集成（MySQL 8.0 真实执行）」加入 develop 必需检查（U-4 / E-2） | ✅ **5 条 → 6 条**，`strict=true` 不变，其余保护项（linear history / force push / deletions / admins）**逐项复核未被改动** | `PATCH .../protection/required_status_checks`；复读确认 |
| 3 | 清理远端已合并分支（E-4 / PR-6） | ✅ **40 → 3 条**，删除 **38 条**、失败 0；逐批 10 条 + **每批后复验已 404** | 剩 `develop` / `main` / S1 分支 |
| 4 | 开 S1 分支并进入 S1 | ✅ `feature/DEMO1-S1-customer-intake` 从 `60a94b3` 切出 | 见 §8 |
| 5 | H7a 补逐条 `mocked is False` 断言（U-6 / PR-7） | ✅ **PR #114**（分支 `chore/h7a-real-response-assertion`）：判据从"入口配置"下沉到"**每条响应**"，桩响应丢弃不计分、`rc=2` | 该 PR 内含 `ruff check` / `format --check` / `--dry-run` 实测 |

**⚠️ 两处如实交代**：
1. **删分支用的是四条判据**（PR `mergedAt` 非空 + `headRefName` 匹配 + 远端当前 sha == PR head sha
   + `merge_commit` 是 develop 祖先）。第 4 条的写法我在预演时**读反过一次**：
   `compare/{mergeCommit}...develop` 的 `status` 是"**develop 相对 mergeCommit**"，
   `ahead` 恰恰表示 **mergeCommit 已在 develop** ⇒ 我差点把 38 条全判成"不可删"，
   并得出"远端已合并分支都不能清"的**反向结论**。改用 `behind_by == 0`
   （不依赖命名的直觉）后复算无误。**判据要选"含义可直接验证"的字段，别选靠名字记方向的。**
2. **squash 合并的代价**：这些分支的原始提交**不在** develop 的祖先链上（develop 上是新的 squash 提交），
   删分支后只能通过 PR 记录（`head.sha`）追溯。**内容不丢**（已用 `merge_commit` 祖先判据逐条验证），
   但"还能把那条分支 `checkout` 出来"**不再成立**。这是 squash 工作流的固有性质，不是本次操作引入的。

### 9.1 执行过程中触发的本机故障（留档）

删本地已合并分支时再次触发本机 git 故障（松散 ref 被清）。**判据已扩展为三条合看**：
① refs 总数；② `git rev-parse HEAD` 可解析；③ `git log -1` 非空。
**本次 `git status --porcelain` 的 ` D` 行自始至终为 0** ⇒ 只看 ` D` 会把"HEAD 已 unborn"
判成"一切正常"。另有一条**易漏点**：`git symbolic-ref HEAD <分支>` 只改 `.git/HEAD`、**不动索引**，
所以切分支后**索引里仍留着上一个分支的内容**（表现为 `M ` 暂存态），
必须用 `git checkout HEAD -- <显式路径>` 还原，否则会误提交到错误的分支。

### 9.2 S1 第一切片的执行记录（2026-09-16，同一轮授权）

| # | 事项 | 结果 | 证据 |
| --- | --- | --- | --- |
| 1 | 后端只读端点 + 用例 | `GET /my-entrustments` / `list_my_entrustments`；`scope_matrix` **61 → 62**；新用例 **14 条全过**（含交叉断言） | 提交 `b2145d4` |
| 2 | 前端契约层 + UI-07 + 五处登记 + `cargo.js` 接线 | 前端静态 8 项全过；`verify_routes.js` 页面 **22** / 注册 **22** | 同上 |
| 3 | 全量门禁复跑（修掉两处 ruff 之后） | **13/13 PASS**；pytest **705**（通过 694 / 跳过 11 / 失败 0 / 错误 0） | `.workbuddy/_gates_all2.txt` |
| 4 | 提交与推送 | `405bd29..b2145d4`（**单提交**、19 文件、+1781/−26）；推送后远端 sha 与本地一致 | `git ls-remote origin` |
| 5 | 开 PR | **#115**（base `develop`） | `POST /repos/.../pulls` ⇒ `201` |

⚠️ **本切片未验证 S1 的出口判据**（见 §8.1 的三条）⇒ 本 PR **不声称任何 D1 行通过**。
（合并由 HO 在**下一轮授权**中给出，见 §9.3 第 1 行 —— DR-0003：PR 作者不自批。）

### 9.3 S1 第二切片的执行记录（2026-09-16，HO 授权「下一步建议 1–5 全部执行」）

| # | 事项 | 结果 | 证据 |
| --- | --- | --- | --- |
| 1 | 合并 PR #115 / #114（HO 授权） | ✅ squash → **`d932c43`**；本地 `develop` 已同步（`refs/rorigin/develop` == `d932c43`） | `PUT /pulls/115/merge` ⇒ `{"merged":true}` |
| 2 | UI-07 真机走查 | ✅ `verify_miniapp_devtools.py` 新增 **㉞ 章**（69 项）；首跑 **FAIL=11** ⇒ 定位到入口二次编码缺陷 ⇒ 修完重跑 **FAIL=0** | `miniapp-device-artifacts/_walk_082603_out.txt` |
| 3 | 修入口参数二次编码 | ✅ `routes.js` 新增 `decodeParam()`；`cargo_name.maxLength` **64 → 512**（对齐后端 `cargo_summary`）；**7 个入口页**一并接上归一化 | 静态门禁 `verify_ui_interactions.js` ⑩ 章（**真页面模块 + 真路由模块**跑行为，含反向断言） |
| 4 | 在途幂等键按用户持久化 | ✅ `intake.js` 7 处落 `storage`；`onLoad` 内**续接优先于 carry** | 真机 ㉞ 第二节 PASS |
| 5 | 出口判据落成可执行断言 | ✅ `backend/tests/test_entrust_s1_exit_criteria.py` **8 条** | 门禁 pytest **713**（通过 702 / 跳过 11 / 失败 0 / 错误 0） |
| 6 | 修 `router.py` 漏捕 `AccessDeniedError` | ✅ 三处 `except` 收敛为 `(svc.AssignmentError, AccessDeniedError)` | 出口判据用例（第 4 项判据的负例）复现 → 修 → 复测 |
| 7 | 全量门禁复跑 | ✅ **13/13 PASS** | `.workbuddy/_gates.txt` |

⚠️ **三处如实交代**：

1. **合并 ≠ 出口判据通过。** #115 合并时出口判据"一条都还没验证"；本切片补的是**证据**，
   而证据里有 4 项 `NOT_RUN` + 3 项 `LIMITATION` ⇒ **本切片同样不声称任何 D1 行通过**。
2. **首跑的 11 条 `FAIL` 全部是同一个根因的链式后果**（页面进了错误态 ⇒ 表单没渲染 ⇒
   输入框与提交钮都点不到）。定位过程**没有重跑真机**：先做排除法（导航前的 `resolveNavigation`
   用**同一个** `parseQuery` 校验且通过了，⇒ 只可能是页面又编码了一次），再用 **Node 直跑被测模块**
   复现出**同一条** reason（精确命中「超长（上限 64）」）。真机一轮约 10 分钟、Node 复现是秒级
   ⇒ **"能不能用纯逻辑复现"应当先于"再起一次环境"来问**。
3. **环境侧代价（留档）**：本机 shell shim 构造 `PATH` 失败（`C:\Windows\System32` 被截断）⇒
   `tasklist` / `taskkill` / `mkdir` / `tail` 全部不可达，而走查运行器**依赖 `tasklist` / `taskkill`**
   ⇒ 会**静默降级成假结论**；叠加"本环境在命令结束时回收该命令的全部后代进程"与
   "IDE 冷 `CompileCache` 约 9 分钟、而就绪闸门只给约 2 分钟" ⇒ 曾连续两轮 `ENV_BLOCKED`。
   解法：**起 IDE + 探测 + 跑走查必须同一个脚本内完成**，且**必须先 `open_project_window`
   再读 `pageStack`**（否则回 `cant find runtimeid by projectpath` —— 那是应有回答，不是故障）。

### 9.4 提交 / 推送 / CI / PR 记录（2026-09-16，同一轮授权）

| # | 事项 | 结果 | 证据 |
| --- | --- | --- | --- |
| 1 | 按关注点分三个提交（后端 / 前端 / 文档） | ✅ `38c8e80`（2 文件 +590/−4）/ `f92356c`（14 文件 +1139/−30）/ `fe42f34`（4 文件 +170/−19） | `git diff d932c43..HEAD --stat` = 20 文件、+1899/−53，与提交前的 `--stat` 逐项吻合 |
| 2 | 推送 | ✅ 第 1 次 `schannel: server closed abruptly`、**第 2 次成功**；远端 sha == 本地 HEAD | `git ls-remote` ⇒ `fe42f34134bcaf70aafaf972ada0b4aca9ac1de4` |
| 3 | 开 PR | ✅ **#116**（base `develop`） | `POST /repos/{R}/pulls` ⇒ `201` |
| 4 | CI | ✅ **12/12 全绿**（`not_success=0`） | `GET /commits/{sha}/check-runs` |

⚠️ **两条如实交代**：

1. **CI 绿 ≠ PRD 完成，也 ≠ 出口判据通过。** 12 项绿只证明"既有门禁在**本提交**上通过"；
   ㉞ 章的 4 项 `NOT_RUN` + 3 项 `LIMITATION` 一条都不会因为 CI 绿而变成"有证据"。
2. **本轮遇到一次"写操作脚本被 SIGTERM ≠ 操作没发生"**：分组提交脚本在**收尾打印阶段**被
   工具侧超时杀掉（stdout 全空、`Exit Code 1 / Signal SIGTERM`），重跑报
   `nothing added to commit` —— 但 **3 个提交其实已全部创建**（`git log -5` 三条齐全、
   `diff --stat` 与提交前完全吻合、refs 总数未变、`D` 行数 0）。⇒ 判据是**目标产物在不在**，
   **不是**进程存活状态或返回码（与"推送超时 ≠ 推送失败"同源）。

### 9.5 本轮执行记录（2026-09-16，HO 授权「执行下一步建议 1–5」+ D-4 裁定）

| # | 事项 | 结果 | 证据 |
| --- | --- | --- | --- |
| 1 | 合并 chore 的 **PR #118** | ✅ squash ⇒ **`2ee4c2b`**（无依赖、纯仓库卫生 ⇒ 先合） | `mergedAt=2026-09-16T02:44:28Z` |
| 2 | 合并功能 **PR #117**（S1 第三切片） | ✅ squash ⇒ **`b30577c`**；`develop` 远端 = `b30577c`，本地已同步 | `mergedAt=2026-09-16T02:45:10Z`；`refs/rorigin/develop` == `HEAD` |
| 3 | 按 HO 裁定的 **D-4** 实现受理入口判据 | ✅ 前端 3 文件；**6 个静态门禁全绿** | `DEMO-1-interface-delta.md` §7.3 |
| 4 | 真机新增 **㊱ 章**（受理入口判据 · 双向对照 · 403） | ✅ **19 项 PASS / FAIL=0**；另 2 项 `LIMITATION` 如实登记 | `miniapp-device-artifacts/_walk_110629_out.txt` |
| 5 | ㊱ 首跑有 1 条 `FAIL` ⇒ 定位到**测试自己漏了环境控制** | ✅ 修完重跑 `FAIL=0` | 同上（`[重跑关联]` 两行：首跑 `=FAIL`、次跑通过） |
| 6 | 补 **OS 级鼠标通道**：tabBar 凸起「发布货物」 | ✅ 页面栈 **1 → 2**、落 `pages/publish/cargo/cargo`；**对照点击**落「订单」tab 且**栈深不变** | `docs/entrust/㉞-tabBar凸起-OS级真实点击记录.md` |
| 7 | 清理 chore worktree + 已合并的遗留本地分支 | ✅ `worktree remove`；本地分支清到只剩 `develop` / `main` | ` D`=0、`ls-files`=359 不变、`refs/rorigin/develop` 完好 |
| 8 | 统一门禁全量复跑 | ✅ **13/13 PASS**；pytest **713 项（通过 702 / 跳过 11 / 失败 0 / 错误 0）** | `.workbuddy/_gates_d4.txt` |

⚠️ **五条如实交代**：

1. **㊱ 章首跑的 1 条 `FAIL` 不是产品缺陷，是测试漏了环境控制**：断言「多组织身份进工作台
   **不预选**」假定走 `pickOrg` 的 `ambiguous` 分支，而该 Storage 键（`entrust_active_org`）
   在开发者工具里**跨 IDE 重启保留**，上一次走查（⑯ 章末尾点过组织药丸）留下的值让页面走了
   `saved` 分支 ⇒ 断言必然落空（实测 `orgs=['2','3'] active='3'`）。修法是**补上被漏掉的
   `remove_storage`**（⑯ 章同类断言早有此步），并把 `orgReason` 一并纳入判据让失败可自诊。
   ⚠️ 这**不是**"为了好过而放宽断言"：被跳过的那一支（`saved`）由 ⑯ 章单独覆盖。
2. **`FAIL` 与 `NOT_RUN` 的区别必须守住**：本轮整轮 `RESULT: NOT_RUN`
   （PASS 51 / FAIL 0 / `NOT_RUN` 1 / `LIMITATION` 3）。它**既不表示"没跑"、也不表示"有失败"**；
   `LIMITATION` 在 HO 五档里会被归并进 `NOT_RUN` ⇒ 汇报一律**双句**。
3. **两条通道的证据必须分别记录**：㉞ 章 tabBar 那格的 `NOT_RUN`、⑧b / ㉕D 的 `LIMITATION`
   **一律保持原样**（见 §4 U-8）—— 本条补的是**另一条通道**的证据，不是把那一格涂绿。
4. **D-4 仍有一条分支未取证**：裁定 §5 的「**权限被撤销** ⇒ 界面刷新」只证到**后端拒绝**
   （403，㊱ 章 API 直证），**界面刷新**只有静态断言（缺"运行中改成员角色"的通道）。
5. **本机 git 故障本轮又出现一次（第三种表现）**：`update-ref -d` 删**一条**已合并分支时，
   把 `.git/refs/heads/` 下**另一条**未动的分支 ref 连带清掉（脚本对它的判定是"保留"）。
   ⚠️ 这次 **` D` 始终为 0、`ls-files` 也没变** ⇒ 单看 ` D` 会判"一切正常"。
   已按"先证明没丢东西"的顺序核实：该分支的 PR `state=MERGED` 且其 squash 提交是 `develop`
   的祖先 ⇒ **内容都在、无需恢复**。⇒ **删 ref 的前后对账必须"逐条比名字"，不能只比数量。**

