# S3 发布与客户响应 · 数据设计（HO 0917-3 裁定三）

| 字段 | 内容 |
| --- | --- |
| 状态 | **后端已落**（表 + 命令 + **9 条端点** + 来源标注 + 客户下载白名单）；**界面已上线**（客户侧「对客报价」卡 / 经理侧发布·撤回）。⚠️ 仍**不声称任何 D1 通过** |
| 日期 | 2026-09-17 |
| 依据 | HO 0917-3 裁定三（A/B 已明确裁定并批准开工）；合同 BP-03 第 4/5/6/7/10 条；D1-06 / D1-07 / D1-08 / D1-11 |
| 相关 | `S3-范围与依赖评估.md`（范围评估）、`DEMO-1-interface-delta.md`、`DEMO-1-fixture-manifest.md` |

⚠️ **本文件不承诺排期、不声称任何 D1 通过。** 它只把"数据长什么样、每条设计挡住哪件事"写清楚。

## 1. 裁定三给出的口径（逐条登记）

| 项 | 裁定 |
| --- | --- |
| A：客户身份 | **客户 = 当前委托的货主本人**，复用现有账号与 `owner_user_id`；**不新增全局 `customer` 角色** |
| A：客户入口 | 扩展现有「我的委托／委托详情」，加入「待确认报价、合同、响应记录」 |
| A：权限 | **组织经理权限不能代替客户确认权限**；按**具体委托归属**判断，不只看全局角色 |
| B：发布版本 | 增加**独立的发布记录**，绑定**精确成果 revision**，并保存**当时的客户白名单快照** |
| B：客户响应 | 绑定发布记录，记录接受／拒绝、**客户身份**、时间；**幂等且事务内处理并发** |
| B：后续修改 | 内部编辑产生新版本，**不改已发布快照**；新版本重新发布后，旧待响应版本**不得继续接受** |
| 冻结细节 ① | **新建内部草稿不等于自动撤销旧报价** —— 只有显式重新发布／撤回才改变旧报价的可响应状态 |
| 冻结细节 ② | **已接受的旧版本永久保留**；后续变更需要新的客户确认，不能覆盖此前的接受事实 |
| 客户下载 | 必须**限定到该发布记录明确授权的文件**，不能因为客户属于同一委托授权就把内部供应商报价和其他附件全部开放 |
| 航段 | 需要**最小结构化航段对象**（稳定 ID、所属委托、运输方式、起终点、顺序），任务与候选运力引用对应航段；**不建通用路线图或优化引擎** |

## 2. 四张表与它们各自挡住的那件事

| 表 | 一句话 | 它挡住的错误形态 |
| --- | --- | --- |
| `ent_leg` | 航段（委托内 `seq` 唯一） | 「三段运输只写在任务说明里」⇒ 变更复核与运力确认无从按航段对齐 |
| `ent_capacity_candidate` | 候选运力（可用吨位／船数／是否拆批是**数据**） | 只写"900 吨" ⇒ 「变更后 950 装不下」**算不出来**，只能靠模型意见（合同明令禁止） |
| `ent_offer_release` | 对客发布记录（精确 revision + 白名单快照 + 授权附件 + 三态） | 「发布后按现在的规则重算」⇒ 客户已看到的内容被悄悄改掉；「有更新的记录」被当成"旧报价已撤回" |
| `ent_offer_response` | 客户响应（`UNIQUE(release_id)`） | 「先查后写」在并发下必然漏 ⇒ 同一次发布被响应两次 |

### 2.1 为什么发布记录要存**快照**而不是只存 revision

发布记录存了 `snapshot_json`（发布那一刻的**客户白名单投影**结果）。理由：

- 客户是照着**当时看到的那份内容**做决定的。若发布后按"现在的投影规则"重算，
  改一次内部字段归属就可能悄悄改变客户已看到的内容 —— 而记录上还写着"同一个版本"。
- 合同 §3.2 明确要求区分「**read-only preview**」与「**completed business action**」，
  以及「model proposal」与「authorized business execution」；快照是让"客户看到的"
  这个事实**可回溯**的最小手段。

### 2.2 为什么 `status` 是显式三态，而不是"取最新一条"

裁定三冻结了两条语义，它们都**不能**从"有没有更新的记录"推出来：

- **新建内部草稿 ≠ 撤销旧报价** ⇒ 不能因为有人开了新草稿就把旧发布标成失效；
- **已接受的旧版本永久保留** ⇒ 旧发布被"取代"后仍然要能被读出来（连同它的响应）。

所以 `released → withdrawn / superseded` 必须由**显式动作**写下来，并留 `closed_by` /
`closed_at` / `close_reason`。**由推断得出的状态是不能审计的。**

### 2.3 「同一发布只能被响应一次」为什么必须落在**唯一约束**上

裁定三原话是"幂等且事务内处理并发"。应用层"先查有没有响应、没有再插"在并发下
两个请求可以同时通过检查 —— 于是同一次发布被接受两次，而两次都"看起来"合法。
`UNIQUE (release_id)` 是数据库层面的事实判据；应用层再叠加幂等键（`ent_idempotency`）
负责"同键重放返回同一结果"。

### 2.4 客户身份的落法

`customer_user_id` 存的是**委托的货主本人**，而**不是**一个新角色：

- 判权时按**该委托的归属**判（`ent_assignment.owner_user_id == 调用者`），
  **不**依据全局角色 —— 裁定三明确"组织经理权限本身不能代替客户确认权限"；
- 这意味着**同一个账号**在卖家侧（组织经理）与买家侧（货主）看到的入口与能力不同，
  而这正是"按具体委托归属判断"的意思。

## 3. 与本轮其余工作的关系

- **已实现（本轮及上一轮）**：发布命令、客户响应命令、撤回、来源核验台账与发布前门槛、
  **9 条端点**（含客户按发布清单下载附件）、**数据来源标注**（`ent_artifact_origin`，
  合同 BP-03 第 9 条 / §11.1 的 `labeled synthetic/manual/live sources`）、
  **签署证据模式标注**（`labeled_sample` 随快照冻结）、**界面**（见 §6.3）。
- **仍未实现**：运力确认与有效期 —— 属 S3 后续切片
  （HO 执行顺序：*S3 发布/客户响应纵向切片 → 运力确认与合同派生*）。
  > 后续进展（2026-09-17）：**合同派生（BP-03 第 8 条）已落** —— 见 `S3-合同派生切片.md`。
  > 它**不改本文件的四张表**，另起迁移 `ent_contract`（派生记录 + 字段来源表）。
- **不动既有表、不加 FK**（沿用 `ent_` 支线"应用层保证引用完整性"的既有约定 —— 见
  `ent_extract.py` 的同一论据）。本轮**只新增表**（来源核验台账 + 来源标注表）。
- **未核验提案不得被当作已核实依据**（裁定二）**已落实在发布环节**：见 §6.2。

## 4. 本文件**不做**的事

- 不排期、不给百分比、不声称任何 D1 通过；
- 不替 HO 决定"客户入口放在哪一屏"这类产品细节（裁定三只给了方向："扩展现有
  「我的委托／委托详情」"）；
- 第 5 节是**切口**、第 6 节是**实施记录**；两者都不等于"这一步已完成"。

## 5. 下一条交付：**发布 → 冻结内容 → 客户响应** 纵向切片（切口冻结）

> HO 0917-3 执行顺序原文：*「下一条完整交付应是：经理发布指定版本 → 货主查看冻结内容 →
> 接受／拒绝 → 经理看到响应。权限、失效版本、重复请求和并发处理随这条业务一起完成。」*
> 本节把这句拆成**可逐条验收的出口**，并按"业务结果"而不是"建表/按钮"来写。

### 5.1 出口（按顺序，每一步都要有可运行结果）

| # | 出口（可运行的业务结果） | 判据（正向） | 判据（负例，与正例同等重要） |
| --- | --- | --- | --- |
| 1 | **经理发布指定版本** | 指定 `artifact_id` + `revision_no` ⇒ 新增一条发布记录：`status=released`、绑定**精确 revision**、`snapshot_json` = 发布那一刻的**客户白名单投影**结果、`customer_user_id` = 该委托货主 | 无权者（非组织成员 / 无 `quote:publish`）⇒ 404/403；对**不存在的 revision** ⇒ 400（不得"取最新"）；对**已作废（void）**的 revision ⇒ 400 |
| 2 | **货主查看冻结内容** | 货主在「我的委托／委托详情」看到该发布：**内容是 snapshot**（不是按现在的投影规则重算）；内部成本/会话/未授权附件**不在**其中 | 非该委托货主（含**同组织的经理**）⇒ 404；未发布的草稿 ⇒ 不可见；**下载**只放行该发布记录明确授权的文件 |
| 3 | **客户接受／拒绝** | 一次性写入响应：`accept`/`reject` + 客户身份 + 时间 + 目标 revision ⇒ 经理侧立即可见 | **经理冒充客户** ⇒ 403（组织权限不能代替客户确认权限）；对**未发布**或**已被取代/撤回**的版本响应 ⇒ 409；**同一发布重复响应** ⇒ 幂等键同键返回首次结果、**不同键**被 `UNIQUE(release_id)` 挡住（409） |
| 4 | **经理看到响应** | 发布记录上能看到响应方、结论、时间与**精确版本** | 响应不得改写已发布快照（改内容 ⇒ 只能产生**新 revision + 新发布**） |
| 5 | **失效语义**（裁定三冻结细节） | 显式 `withdrawn`（撤回）改变可响应状态 | **新建内部草稿不改变**旧发布的可响应状态；已接受的旧版本**永久保留**（连同响应） |

### 5.2 实现面（只列"必须新增"的部分）

- **命令 + 端点**（`scope_matrix` 会增 ⇒ 条目数测试要一起改）：
  - `POST /entrustments/{eid}/offer-releases`（发布；幂等）
  - `GET /offer-releases/{id}`（经理视角）与 `GET /my-offer-releases`（货主视角，**白名单投影**）
  - `POST /offer-releases/{id}/responses`（客户响应；幂等）
  - `POST /offer-releases/{id}/withdraw`（撤回；幂等）
- **并发**：`ent_offer_response` 的 `UNIQUE(release_id)` 是**唯一判据**；
  应用层"先查后写"在并发下必然漏 ⇒ 需 **MySQL 真实并发用例**（CI 第 6 个 job 已在跑 MySQL 8.0）。
- **发布前来源门槛**（裁定二的具体化）：发布命令**必须**能区分"已核对来源"与"未核对来源"，
  未核对的提案**不得**被发布；核验动作要留下**核验对象 + 记录**，不是一个勾选。
- **界面**：货主侧扩「我的委托／委托详情」（待确认报价 + 响应记录）；经理侧在成果/发布处给出
  "发布这一版"与"看响应"。⚠️ 界面必须显示**冻结内容**与**当前内部版本**的差别，不能只显示"已发布"。

### 5.3 本切片**不做**（防范围蔓延）

- 合同派生（第 8 条）、运力确认与有效期（第 3 条）、签署证据模式标注（第 9 条）——
  属 S3 后续切片；
  > 后续进展（2026-09-17）：**签署证据模式标注（第 9 条）已落**（见 §6.4，随快照冻结为
  > `labeled_sample`）。本节是**切口当时**的冻结记录，刻意不改写 —— 进展一律记在 §6。
  > 合同派生与运力确认仍在后续切片。
- 任何 S4 内容（结案/结算/变更复核）；
- 不新建表（四张表已在迁移 `ent_commitment`）—— 若确需新列，**只走追加迁移**。



## 6. 实施记录（发布 → 冻结内容 → 客户响应；**后端与界面均已落**）

> 本节只记"实际做了什么、凭什么判定、还差什么"，**不声称任何 D1 通过**。
> 出口清单与正负例判据见第 5 节（切口），这里逐条对账。

### 6.1 新增的东西（可枚举）

| 类型 | 内容 |
| --- | --- |
| 迁移 | `ent_offer_source:1` → 表 `ent_offer_source_check`（来源核验台账）；`ent_offer_source:2` → 表 `ent_artifact_origin`（数据来源标注；**与核验台账刻意不共用一张表** —— 混在一起会让发布门槛被污染，作业行的 mocked 事实会被当成永远卡住的待核验项）。既有表一字未改。 |
| 服务层 | `app/modules/entrust/offers.py`：`release_offer` / `withdraw_release` / `respond_to_offer` / `source_gate` / `record_source_verification`（经 `record_source_check`）/ `record_origin` / `get_origin` / `resolve_origin` / 两条投影函数 |
| 端点（**9 条**，`scope_matrix` 63 → **72**，其中 1 条是成果侧的读口） | `POST /entrustments/{eid}/offer-releases`、`GET /entrustments/{eid}/offer-releases`、`GET /my-offer-releases`、`GET /offer-releases/{id}`、`POST /offer-releases/{id}/responses`、`POST /offer-releases/{id}/withdraw`、`GET|POST /artifacts/{id}/source-checks`、**`GET /offer-releases/{id}/attachments/{aid}/download`**（客户按冻结清单下载） |
| 采纳侧 | `POST /agent/jobs/{id}/adopt` 现在**同时**把信封里声明过的来源写进台账（门槛的「对象」由此而来），并按 `job.mocked` **写入来源标注** |
| 用例 | `tests/test_entrust_offer_release.py`（**15 条**，SQLite 功能与负例）+ `test_mysql_integration.py::test_offer_response_race_exactly_one_accept`（**真 MySQL** 并发） |

### 6.2 发布前来源门槛怎么落的（裁定二）

**先说一个必须说清的事实**：`ent_artifact` / `ent_artifact_revision` 上**没有任何溯源列**
（只有 `payload_json` / `source` / `note`），成果→作业的关联列也还没有（DR-0012 未完成项）。
所以"在发布命令里加个 `verified` 勾选"必然是**假门槛**：勾了也没有可核对的对象。

落法是把两件事分开存，并让判定只依赖数据：

1. **声明**（`state='declared'`）：**采纳提案时由服务端**把作业信封里声明过的
   `(kind, ref)` 逐条写入 —— 核验对象由此确定，不由人随便指定；
2. **核验记录**（`state='verified'` / `'rejected'`）：人工核对后追加一行，**必须**带
   `method`（依据）与 `checked_by` / `checked_at`；追加，不覆盖声明行。

判定（`offers.source_gate`，唯一实现）：

| 情形 | 结果 |
| --- | --- |
| 每条声明都有 `verified` | 放行 |
| 有声明没有 `verified` | **拒发 400**，并回**待核验清单**（只说"未核验"没法干活） |
| 有 `rejected` | **拒发 400**，并把"判定不可用"与"还没核"**分开列** |
| 人工直写/修改的版本（无声明） | 放行（它本来不是"模型提案"） |
| **模型产出却一条声明都没有** | **拒发 400** —— 采纳与台账写入不在一个事务里（`create_artifact` 自己 commit），把"没有声明"当"没有待核验项"会让这道缝**静默放行**；宁可拒发并把原因写清 |

### 6.3 客户下载白名单（合同 BP-03 第 10 条）

端点：`GET /offer-releases/{release_id}/attachments/{attachment_id}/download`。

| 判定顺序 | 规则 | 为什么 |
| --- | --- | --- |
| 1 | 先判发布**存在且可见**（客户本人或该委托/受理的可见者） | 不可见 ⇒ 404，与读路径同码 |
| 2 | **判据只有**发布那一刻冻结的 `authorized_attachment_ids` | "由发布那一刻决定，不由事后可见性决定" —— 事后按当前规则重算，等于把客户手里的清单悄悄改掉 |
| 3 | 清单外 ⇒ **404**（**不用 403**） | 403 等于承认"这个文件存在、只是你不能下"。对客户而言，**内部底稿的存在性本身就是信息** ⇒ 用 404 一并抹掉 |
| 4 | 再要求 `same_entrustment or same_assignment` | 只判清单仍有缝：清单被改到**别的委托**上时，持有该 id 的人会拿到跨单文件 |

⚠️ **刻意不复用** `attachments_api.load_visible_attachment`：那是"按当前可见性放行"的
通用出口。这里必须走发布清单 —— **用通用口等于把内部底稿一起开给客户**。
（用例里有一条**反面证据**：同一个经理从通用出口能下内部件、走本端点则只看到客户那几份。
没有这条，"白名单生效"与"两个口都恰好没数据"在断言上长得一样。）

### 6.4 数据来源标注（合同 BP-03 第 9 条 / §11.1）

表 `ent_artifact_origin`（`artifact_id` + `revision_no` 唯一），`mode` ∈
`live` / `synthetic` / `manual` / `unknown`。

- **判据只认事实**：由采纳作业时按 `job.mocked` 写入（`True`⇒`synthetic`、
  `False`⇒`live`、其它⇒`unknown`），**不采信调用方在请求体里的自述**。
- **`unknown` 不得猜成 `live`**。解析顺序：① 有标注行 ⇒ 用它；② `source == 'manual'`
  ⇒ `manual`；③ 其余 ⇒ `unknown`。把"不知道"折成"真实来源"等于**替审计做了判断**。
- **两条投影各给各的**（AC-26）：经理投影给 `data_origin`（mode + **依据** `basis` +
  内部 id）；客户投影**只给 `mode`**，不给 `basis`、不给内部 id。

### 6.5 还差什么（不掩饰）

- **合同派生与运力确认/有效期未做**：合同 BP-03 第 3、8 条在 S3 后续切片；
  本切片**不做任何 S4 内容**（结案/结算/变更复核）。
- **签署是样张模式**：`labeled_sample` 只说明"这次确认按演示口径记录"，
  **不是**一份已生效的法律签署。界面上常驻显示这句（藏进气泡里就会被读成已签署）。
- **权限口径一处实测澄清**：权限按 **(组织, 货主) 作用域**解析（DR-0008），
  不是按"某一条授权记录"解析 ⇒ 给同一个 (org, owner) 再插一条只读授权**不会**收回权限。
  用例初版就因为按"每条授权独立"来写而红过（详见 `test_entrust_offer_release.py` 的注释）。
- ⚠️ **本文件不声称任何 D1 通过**；各端点的实测结论以用例与本地门禁的实际输出为准，
  不以本文的叙述为准。
