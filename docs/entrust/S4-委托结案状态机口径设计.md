# S4：委托结案状态机口径设计（**Q1–Q4 已裁定**）

| 字段 | 内容 |
| --- | --- |
| 状态 | **口径已裁定（HO 2026-09-17「0917 决策」）** ⇒ **归 S4 交付**。**本文件仍不动代码**：先定死状态与迁移口径，实现另开切片 |
| 日期 | 2026-09-17（草案）／2026-09-17 HO 裁定（见 §0） |
| 起因 | `DEMO-1-runbook.md` §7 第 3 行「已完成历史委托」夹具的**阻塞理由订正**（原写"结案命令未实现"，回查代码后订正为"状态机无终态"） |
| 相关 | PRD v1.0 的 Assignment 状态表与结案规则；`DEMO-1-runbook.md` §7 / §9；`DEMO-1-plan.md` §4.1；DR-0018 §5 |

## 0. HO 裁定（2026-09-17「0917 决策」，**本章为现行口径**）

来源：`近期HO 决策：阻塞项与待定调清单决策列表0917.docx`。裁定原文要点与落地约束：

| 项 | 裁定 | 对本设计的影响 |
| --- | --- | --- |
| **Q4** | **是，必须进 S4**。本就是合同必交范围，**不再等"是否做"的二次批准** | §6「本轮不做的事」里"等 Q4 裁决"一条作废 |
| **Q1** | **DEMO-1 暂不新增 `claimed` 后通用取消**。已发生采购/履约/费用后的终止需要独立处置规则，**不是加一条状态边即可** | §5.2 迁移图**删掉** `claimed →cancel→ cancelled` 这条候选边 |
| **Q2** | **禁止 `completed` 直接取消**；允许有权限、有理由的**纠错重开**，**重开不自动获得取消资格** | §5.2 只保留 `completed →reopen→ claimed`；重开后能否取消**回到 Q1 的口径**（仍不可） |
| **Q3** | 财务状态**由费用/结算/收付及处置事实派生**；**不允许直接 PATCH 一个状态就宣称结清**；**授信（`closed_with_credit`）不得显示成财务已结清**，财务仍 `open`，授信另记依据与批准事实；**DEMO-1 主路径先走余额结清**，不顺带开发完整授信业务 | §5.3 的 `closed_with_credit` **改判**（见下） |
| 实现约束 | 五类结案前置、客户确认、**幂等**、**MySQL 并发保护不可省略** | §5.4 之外，实现切片必须另带这四条 |
| 夹具 | 「已完成历史委托」夹具**必须通过真实业务命令生成，不得直接改状态造出来** | §5.7 加约束 |
| 映射/迁移 | `claimed` ↔ PRD `active` 显式映射可接受；新增状态/时间戳/记录走**追加迁移**，**旧数据不推测完成** | §5.1 / §5.6 维持 |

⚠️ **本文件仍是"口径"不是"实现"**：裁定只解决"做不做、按什么口径做"，
**不授权此刻动代码** —— 实现仍走独立切片 + 门禁 + PR。

### 0.1 HO 0917-2 补充裁定（语义矛盾修正与排期）

来源：`近期HO 决策：阻塞项与待定调清单决策列表0917-2.docx` §3。

| # | HO 指出的问题 | 裁定 | 落地（本文件） |
| --- | --- | --- | --- |
| 1 | 「一面允许 `completed` + `open`，一面要求 `complete` 必须 `settled`」⇒ 实现分叉 | **明确分期**：DEMO-1 当前完成路径**要求结清**；授信下"运营完成但财务未结"的路径**本期不开放** | 新增 §5.3.1；§5.4 第 5 条改为硬门槛并注明本期不可达组合 |
| 2 | `financial_status=open` 被窄化成"余额非零" | **不得只按余额判定**：已有费用、待批准结算、未决争议、缺少收付依据，均属未结；**余额为零不自动等于 `settled`** | 新增 §5.3.2（四条成因 + 三取值判据） |
| 3 | `not_started` 可能掩盖"派生逻辑还没接通" | **只能表示尚未开始财务处理**；派生接通前**不展示**该字段 | §5.3.2 末条 + §5.6 数据迁移条 |
| 4 | S4-a 的边界 | **追加迁移 ≠ 结案能力完成**：不开放完成按钮、不生成「已完成历史委托」、不开放缺前置检查的完成命令；**不抢占 S2 主线** | §5.6 末条 + §7 切片表 |

## 1. 结论先行

「已完成历史委托」夹具做不出来的原因**不是缺一条命令**，而是**委托状态机没有终态**：
`claimed` 是死状态（无出边）。⇒ 要做，先扩展状态机 —— 本文档要定死的就是这件事。

## 2. 现状（2026-09-17 实测，附行号）

| 项 | 现状 | 出处 |
| --- | --- | --- |
| 状态取值域 | `draft` / `submitted` / `claimed` / `cancelled` | `assignments.py:44-47` |
| 活跃集 | `ACTIVE_STATUSES = (draft, submitted, claimed)` | `assignments.py:49` |
| 可取消集 | `_CANCELLABLE = (draft, submitted)` ⇒ **`claimed` 不在内** | `assignments.py:53` |
| 迁移图 | `draft →submit→ submitted →claim→ claimed`；`draft｜submitted →cancel→ cancelled` | `migrations/ent_assignment.py:7-11` |
| 表字段 | `status VARCHAR(16) NOT NULL DEFAULT 'draft'`（注释 `draft/submitted/claimed/cancelled`）；时间戳有 `submitted_at` / `claimed_at` / `cancelled_at`，**无 `completed_at`** | `migrations/ent_assignment.py:44-51` |
| 路由 | 无结案类端点（`close` 只存在于**案件**层的 `exceptions.py:close_case`） | `router.py` |
| 并发 | `revision` + `expected_revision`，不匹配即 409 | `migrations/ent_assignment.py:14-18` |

⇒ **`claimed` 之后无处可去**：既不能完成，也不能取消。已受理的委托在状态机层面
**只能停在那里**。补一条"结案命令"也**无处可去** —— 这正是阻塞理由要订正的原因。

## 3. PRD 要求（逐条给依据，不是本文档编的）

| PRD 口径（原文要点） | 要求 |
| --- | --- |
| `Assignment`: `draft`, `submitted`, `active`, `completed`, `cancelled`; **separate derived stage and financial closure status** | 有 `completed` 终态；**另有派生的 stage 与独立的财务结案状态** |
| Closure requires completed/cancelled-with-disposition tasks, required delivery evidence, resolved blocking exceptions, approved settlement and explicit disposition of outstanding balances | 结案有**五条硬前置** |
| Open balances keep a visible financial-open state unless a documented authorized credit arrangement permits operational closure | 未结余额保持**可见的"财务未结"** |
| Operational completion and financial closure remain distinguishable | **运营完成 ≠ 财务结案**，必须可区分 |
| Critical unresolved cases block the relevant execution/closure actions；非关键残留只能按 explicit disposition rule 关闭 | 关键未决案件**阻塞**结案；不得 generic skip |
| Reopen a permitted task with a reason and retain the complete history | 允许**重开**，带理由、**保留完整历史** |

## 4. 差距（三条）

1. **缺终态**：实现无 `completed`（PRD 有）。
2. **缺财务维度**：实现只有单一 `status`（PRD 要求运营 / 财务两个维度可区分）。
3. **`claimed` 是死状态**：连取消都不行，与 PRD「经理可在各阶段介入」的灵活性不一致。

## 5. 提议的状态与迁移口径

### 5.1 命名映射（先消歧，避免"两侧都空"的假绿）

PRD 用 `active`、实现用 `claimed`，二者指**同一阶段**（已受理、有认领经理）。
⇒ 建议**保留实现名 `claimed`**，并在接口与前端文档里**显式登记 `active ≡ claimed`**。

理由：改名要同时动表注释、服务常量、前端镜像表、接口文档，代价大且极易留下半改状态；
**把映射写清楚**比改名可控（与 DR-0008 / DR-0017 那类"口径先对齐"的处置一致）。
⚠️ 该映射一旦在前端再现，**必须登记 CI 逐格比对**（规范 3.5）—— 否则就是静默失效。

### 5.2 状态集与迁移图（提议）

```
draft ──submit──> submitted ──claim──> claimed ──complete──> completed
  │                   │                                          │
  └──────cancel───────┘                                ──reopen──> claimed
                                                          （带理由 + 留痕）
```

- 新增终态 **`completed`**、命令 **`complete`**、时间戳 **`completed_at`**；
- 新增重开 **`completed → claimed`**（带理由 + 事件留痕）；
- ⛔ **不新增** `claimed →cancel→ cancelled`（**Q1 裁定**：DEMO-1 不做通用取消）；
- ⛔ **不新增** `completed →cancel→ cancelled`（**Q2 裁定**）；
- ⚠️ **重开不自动获得取消资格**（Q2）：回到 `claimed` 后，取消这条边**依然不存在** ⇒
  重开只能再走 `complete`，或走"已发生事实的独立处置规则"（该规则不在本设计范围）。

### 5.3 两个维度（关键设计）

| 维度 | 字段 | 取值 | 性质 |
| --- | --- | --- | --- |
| 运营状态 | `status` | `draft / submitted / claimed / completed / cancelled` | 命令驱动 |
| 财务结案 | `financial_status` | `not_started` / `open`（有未结余额） / `settled` | **由业务事实派生**（费用／结算／收付／处置记录），**不接受直接 PATCH** |

⚠️ **`closed_with_credit` 已从取值域中移除**（HO 0917 Q3 裁定）。原草案把它列为第四个取值，
HO 指出：**授信意味着"允许延期付款"，不意味着"钱已结清"** —— 把它做成财务状态会让
"钱没到账"在界面上显示成"财务已关闭"，这正是 PRD「运营完成 ≠ 财务结案」要防的那件事。

⇒ 正确落法（三件事分开记）：

| 事实 | 落在哪 |
| --- | --- |
| 运营已完成 | `status = completed` |
| 钱是否已结清 | `financial_status`（派生；有未结余额 ⇒ **`open`，不是已结**） |
| 允许延期付款的书面依据 | **另记**：授信依据 + 批准事实（独立记录，不改 `financial_status`） |

DEMO-1 主路径**先只做余额结清**，完整授信业务不在本轮范围。

PRD 要求「separate derived stage and financial closure status」与「运营与财务可区分」
⇒ **不塞进一个字段**。"两个维度"在**结构上**是两个字段，任何时候都可能不同步。

### 5.3.1 分期：本期**不开放**「运营完成但财务未结」这条路径（HO 0917-2 裁定）

⚠️ 上一版同时写了两句话，它们**在当前期不能同时作为可达规则**：

* 「允许 `status=completed` 且 `financial_status=open`」（本 §5.3 上文）；
* 「`complete` 的第 5 条前置是余额已处置 ⇒ `settled`」（§5.4 第 5 条）。

第二条成立时，第一条描述的状态**在本期不可达** —— 两条都在，实现就会分叉（照哪条写都对、
又互相矛盾）。**按 HO 0917-2 明确分期**：

| 期 | 「运营完成 + 财务未结」路径 | 说明 |
| --- | --- | --- |
| **DEMO-1（本期）** | **不开放** | 完成路径**要求结算已结清**：`complete` 的第 5 条前置（§5.4）拦住未结清的委托 ⇒ `completed` 与 `settled` 在本期**同时成立** |
| 后续期 | 待定 | 授信下"运营已完、财务未结"的路径**本期不做**；需要时按 §5.3 的三件事分开记的落法另行开放 |

⇒ 因此**"两个维度"在本期体现为结构与取值域已经分开**（`financial_status` 独立存在、
取值域独立于 `status`），**而不是**"本期就要支持四种组合"。实现 S4-a/S4-b 时
**不得**因为"文档说两个维度可不同步"就去放行未结清的 `complete`。

### 5.3.2 `financial_status` 三个取值的判据（HO 0917-2：不得只按余额判定）

⚠️ **`open` 不能只解释为"余额非零"**。以下任一条成立即属**未结**，与余额是否为零无关：

| # | 未结的成因 | 判据（按业务事实，不按余额） |
| --- | --- | --- |
| 1 | 已有费用未结 | 存在未结清的费用记录 |
| 2 | 结算**待批准** | 结算记录存在但未处于已批准状态 |
| 3 | **未决争议** | 存在未关闭的争议/异常处置 |
| 4 | **缺少收付依据** | 声称已收付但没有对应的收付凭证记录 |

⇒ **余额为零不自动等于 `settled`**：可能是"还没记账"，也可能是"有争议未入账"。
`settled` 的正向判据是**上述四条全部不成立**且余额已显式处置 —— 不是"余额这一个数等于 0"。

| 取值 | 含义（**HO 0917-2：不得用于掩盖未接通的派生逻辑**） |
| --- | --- |
| `not_started` | **只能**表示"尚未开始财务处理" —— 即确实还没有任何费用/结算/收付事实 |
| `open` | 已进入财务处理，但按上表任一条仍属未结 |
| `settled` | 上表四条全部不成立，且余额已有显式处置 |

⚠️ **`not_started` 不得当作"派生还没接好"的遮羞布**：S4-a 只落结构与迁移时，派生逻辑尚未接通，
此时**不得**让所有行默认显示 `not_started` 并宣称"财务状态已上线"。正确做法是
**在派生逻辑接通前，界面与接口不展示 `financial_status`**（或显式标注"派生未接通"），
而不是拿一个默认值冒充结论 —— 与项目铁律「未知保持未知」（规范 3.3）同源。

### 5.4 结案前置（硬门槛，来自 PRD 结案规则）

`complete` 必须**全部满足**才允许；任一不满足即 409，并**逐条报出缺哪条**：

1. **任务**：全部 `completed`，或按 disposition `cancelled`；
2. **交付证据**：要求的证据齐备（缺就是缺，不得"先过再说"）；
3. **异常**：无未解决的**阻塞性（critical）**案件；非关键残留须有显式 disposition；
4. **结算**：已批准；
5. **余额已结清**：按 §5.3.2 四条判据**全部不成立**且未结余额有显式处置 ⇒ `financial_status = settled`。
   ⛔ **授信不再是结案路径**（HO 0917 Q3）："`closed_with_credit` 且带依据"已作废 ——
   授信只说明"允许延期付款"，不构成余额已处置。
   ⚠️ **本期完成路径要求结清**（HO 0917-2）：本条是**硬门槛**，不满足即 409。
   ⇒ **DEMO-1 期间不存在「`completed` + `open`」的可达状态**（见 §5.3.1 分期表）；
   "运营已完、财务未结"的授信路径**本期不开放**，不是"留着以后再说"的待办。

⚠️ 前置检查**不得**留"管理员可跳过"的后门（PRD：Required hard checks cannot be bypassed
by chat, generic "complete", or administrator UI）。

⚠️ **HO 0917 另外点名"不可省略"的四条**（实现切片必须同时交，不是加分项）：

| # | 约束 | 为什么不可省 |
| --- | --- | --- |
| 1 | **客户确认** | 委托是替客户办事，运营方单方面宣布完成会让"完成"失去对客效力 |
| 2 | **幂等** | 重复提交 `complete` 不得产生第二条完成事件、不得重复扣减余额 |
| 3 | **MySQL 并发保护** | 沿用 `revision` + `expected_revision`（现状 `ent_assignment.py:14-18`）；两个写者同时结案必须有一个拿到 409，**不得后者静默覆盖前者** |
| 4 | **五类前置逐条报出** | 不满足时 409 且**逐条列出缺哪条**（现状设计 §5.4 首段），只回"不可结案"会让调用方无法自助 |

### 5.5 Q1–Q4 的 HO 裁定结果（2026-09-17，**已裁，不再是待决**）

| # | 问题 | **裁定** | 理由（HO 原文要点） |
| --- | --- | --- | --- |
| **Q1** | `claimed` 是否可 `cancel`？ | **否 —— DEMO-1 暂不新增通用取消** | 已发生采购、履约、费用后的终止**需要独立处置规则**，不是加一条状态边即可 |
| **Q2** | `completed` 之后能否 `cancel`？ | **否**。只允许有权限、有理由的**纠错重开**（`completed → claimed`）；**重开不自动获得取消资格** | 完成后直接取消语义矛盾；重开是纠错，不是"回到可以随便取消的状态" |
| **Q3** | `financial_status` 派生还是可写？ | **派生**（由费用/结算/收付/处置事实），**不允许直接 PATCH 一个状态就宣称结清**；授信**另记**、不改财务状态 | 授信＝允许延期付款，≠ 钱已结清 |
| **Q4** | 本设计是否进 S4？ | **是，必须进**。交付：完成状态 + **真实结案命令** + **受控重开** + 结案前置检查 + 并发保护 | 本就是**既有合同范围**，不再等待"是否做"的二次批准 |

⚠️ **Q1/Q2 的"否"不等于"委托永远不能终止"** —— 它说的是：**终止不是一条状态边能表达的**，
需要一套独立的处置规则（已发生事实怎么收尾）。那套规则**不在本文档范围**，
DEMO-1 期间 `claimed` 的委托仍无取消出口（与现状一致，**不是新引入的缺陷**）。

### 5.6 迁移口径（**Q4 已裁为"进"** ⇒ 实现切片按此执行）

- **新增迁移条目**，**不改已发布条目**（规范 3.4）；
- 新增列：`completed_at DATETIME NULL`、`financial_status VARCHAR(16) NOT NULL DEFAULT 'not_started'`；
  - ⚠️ `financial_status` 取值域是 **`not_started` / `open` / `settled` 三个**（§5.3 已移除
    `closed_with_credit`）；授信**不加列、不改这个状态**，走独立记录；
- 索引：既有 `(owner_user_id, status)` / `(org_id, status)`；"历史委托"列表建议加 `(status, completed_at)`；
- `status` 列注释同步为五个取值；
- **数据迁移**：既有 `claimed` 行**保持 `claimed`**（HO：旧数据**不推测完成**），
  `financial_status` 一律 `not_started`；
  - ⚠️ 这里的 `not_started` **只作列默认值**，**不等于"这些委托财务未开始"这个结论** ——
    迁移阶段派生逻辑尚未接通，按 §5.3.2 末条，**派生接通前不得对外展示该字段**；
- 幂等、可重跑（按迁移执行器既有约定）；
- ⚠️ **「追加迁移完成」不等于「结案能力完成」**（HO 0917-2）：S4-a 只落结构与迁移时，
  **不开放完成按钮**、**不生成「已完成历史委托」**、**不开放缺少前置检查的完成命令**。
  列存在但没有 `complete` 命令与五类前置时，正确表现是"这个能力还没有入口"，
  **不是**"给个按钮点了报 409"，更不是手改状态造出一条 `completed` 行；
- ⚠️ **`_CANCELLABLE` 保持 `(draft, submitted)` 不变**（Q1 裁定）⇒ 迁移**不扩大**可取消集。

### 5.7 对下游的影响

| 下游 | 影响 |
| --- | --- |
| 「已完成历史委托」夹具 | 有 `completed` 才铺得出"走完生命周期含结案"的委托；夹具脚本需能构造 §5.4 的五条前置。⚠️ **HO 0917 硬约束：该夹具必须通过真实业务命令生成，不得直接改状态造出来** —— 手改状态产出的"已完成委托"不证明结案链路可用，会让 runbook §7 那一行变成假绿 |
| 合同 13 步主脚本第 8–12 步 | 同样依赖结案能力（**Q4=是 ⇒ 不再被"是否做"阻塞，改由实现排期决定**） |
| 前端镜像表 | 状态枚举一旦在前端再现 ⇒ **必须登记 CI 逐格比对**（规范 3.5）；`financial_status` 若在界面出现，同样要登记 |
| 案件层 | `close_case`（**案件**结案）与委托 `complete` 是**两个层级**，**不得互相替代** |

## 6. 本文件不做的事（口径已定，**实现另开切片**）

- **本文件不动代码** —— HO 裁的是"做不做、按什么口径做"，实现仍走独立切片 + 门禁 + PR；
- 不改任何既有阈值 / 判据；
- **不把现有 `claimed` 数据"推断"成 `completed`** —— 未知保持未知（规范 3.3）；
- ~~不替 HO 决定 Q1–Q4~~ ⇒ **已由 HO 0917 裁定**（见 §0 / §5.5），本条作废。

## 7. 实现切片的建议拆法（**供排期参考，不构成承诺**）

HO 说「先定 S4 口径并**继续当前切片**」⇒ 口径与本文件的落地**可以分开**：

| 切片 | 内容 | 依赖 |
| --- | --- | --- |
| S4-a | ✅ **已交付（仅结构部分，2026-09-17）** —— 状态与迁移：`completed` / `completed_at` / `financial_status` + §5.6 迁移 | 无（可独立先做）。⚠️ **只做结构与迁移**：不开放完成按钮、不生成「已完成历史委托」、派生接通前不展示 `financial_status`（§5.6 末条）。⚠️ **不抢占 S2 主线**（HO 0917-2） |
| S4-b | `complete` 命令 + §5.4 五类前置 + 逐条报缺 + 并发保护（409） | S4-a |
| S4-c | 受控重开 `completed → claimed`（理由 + 事件留痕） | S4-b |
| S4-d | 「已完成历史委托」夹具（走**真实业务命令**，非手改状态） | S4-b |
| S4-e | 终止/取消的独立处置规则（**Q1 明确不在 DEMO-1**） | — |

⚠️ **S4-e 仍不排期**：Q1 裁定是"暂不新增通用取消"，不是"以后一定补"，**别把它读成待办**。

### 7.1 S4-a 的交付与一条待认可的口径解释（2026-09-17 补记）

S4-a 已交付：`ent_assignment` 追加 `completed_at` / `financial_status` 两列 + `(status, completed_at)` 索引，
`status` 列注释同步为五个取值；三条迁移**只做 DDL**（无 `UPDATE` ⇒ 历史 `claimed` 行不被推断成 `completed`）。
交付清单、8 条闸门用例与实测证据见 `docs/entrust/S4-a-结构与迁移切片.md`。

⚠️ **口径解释（可争议，待 HO 认可）**：§7 表里 S4-a 写作「**状态**与迁移」，本切片把「状态」
落在 **schema 与列注释**上（五个取值），**没有**在 `assignments.py` 里加 `STATUS_COMPLETED`。
理由是一条连锁反应：`verify_entrust_ui.js` 强制前端 `STATUS_META` / `STATUS_ORDER` / `STATUS_HINT`
与后端 `STATUS_*` **逐格一致**，而 `STATUS_ORDER` 派生**工作台筛选条** ⇒ 加常量的必然结果是出现一个
「已完成」筛选片，而**此刻没有任何数据能处于该状态** —— 与 §5.6 末条「列存在但没有命令时，
正确表现是这个能力还没有入口」冲突。⇒ 代码取值域与前端镜像留到 **S4-b 与 `complete` 命令同一个提交**
里一起改（代价：一个常量 + 前端三张表，**无迁移**）。若 HO 认为应当此刻同步，改起来是一笔独立提交。
