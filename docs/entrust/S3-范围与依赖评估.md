# S3 范围与依赖评估（**只评估，不开工**）

| 字段 | 内容 |
| --- | --- |
| 状态 | **评估稿 + 裁定更新**。本文件**不承诺排期、不承诺百分比、不声称任何 AC 通过**；它回答"要做什么、现在有什么、卡在哪" |
| 日期 | 2026-09-17（**0917-3 裁定三后更新**：A/B 已裁定并批准开工，数据底座已落 —— 见 `S3-发布与客户响应数据设计.md`） |
| 依据 | `DEMO-1-contract-v1.0.md` §8.1（S3 出口）、§4 BP-03（十条必需实现）、§9（主验收 D1-06/07/08/11/15）、§10.1 主演示脚本第 4–7 步 |
| 相关 | `DEMO-1-plan.md`、`DEMO-1-readiness.md`、`DEMO-1-interface-delta.md`、`DEMO-1-fixture-manifest.md` |
| 为什么单独立文 | 合同 §8.4 要求"发现的范围增量进剩余台账，除必要出口外不加承诺"。S3 的规模**明显大于 S2 剩余部分**，先评估再决定是否开切片 |

## 1. 合同对 S3 的定义（逐字口径）

- **Slice**：`S3 — Commercial commitment`；**Work**：`BP-03`；
  **Exit**：`Supplier confirmation, released offer, customer exact-version response, contract record`。
- **主验收**：D1-06（两家报价可比、只有合规证据才可确认运力）、D1-07（客户接受/拒绝**精确发布版本**；
  未发布或被取代的响应必须失败）、D1-08（合同从已接受事实派生；证据与状态**不得**等同于实时电子签）、
  D1-11（越权读写/下载/作业失败且不泄漏）、D1-15（见证人改一个受支持的值，下游随之变且刷新后仍在）。
- **主演示脚本第 4–7 步**：三段计划与任务前置 → 两家比价 + 证据支撑的采购确认 → 发布报价、
  客户接受**该精确版本** → 生成合同并记录**标注来源的**样件签署证据。

## 2. 现状矩阵（逐条对 BP-03 的十条必需实现）

| # | BP-03 要求 | 现状 | 性质 |
| --- | --- | --- | --- |
| 1 | 三段计划 + 并行的单据/操作任务 | ✅ **后端（读＋写）、界面（读＋**写**）与设备侧证据均已具备**（2026-09-18）：7 个任务槽位（`collect_documents`/`quote`/`purchase`/`contract`/`execution`/`handover`/`settlement`）已就位；"三段（公路—内河—公路）"已有载体 `ent_leg`。**读**：`GET /assignments/{id}/plan` ＋ 委托详情页「运输计划」卡逐行显示航段与**必需任务前置**。**写**：航段命令三条（建段 / 改段留版本 / 版本历史，`scope_matrix` 81 → **84**）＋ **写侧界面**（加一段 / 改这一段 / 版本历史展开，**页内表单**），口径＝HO 2026-09-17 裁定（谁能建段＝任意验收者/测试者 · **不强制**公–水–公 · 改段**保留版本**）。设备侧：读侧 **㊻ 章**、写侧 **㊼ 章** | 原判"需新领域对象（或明确以任务描述承载）"**已裁定并兑现**（最小结构化航段对象）。⚠️ 剩余**不是**缺口而是档位：**设备侧证据 ≠ 业务验收**（见 `DEMO-1-interface-delta.md` §7.21 / §7.22 / §7.24） |
| 2 | 两家报价可比（运力/价格口径/数量单位/有效期/证据可见） | ✅ **载体与界面已成**（2026-09-17）：`supplier_compare` 类型已注册 + AG-02 已能产出比价提案；**候选运力表已落** `ent_capacity_candidate`（可比的每一样都在列上：`rate` / `rate_unit` / `currency` / `valid_until` / `evidence_kind` / `evidence_ref`）；界面有**两条候选并排比较**（`DEMO-1-interface-delta.md` §7.18）。⚠️ **并排比较没有设备侧证据** —— ㊺ 章明说不验它 | 原判"需接线 + 少量新字段"已兑现。剩余缺口＝**该界面的设备侧走查**（证据另行登记） |
| 3 | 「选中报价」与「确认运力」分离；过期/不适用资源不得确认 | ✅ **后端与界面均已实现，且已有设备侧证据**（2026-09-17）：`capacity.confirm_capacity` + **6 条端点** + 四条确定性规则（需求口径／证据／有效期／容量）逐条判定，并有一条**闸门**（每规则必须产出判定，缺一条即中止）。**候选登记与确认分属两条命令**，只登记候选不产生任何确认痕迹。界面＝委托详情页运力块（登记 / 确认 / 只读复算）；㊺ 章设备走查 **`PASS=22 / FAIL=0 / NOT_RUN=0 / LIMITATION=0`**（截图 `miniapp-device-artifacts/walk-20260917-215133/`） | ⚠️ **设备侧证据 ≠ 业务验收**：前者答"真机上点得动、写进去读得回来"，**后者是 HO 的裁定** ⇒ 不得据此声称 BP-03 第 3 条"按业务结果可演示"。另：界面「状态冲突 ⇒ 刷新」那一支**驱动不到**（候选确认后 `confirmable=false`，确认条不再出现）⇒ 由静态门禁守，**不得**记成"设备侧验过"。确定性命中口径：**不得**用 LLM 判断 |
| 4 | 对客报价用**服务端**客户白名单投影 | ✅ **已接线**：`registry.CUSTOMER_VISIBLE_TYPES = {customer_quote, contract_review}` 与 `project_for_customer()` 已在**对客通道**上使用 —— `project_release_for_manager` / `project_release_for_customer` 两条投影由**服务端选**（AC-26），前端只拿到选好的那一份，**不是"先返回再隐藏"** | 原判"需接线"已兑现。判据＝**逐字段断言内部字段不出现在客户投影里**（已挂静态门禁） |
| 5 | 发布一个**不可变**的对客报价版本 | ✅ **后端与界面均已实现**：`offers.release_offer` + `POST /entrustments/{eid}/offer-releases` 绑定**精确 revision**、存**发布那一刻的客户白名单快照**、显式取代旧的可响应版本；经理侧入口在成果页**版本行**上（页内确认条） | 界面已上线（`DEMO-1-interface-delta.md` §7.11）；**该界面的设备侧走查未覆盖** ⇒ 不得记成"设备侧验过" |
| 6 | 认证客户查看并接受/拒绝**该已发布版本**；留 actor/time/target revision/response | ✅ **后端与界面均已实现**：`GET /my-offer-releases` / `GET /offer-releases/{id}` / `POST /offer-releases/{id}/responses`；响应留客户身份、时间与**精确版本**。客户侧入口＝委托详情页「对客报价」卡（页内展开条） | 界面已上线；**该界面的设备侧走查未覆盖** |
| 7 | 拒绝"未发布/已被取代"的接受；经理人评审**不得**伪造客户接受 | ✅ **后端已实现且已验**：未发布/已撤回/已被取代 ⇒ 409；经理冒充 ⇒ **403**；同一次发布重复响应 ⇒ 唯一约束兜住（含**真 MySQL 并发**用例） | — |
| 8 | 由已接受事实 + 已批准范围生成一份结构化合同草稿 | ✅ **后端已实现**（2026-09-17）：`contracts.derive_contract` + `POST /offer-releases/{id}/contract`，并按依赖 E 落了**逐字段来源表** `ent_contract_field_source`（闸门：合同每个字段都必须有来源行，缺一个就中止）。**界面未做** | **界面未做**（原判"需新领域行为"已兑现） |
| 9 | 签署证据可上传/记录并带**来源与模式标注**；保留历史版本 | ⚠️ **模式标注已落**：`offers.SIGNATURE_MODE_LABELED_SAMPLE = "labeled_sample"` 随发布快照冻结；数据来源标注另表 `ent_artifact_origin`（`live` / `synthetic` / `manual` / `unknown`），判据取作业行的 `mocked` 事实、**不采信调用方自述**，`unknown` 不猜成 `live`。版本链本就在（`POST /attachments` + 成果版本 append-only）。⚠️ **取证动作未做**：上传通道**不接收** `evidence_kind`，把签署证据**绑到合同版本**上的动作缺失 | 剩余缺口＝**取证动作**（与 D1-03/D1-05 同类）。⚠️ 这是"**动作缺失**"，不是"字段缺失" —— 别照抄旧口径 |
| 10 | 客户旅程所需的查看/下载，且**不暴露**内部成本、会话、受保护附件 | ✅ **已接线**：`GET /offer-releases/{id}/attachments/{aid}/download` —— 判据**只有**发布时冻结的授权清单，**清单外一律 404**（不用 403：403 会承认内部底稿存在）；前端 `downloadOfferAttachment` 走的就是这条口，**不经**通用附件下载口 | 原判"需接线 + 负例证据"已兑现（"清单外 404"即负例）；内部成本/会话不进客户投影由 §2 第 4 行那条投影判据守 |

**合计（评估当时的快照，见下方 2026-09-17 重算）**：10 条里 **0 条已完整交付**，
3 条"半成品可接线"（4/8/9），3 条"部分"（1/2/10），4 条**完全未实现**（3/5/6/7）。
这与 S2 的形态**不同** —— S2 是"后端齐备、前端未接"，
S3 是**先要新领域概念**（发布版本、客户响应、运力确认），再谈界面。

> ⚠️ **2026-09-17 重算（本节旧文保留，不重写）**：上面那段是**评估当时**的快照，后续切片
> 已把多数行推进（行注已逐条更新，§3 与 §6.1 是裁定与进展的落点）。当前口径：
> **后端已实现** 3/5/6/7/8/10（8 见 `S3-合同派生切片.md`，3 见 `S3-运力确认与有效期切片.md`）；
> **1 的读模型与界面已成**（`plan.py` ＋ 详情页「运输计划」卡，见 `DEMO-1-interface-delta.md` §7.21）；
> **2/3/4/5/6/10 的载体与界面均已成**（2026-09-17 逐行复核：候选运力表 + 并排比较、运力确认界面、
> 客户白名单投影**已接线**、发布 / 响应 / 下载三处的界面）。
> **"部分"只剩 9**（**模式标注已落、取证动作未做**）—— 十条里**没有一条**还停在"完全未实现"；
> **界面未做**只剩 **8**（合同派生）；1–7、10 的界面均已上线。
> 剩下的缺口按性质只有四类：**界面**（8；以及 1 的**写侧**界面 —— 建段/改段没有页面入口）、
> **取证动作**（9 的 `evidence_kind` 落到**合同版本**上）、
> **设备侧走查**（1 / 5 / 6 / 7 / 8 至今 `NOT_RUN`；2 的并排比较 ㊺ 章**明说不验**）。
> ✅ **"建段命令"这一项已于 2026-09-17 裁定并落地**（HO 三条口径 ⇒ `ent_leg_revision`
> ＋ 三条写/读端点，`scope_matrix` 84；见 §7.22）—— 它**不再**是缺口，
> 但**后端已成 ≠ 可演示**：写侧界面还没有，设备侧也未走。
> ⚠️ 3 已有设备侧证据（㊺ 章 `PASS=22 / FAIL=0`），**但"设备侧验过"≠"业务验收"**。
> ⚠️ 本节表格与这行注此前**互相矛盾**（表里写"界面未做"、注里写"界面已上线"）——
> 2026-09-17 已逐行对齐，并对齐到维护中的 `DEMO-1-plan.md` §3.3。
> ⛔ 本条**不是**"已完成 8 条"：合同 §8.2 的口径是**报告哪些业务出口可演示**，
> 不数端点；"后端已实现"与"按业务结果可演示"之间隔着界面与设备侧走查两道。

## 3. 依赖与咽喉（按阻塞强度排序）

> **0917-3 更新**：下面 A 与 B 已由 HO **明确裁定**（客户＝委托货主本人、不新增全局角色；
> 发布＝独立记录 + 精确 revision + 客户白名单快照 + 客户响应绑定发布），
> 并且**数据底座已经落地**（迁移 `ent_commitment`：`ent_leg` / `ent_capacity_candidate` /
> `ent_offer_release` / `ent_offer_response`）。见 `S3-发布与客户响应数据设计.md`。
> ⚠️ **落地的是结构，不是能力**：命令、端点与界面**均未实现**。
> ⇒ 上面这句对**当时**成立。其后逐片落地：A/B 相关命令与端点、D（签署模式标注）、
> E（合同派生）、C（运力确认与有效期）**均已实现**；**仍不成立的是界面**（1/2/3/8）
> 与**设备侧走查**（3/6/7/8 至今 `NOT_RUN`）。

| # | 依赖 | 为什么是前置 |
| --- | --- | --- |
| A | **客户身份与入口** | D1-07 要"认证客户"接受精确版本。当前三个角色是 `shipper/owner/port`，**没有客户通道**；第 6/7/10 条都挂在它上面。先要裁定"客户＝货主本人"还是"独立客户角色" |
| B | **不可变发布版本** | 第 5/6/7 条共同前置。需要一个"把某个成果版本标记为已发布"的概念（新列 + 追加迁移），且发布后该版本**不可改写**（只有新版本） |
| C | **运力/有效期的事实载体** | ✅ **已落**（2026-09-17）：`ent_capacity_candidate`（候选事实）+ `ent_capacity_confirmation`（确认，**冻结**判定读到的每一个值）+ `ent_capacity_rule_check`（**逐规则**判定）。规则**不发明口径**：容量那条逐字取自 `demo1_canonical.json` 的 `deterministic_capacity_rule`（候选适用 ⇔ `capacity_tonnes ≥` 该水运段实际承运量；声明单船且不拆批 ⇒ 必须**一条船**装下）。见 `S3-运力确认与有效期切片.md` |
| D | **签署证据的模式标注** | 第 9 条 + §3.2「合同草稿/上传的样件签署证据 **不得**表示为实时电子签」，与 D1-03（live）/D1-05（offline）是同一类"来源必须可分" |
| E | **合同生成规则** | 第 8 条要求"从已接受事实派生"，需要一个可核对的字段来源表（哪些字段来自哪份已接受版本）。✅ **已落**（2026-09-17）：表 `ent_contract_field_source` 逐 (`field_path`, `source_kind`, `source_ref`) 一行，判据在一个事务里写入并带**双向**闸门（合同缺来源 ⇒ 中止；来源指向不存在的字段 ⇒ 也中止） |

## 4. 与现有切片的接口面（必须先落的口径，再谈实现）

- **新增端点必须登记 `scope_matrix`**（条目数锁定测试会红；本轮 S2 第三片是 63 条，S3 只会增）；
- **客户侧数据必须走服务端白名单投影**（§3.2 / AC-26：不得"先返回前端再隐藏"）；
- **新增状态/列只走追加迁移**，历史行**不推测回填**（既有规范）；
- **值映射在前端再现 ⇒ 必须登记 CI 逐格比对**（`verify_entrust_ui.js` 的取值域表）；
- **客户响应与发布都必须幂等**，且并发下的"同一版本只能被响应一次"要有 MySQL 级保护。

## 5. 规模判断（不做百分比承诺）

- S2 第三片（上传→提取→引用）是**一天量级**的接线工作；
- S3 的 A–E 五个前置里，**A 与 B 属于新领域概念**，C 属于新校验规则，D/E 属于派生与标注；
  它们彼此**可以并行**（A/B 互不依赖），但**每一条都要独立切片 + 门禁 + 证据**；
- ~~因此在 A（客户通道）与 B（发布版本）的口径被裁定之前，**不建议**开工任何界面部分~~
  ⇒ ✅ **该前置已解除**（HO 0917-3 裁定三）：A/B 口径已定、**并已批准开工**，
  数据底座（`ent_leg` / `ent_capacity_candidate` / `ent_offer_release` / `ent_offer_response`）
  已落。现在**不建议**的只有一件事：把"界面先画出来、底层概念再说" ——
  顺序仍是**先命令与端点，再界面**，因为"发布"这个概念一旦画在页面上就必须是对的。

## 6. 本文件**不做**的事

- 不排期、不给百分比、不声称任何 D1 通过；
- ~~不替 HO 决定 A（客户身份口径）与 §2 第 1 条（航段是否值得独立实体）~~
  ⇒ ✅ **两项均已裁定**（HO 0917-3 裁定三）：A＝客户为当前委托的货主本人；
  §2 第 1 条＝**需要最小结构化航段对象**（已落 `ent_leg`）。本文件从此**不再列待决策项**；
- 不动任何代码、不加迁移、不改阈值（**本轮之后**：实现走独立切片，本文件只做范围评估）。

## 6.1 决策清单：**无未决项**（2026-09-17 起）

| 原"待裁决"项 | 现状 |
| --- | --- |
| A 客户身份与入口 | **已决策** ⇒ 开工 |
| B 不可变发布版本 | **已决策** ⇒ 开工 |
| §2 第 1 条 航段实体 | **已决策**（最小结构化对象）⇒ 已落表 |
| 发布前来源核验 | **已决策**：列为 S3 必须实现的门槛 |
| C 运力/有效期事实载体 | **无需决策**（裁定三已给"确定性规则、不得用 LLM 判断"的取向）⇒ ✅ **已实现**（2026-09-17，见 `S3-运力确认与有效期切片.md`） |
| D 签署证据模式标注 / E 合同派生规则 | 无需决策 ⇒ 实现 |

## 7. 与并行工作的关系

| 可并行项 | 依赖 S3？ | 说明 |
| --- | --- | --- |
| 可恢复 UI 兜底（HO 顺序 4） | 否 | 只涉及解析失败的用户侧恢复，与 S3 无关 |
| S4-a 结构与迁移（HO 顺序 5） | 否 | 委托结案状态机，与 S3 无接口重叠；但 HO 要求"不抢 S2 主线" |
| 「已完成历史委托」夹具 | 是（S4） | 与 S3 无关，属 S4 |
| 客户通道（A） | — | **已裁定、已批准开工** ⇒ 它是 S3 的第一块砖（发布→响应的纵向切片从它切入） |
